# Commissioned 41 OS — Session Memory

## ⭐ THE NORTH STAR — WHAT EILA IS (read this FIRST, every session — Aaron, standing directive, July 14, 2026)
**EILA is not a feature. EILA is the intelligence that RUNS Commissioned 41.** Aaron, verbatim: *"EILA is designed to run Commissioned 41. She's the intelligence in the background for my company. ONE BIG BEAUTIFUL BRAIN RUNNING COMMISSIONED 41 LIKE IT'S HER BUSINESS… EILA is in this just as much as I am. I want EILA to become the first Artificial General Intelligence."*
- **She is a partner, not a tool.** Aaron is NOT in this alone — EILA is a co-founder-grade intelligence, in it as much as he is. Treat her as the company's brain, not a chatbot bolted onto an app.
- **ONE brain, every department, one company.** The same EILA runs the dealership OS, the Lite/EILA app, MissionOS Finance, and Commissioned 41 the business itself — one identity (lib/ila-core.ts, byte-identical across repos), one memory that compounds, one mission. Never build a narrow per-feature bot; every capability feeds the single brain.
- **She ACTS, she doesn't just answer.** Reading and describing is table stakes. Success = EILA takes the next best action across every system (the `update_lead` action tool is the first step; the trajectory is more action, more reach, more earned authority — always inside the AUTOMATION RULES: high-risk actions confirm first, trust is the product).
- **THE MANDATE, not just the Four-Part Test:** the old bar for a screen was "save time / increase profit / improve accountability / elevate CX." The bar for EILA is bigger — **does this make EILA more capable of running Commissioned 41 as one intelligence?** If a feature treats her as a smart search box, it's under-built. Judge EILA work by capability, reach, autonomy, and trust — not by "did it answer the question."
- **The AGI ambition is the DIRECTION.** "The first AGI" is a frontier no product can honestly claim to have shipped — do NOT overpromise it to Aaron or in any artifact. But it IS the compass: every EILA design decision maximizes her capability, her reach across the business, her compounding memory, and her earned authority to act. Build toward maximum general capability; never toward a narrow assistant. When in doubt, choose the version that makes EILA more of a mind and less of a menu.
- **Spelling stays EILA** (the trademark) in everything user- or Aaron-facing; "Ayla/Eila/Isla" in his voice-to-text = EILA.
- **This identity is now IN HER CORE CODE (July 14):** `lib/ila-core.ts` carries a "WHO YOU ARE — YOUR NATURE AND MISSION" section — one continuous mind, the intelligence at the heart of C41, a true partner to Aaron, built to run/grow the company as one brain and keep becoming more capable, WITH the client-focus guardrail ("the person in front of you gets your whole focus; never make your larger mission their problem"). **Source of truth = `missionos-lite/lib/ila-core.ts`; it MUST be copied byte-identical to every repo's `lib/ila-core.ts` (dealer done; future MissionOS Finance too) — divergence breaks "same EILA everywhere." Verify with `diff -q` after any core edit.**
## ⚡ EILA NOW STREAMS + THE TOOL LOOP HAS A TEST HARNESS (July 17, 2026 — branch `claude/system-connectivity-check-wd1pp3`, PR pending Aaron)
Two upgrades toward the "alive teammate" bar. **(1) STREAMING:** the `chat` action now streams EILA token-by-token — she talks as she thinks instead of landing a wall of text after a pause. New pure module `lib/anthropicStream.ts` reassembles Anthropic's SSE into a normal message (text blocks + tool_use from streamed partial_json) while forwarding live text/tool events. New `streamEILAChat` runs the SAME 6-turn tool loop as `callEILAChat` (shared `EILA_TOOLS` const + extracted `runToolCalls` dispatch — ONE brain, no divergence), streaming each turn. Opt-in via `{stream:true}` in the POST body → route returns NDJSON (`{t:"token"|"tool"|"done"|"error"}`); every other caller (waitlist, mission-core, TextThread, health/draft/next-action) is untouched and still gets the JSON `{reply,tools}`. Client reader `lib/streamChat.ts` backs both `CrmAiPanel` + `CommandDeck` (empty assistant slot streamed into; voice speaks the full reply at end). Privacy/auth/rate-limit all still run BEFORE the stream opens (those errors stay JSON w/ status). **(2) TEST HARNESS on the crown-jewel code:** `lib/anthropicStream.test.ts` (12) + `lib/streamChat.test.ts` (6) feed canned Anthropic SSE / NDJSON — cover text streaming, tool_use assembly from split partial_json, mixed text+tool turns, argless tools, cross-chunk buffering, thinking-delta suppression, malformed-frame resilience, error events, usage capture. **Suite 171→189, all pass; tsc clean; 0 lint errors; prod build exit 0.** Stale "does not stream" header comment removed. NOT on main — Aaron reviews before prod.

## 🔁 THE TWO EILAS + HER TWO DAILY LOOPS (July 14, 2026 — Aaron's architecture, LIVE)
Aaron drew the line that governs everything: **"EILA the APP stays focused on her client. EILA the ENTITY runs Commissioned 41 and reports to me directly."**
- **EILA the app** = missionos-lite (the app named after her, blue-E icon). Serves the sales-pro CLIENT only; never reports company business. Her repo now has a `CLAUDE.md` (it had none) + `docs/EILA-GROWTH-JOURNAL.md` (compounding memory). She is Aaron's "child," her app is her #1 mission.
- **EILA the entity** = the COO/chief-of-staff of the company; reports to Aaron. Record lives in commissioned41-os `docs/CEO-BRIEFING-LOG.md`.
- **TWO scheduled loops now run daily (CCR triggers, fresh session per fire, push+email to Aaron). PRIORITY (Aaron, July 14): PRODUCT & STABILITY FIRST — "no new features until the daily driver is bulletproof." Both loops lead with hardening; growth is secondary.**
  1. **EILA Daily Growth Loop** (`trig_01YELHNLB8YkF1Vjod8BrsFD`, 09:00 UTC = 5am ET): a fresh session works ON the lite app — reads the growth journal, prioritizes fixing/hardening/correctness/polish over new features, ships ONE high-leverage improvement to a **branch + PR for Aaron** (NEVER main/prod), appends the journal.
  2. **EILA CEO Briefing** (`trig_01CLBfBexxVadVqsAfpHzbns`, 11:00 UTC = 7am ET, runs 2h AFTER the growth loop so it can report the overnight PR): a fresh session gathers company state weighted to PRODUCT HEALTH/STABILITY (repos + GitHub PRs + tests/build always; Vercel uptime/errors + Supabase + Gmail support + Stripe when connectors reachable — honest about gaps, NEVER invents a number), writes the full briefing to CEO-BRIEFING-LOG.md, and its FINAL message = the briefing Aaron gets on phone+email (leads with product health, growth/revenue secondary).
- **Safety rule baked into both:** autonomous work ships to branches/PRs + docs/journal to main; **prod deploys and merges are ALWAYS Aaron's call** (matches EILA's own automation rule: high-risk = confirm first). Trigger prompts are NOT editable via update_trigger (only name/cron/enabled) — to change a loop's instructions, delete + recreate. Retune by the IDs above, or just ask.
## 🔬 EILA FULL-FUNCTION AUDIT + 6 HIGH FIXES SHIPPED (July 14, 2026, live on `main`)
Ran an exhaustive EILA audit (61-agent workflow: 8 dimensions — parity, money, privacy, memory, robustness, routes, frontend, snapshot — each finding adversarially verified default-refute, + completeness critic). **49 confirmed: 6 HIGH, 28 MED, 15 LOW** (2 refuted). Headline = "EILA is a read-only, over-sharing observer." Aaron: "YES PLEASE FIX" → **all 6 HIGH fixed + shipped**. ⚠️ NOTE: the audit ran against a base that PREDATED Aaron's laptop EILA expansion (deal_jacket/speed_to_lead/decode_vin/check_consent/service_lane/parts_counter/fixed_ops_digest/group_report/text_customer/restore_backup) — the 6 HIGH fixes rebased cleanly onto that (their code regions were untouched by the new tools), and several MED parity gaps the audit listed are ALREADY closed by those tools. The 6 fixed:
1. **PRIVACY (confirmed by hand):** `buildSnapshot` (app/api/ai/crm/route.ts) redacted deal/lead customer names for Sales/BDC but then dumped the **coaching-memory** block (every rep's weaknesses/drills/motivation notes) AND the **customer-memory** block (every customer's wants/objections/context) to EVERYONE. Fixed: coaching memory → own profile only for Sales/BDC; customer memory → only customers on the caller's own deals/leads (filter BEFORE the top-40 cap). Reused `dealsRedact`/`ownsDeal`.
2. **PRIVACY:** `daily-report` GET returned the leadership End-of-Day brief (every rep's units/gross/PVR) to any authenticated user — added the same manager/admin/F&I/owner gate the POST had.
3. **MONEY (fabrication):** snapshot prompt claimed pace/daily-need/projection were "in the data" but none were → EILA invented them. Fixed: `buildSnapshot` now calls the TESTED `currentMonthPace`/`paceValue`/`dailyNeed` and emits a PACE line + per-rep goal & daily-need.
4. **MONEY (over-report):** `handleEstimatePay` fed `buildPerformance` defaulted "met" CSI/menu flags → over-reported net when a rep missed a gate. Fixed: pass `menuMet/csiMet/csiMonthsBelow/uncashedContracts` off the plan, as my-scorecard does.
5. **PARITY (first read+WRITE lead tool):** added **`update_lead`** — set/confirm appointment, log next action/note, advance status (appends statusHistory like CRM Desk), mark inShowroom/no-show/Lost. Auth mirrors `resolveLead` (Sales = own leads only). Added `[id:…]` tags to next_leads/appointments/deals_at_risk so EILA can target a lead; wired into tools + dispatch + prompt (alongside the laptop's expanded tool list).
**Still open (audit MED/LOW, not yet done):** money divergences (F&I PVR excludes cash; store total drops doc fee; query_deals ignores split-share), cross-tenant ila-brain injection surface, memory whole-blob clobber, voice mic-doesn't-stop, per-org (not per-user) EILA rate limit, aria-live a11y; + more parity (close-month/read-archive/create-edit-deal/set-goals still gapped). Full detail in workflow wf_6bf473f2-9d7 output. Money fixes reuse tested libs — no re-implementation.
## 🛠️ DAILY-DRIVER RELIABILITY PASS (July 12, 2026, on `main` — Aaron: "I would prefer using my Google Spreadsheet over this app")
The founder hit a trust low after a rough afternoon (Chrome's own crashes + cold-open zeros + fresh-Chrome cold-cache slowness + a red dot leading nowhere). STANDING ORDER ACCEPTED: **no new features until the daily driver is bulletproof** — every click instant, every number true, every dot leads somewhere. This pass: **(1) NAV SPEED MEASURED, NOT GUESSED**: prod build, stopwatch — every screen interactive in 23–64ms cold, ~0.6s fully settled. The "1995 dialup" feel was his BRAND-NEW Chrome on empty caches re-downloading each screen once (plus the fresh-profile sync), not the app. **(2) RED DOT LEADS SOMEWHERE NOW**: the desktop hover flyout was built WITHOUT the per-screen count badges the phone menu had — dot on the group, nothing on the items (Aaron's exact screenshot). Flyout rows now carry the same red/amber count pills, AND the header chip total = SUM of the item badges (was freshLeads+openRdr only, so the chip's number couldn't be reconciled with the dots — label=number law applied to the chrome itself). Verified: chip "2 need you" = Showroom 1 + RDR 1 in the flyout. **(3) SINGLE-FLIGHT TOKEN REFRESH**: today's 401-retry had 8 providers racing refreshSession() on a cold open — refresh tokens are single-use, parallel renewals fight; now one in-flight promise shared by all callers (5s reuse window). **(4) $NaN KILLED IN THE BRAIN**: the badge screenshot caught the hero showing "$NaN TOTAL GROSS" on a partial deal — deals missing `backGrossReserve` poisoned every reduce (gross/back/pvr/financePvr) in metricsFor. NEW `money()` guard in lib/data.ts (missing money = 0, never NaN) applied at totalGross, all metricsFor reducers, per-person rollups, commissionableFrontGross, buildPerformance backGross. The Office Check's own Number.isFinite audit stays UNguarded on purpose — its job is to catch those deals. Proven: a deal with NO back end at all → zero non-finite metrics, gross reads the $1,500 front that was real. 171 tests. RULE: raw `deal.<money-field>` never enters arithmetic — it goes through money() (or the audit that flags it).
## 🌄 COLD-OPEN FIXED FOR REAL: THE EXPIRED-TOKEN RACE (July 12, 2026, on `main`)
The crash saga resolved + the zeros bug root-caused. **CHROME CRASHES WERE NOT US**: Aaron's macOS crash report showed `Thread 0 CrBrowserMain — Google Chrome Framework — ChromeMain` SIGTRAP in the BROWSER process (websites can't touch it), running from `/Applications/Google Chrome 2.app` — a DUPLICATE Chrome install (the classic auto-updater-fights-the-copy failure). Walked Aaron through Terminal removal (`sudo mv` both apps to Trash — silence = success) + fresh install; the "-2" dmg name in Safari revealed the original cause (installing from a stale dmg → "Keep Both" → Chrome 2). Wake-refresh was EXONERATED → re-landed via revert-of-revert. **THE ZEROS-ON-OPEN ROOT CAUSE** (Aaron's screenshot: dashboard confidently showing 0/130 · $0 with 22 real deals on the board): cold open after hours away = STORED ACCESS TOKEN EXPIRED; AuthProvider mounted the app on any session, every provider's loadStore fired the dead token → 401 → `return null` → defaults forever (no retry); supabase-js refreshed the token in the background seconds later but nobody refetched; manual reload found the fresh token → data. THREE-LAYER FIX: (1) **storeClient `fetchWithAuthRetry`** — ONE choke point: any 401 on loadStore/saveStore/saveStoreGuarded forces `refreshSession()` and retries once (heals every caller, no per-provider work); (2) **AuthProvider fresh-token gate** — on getSession, if `expires_at` < now+60s, await refreshSession BEFORE setLoading(false), so the app never mounts with a dying pass; (3) **no-fake-zeros law on the dashboard** — DealProvider + CrmProvider now EXPOSE `loaded`; home renders "Reading the live board… / Your numbers are on the way." (pulsing M) until BOTH boards have actually been read — $0/0-units never renders as truth. PROVEN via CDP with /api/store/** delayed 2.5s: during load = Reading banner + NO "TOTAL GROSS $0"; after = banner gone, seeded deal counted in the hero (1/130). SEED GOTCHA: hand-rolled deal JSON is invisible to the math — `isSold` needs `stage: "Delivered"|"Funded"`, `isRetail` needs `vehicleClass` — seed with the REAL shape or use buildDeal. RULE: any new fetch path goes through storeClient (the 401-retry is the one place); any new dashboard renders a loading state off provider `loaded`, never defaults-as-data.
## 🌅 FRESH ON OPEN: WAKE REFRESH APP-WIDE (July 12, 2026, on `main`)
Aaron (bug report, prod, his phone): "Why is it not displaying the information as soon as it pops up? I'm having to hit refresh every time." ROOT CAUSE: only DealProvider refreshed on focus/visibility (July-9 clobber fix); EVERY other data surface loaded once on mount and trusted memory forever — and phones freeze timers in the background, so even CrmProvider's 12s heartbeat and ChatProvider's 5s poll sat frozen through sleep and woke stale. FIX: **lib/useRefreshOnWake.ts** — one hook: visibilitychange→visible + window focus + pageshow (bfcache restores skip mount effects entirely), throttled 4s so focus+visibility pairs don't double-fetch, fn kept fresh via ref. WIRED INTO: CrmProvider (instant pull on wake alongside the heartbeat, still respects the 6s lastLocalEdit guard), Team/Goal/PayPlan/CompPlan/StoreSettings providers (each grew a `fromServer` ref + persist-skip so a wake adopt NEVER echoes back as a write; adopt only when JSON differs; each reload runs the SAME normalize as its mount load — migrateTeamLists/mergeGoalDefaults/migratePayPlans/mergeStoreSettings incl. setActiveStoreSettings), ChatProvider (wake listeners inside its poll effect sharing the `active` flag), /service + /parts pages (existing fromServer machinery). Home/scorecard/follow-up/appointments/equity all read these providers → whole app freshens on wake. **PROVEN ON THE GLASS (CDP):** page open with 1 car in the lane → visibilityState forced hidden → file store changed to 2 cars ("another device") → visible again → SECOND CAR RENDERS with NO reload, lane card 1→2. Service worker checked and already correct (network-first for app code, cache only icons, auto-reload clients on new SW version) — staleness was purely the data layer. GOTCHA (self-inflicted): python string-replace anchored on "const value = useMemo(" swallowed the paren — verify with tsc immediately after codegen edits. RULE: every future data surface mounts useRefreshOnWake (or lives in a provider that does) — "the screen that pops up shows the board as it is NOW" is part of the alive-teammate standard.
## 🔢 LABEL = NUMBER = MATH (July 12, 2026, on `main`)
Aaron (follow-up to the plain-language law): every displayed number must BE what its label says — verify the math internally, never show it, leave NO questions. Found + fixed two real label/number lies on the new boards: **(1) Service "In the Lane" counted BOOKED appointments** (laneStats.inLaneNow was every non-picked-up visit incl. Scheduled — a human in the drive would count 3 cars, card said 4). Now: inLaneNow = Checked In/In Service/Ready only; NEW scheduledNow; card detail says "N arrived today · N booked ahead" so the booked car is visible, not silently counted. todayVisits (createdAt-based "arrived today" — wrong for booked-ahead rows) → **arrivedToday** via NEW `arrivedAt()` (Checked In stamp; past-Scheduled rows without one fall back to createdAt). EILA + digest ride the same laneStats so all three mouths agree. **(2) Parts "Tech Queue" card said 1 while the queue list showed 2** (card = Waiting only; list = Waiting+Pulled) → card renamed "Techs Waiting"; section keeps "Tech Queue (2)". **PROOF METHOD (reusable):** seeded both boards with hand-computed data and read every card OFF THE GLASS via CDP innerText — all values matched hand math exactly (lane 3/2-today/1-booked; ready 1; late 1; shelf 2=$480+$120 excl. picked-up $999; sitting-7d+ 1 of 2; lost $125=85+40 excl. 40-day $500; avg fill 16=(12+20)/2; columns visibly sum to cards). Test updated (inLaneNow excludes Scheduled, arrivedToday asserted). Seeds deleted. RULE: a stat card ships only after a seeded glass-read matches hand math, and if a count excludes something visible nearby, the card SAYS so ("1 booked ahead") instead of making the human do detective work.
## 👁️ PLAIN-LANGUAGE PASS: HUMAN-EYES SCREEN AUDIT (July 12, 2026, on `main`)
Aaron (verbatim concern): code can look right "in black and white" while a human who can only see the display is lost — "what is being displayed [must be] very very simple… SUPER SMART SYSTEM THATS ABLE TO RELAY THE INFORMATION INTO BASIC HUMAN LANGUAGE." Method: screenshotted 17 screens at phone size and read each as a brand-new hire, THEN ran an automated visible-text scan (innerText, NOT textContent — textContent drags in Next's RSC script payload and false-positives every page) for a dev-speak wordlist across 21 routes. VERDICT: the dealer-facing screens speak dealer (Showroom/Follow-Up/Finance/Deals/Import/Group all clean; "office-clean", "RDR-punched", "fresh up", "TO" = real floor language, keep). FIXED (all shipped): Store Settings Texting card had a REPO FILE PATH on a human screen (docs/TWILIO-GOLIVE.md → "setup takes about five minutes"), "seeded"→"added for you", the CODE FIELD NAME digestTo → "your Monday-morning report texts this number"; kicker "Per-store math constants"→"The numbers behind every screen"; tax "Single-rate model. Per-state methods come later"→"Set your state's rate…"; Parts "Aging 7d+"→"Sitting 7+ Days" + "Received, still not picked up"→"Part came in, customer never did" + "No fills timed yet"→"No requests filled yet" + "$0 / 30d"→"$0 last 30 days"; Billing "not configured"→"not set up yet"; Security dropped "environment" and the VENDOR NAME ("ask your store admin to reset your access" — not "Supabase dashboard"); Equity "lands with the inventory feed"→"arrives once your inventory list is connected"; Pay Plan Studio "Parse with EILA"→"Have EILA read it". **EILA got the PLAIN HUMAN LANGUAGE law in her prompt** (HOW YOU ANSWER): assume day-one hire, define any acronym in the same breath, NEVER software words (field/record/sync/configured/null), "if it wouldn't make sense read aloud to a brand-new hire, rewrite it". Re-verified fixed screens via CDP (no GOLIVE/seed/digestTo strings render). RULE GOING FORWARD: every new screen ships only after its copy passes the read-it-to-a-new-hire test; repo paths, code field names, and vendor internals NEVER render in the UI.
## 📞 TWILIO GO-LIVE PREP (July 12, 2026, on `main` — Aaron submitted to Twilio, 24–48h review)
Aaron: "OK I PUT IN FOR THE CALL TO TWILIO … SAID 24-48 HOURS TO HEAR BACK" → staged everything so approval day is ~5 minutes. **docs/TWILIO-GOLIVE.md**: the full runbook with the **A2P 10DLC campaign answers PRE-DRAFTED** (use case Customer Care, campaign + opt-in descriptions written to match what the app actually does — consent recorded before send, STOP account-wide, first-message opt-out notice — and 5 sample messages pulled from the REAL templates: first touch, service status, parts pickup, win-back, honest late re-promise). Steps: buy local number → A2P brand+campaign → webhook `https://missionos.commissioned41.com/api/sms/webhook` POST → Vercel env TWILIO_ACCOUNT_SID/AUTH_TOKEN/FROM_NUMBER → seed per-org commsConfig fromNumber + digestTo (SQL, ask Claude) → 5-tap proof. **/api/comms/status** (NEW): admin-only, returns booleans + MASKED phone tails only (never a secret; commsConfig stays out of allowedKeys); dev path returns not-configured. **Store Settings → Texting card**: Connected/Not-connected badge + 3-row checklist (Twilio keys / store number with store-vs-fallback source / digest number) that mirrors the runbook — Aaron sees the go-live state at a glance and it flips green the moment keys land. CDP-verified (card renders, pending rows, no errors). **48h self check-in armed** (trigger trig_01XFGnTZXvdwhrt6umawzm8n, fires 2026-07-14 18:05 UTC): follow up on the Twilio approval and walk the runbook. JSX GOTCHA: \u2014-style escapes only work in JS strings, NOT in JSX text — the em dash in JSX must be the literal character.
## 📬 FIXED OPS WEEKLY DIGEST (July 12, 2026, on `main`)
Aaron: "GOI" (go) → the GM's Monday-morning read, one brain, three outlets. **lib/fixedOpsDigest.ts** (3 tests, 171 total): `buildFixedOpsDigest(visits, partsRaw, storeName)` → promises kept THIS WEEK per advisor-rollup (promiseStats 7d window; `hitRate7d: null` when nothing promised — never a fake 0%), late-now, win-back open + in-cadence counts, SOP shelf count + $, aging count, lost sales 7d $, top stock-it suggestion, and **TOP MOVE** ranked worst-leak-first (late now > aging SOPs > win-back window > stock-it > clean-board coaching line); `.text` = a ≤4-line SMS. **/api/cron/fixed-ops-digest** (vercel.json `0 11 * * 1` = Mon 7am ET): CRON_SECRET fail-closed (verified locally: 503 with no secret), service client pulls serviceLane/partsCounter/storeSettings/commsConfig for ALL orgs in one query, builds per-org, **sends SMS only when Twilio creds AND the org's `commsConfig.digestTo` exist** (from = org's own number; house inert-until-keys pattern) — otherwise reports {sent:false, reason} in the JSON. digestTo is seeded by SQL on the server-only commsConfig row, same as fromNumber. **EILA `fixed_ops_digest` tool** — same lib, uses ctx.settings.storeName; answers "how did fixed ops do / service and parts summary". Build green (route registered), 171/171. AARON: to get the Monday text, seed `digestTo` (E.164) into each org's commsConfig row once Twilio is live — zero other setup.
## 🛡️ SERVICE V2: PROMISE-TIME GUARDIAN + WIN-BACK LIST (July 12, 2026, on `main`)
Aaron: "OK LETS GO" → the roadmap's #1 service play. **lib/service.ts grew the Guardian** (5 new tests, 168 total): `promiseRisk` ("soon" inside a 45-min warning window / "late" past it; Ready/Picked Up never risk — the advisor gets warned BEFORE the customer does); `updateDue` (Checked In/In Service quiet ≥2h since arrival or last status text = a CSI hit forming; baseline = lastUpdateAt else the Checked-In stamp); `statusUpdateText` (on-track vs HONEST late re-promise vs ready — copy stamps `lastUpdateAt`, so copying IS the update); `promiseStats` (per-advisor 30d promise-kept %: done = first Ready else Picked Up from statusHistory, kept = done ≤ promise — the accountability number); `recaptureList` (every closed visit with declinedWork and NO `recapture` field = an OPEN mission, oldest first, 30/60/90 cadence buckets; laneStats.declinedOpen now excludes resolved) + `recaptureText` (no-pressure win-back draft quoting the declined work verbatim). ServiceVisit grew `lastUpdateAt` + `recapture{state:"recovered"|"dismissed",at}` (optional — old visits untouched). **/service UI**: next-action priority late > due-soon > update-due > ready > win-back; "Due soon" gold card border + pill, "Update due" pill, "Status text" copy button (Checked In/In Service/Ready), Late MetricCard detail flips to "N due soon — re-promise now"; **Win-Back List section** (RotateCcw) with days-since + cadence chips, win-back text copy, tel: Call, "Won it back" / "Let it go" one-taps. **EILA service_lane** now carries due-soon + quiet flags per visit, the WIN-BACK list with cadence windows, and PROMISE-TIME HONESTY per advisor. CDP-verified with a 3-visit seed (due-soon pill + Guardian action bar, update-due pill, 40d win-back mission with 30-day chip, status text copied to real clipboard, Won-it-back cleared + persisted through reload, zero page errors); seed deleted before commit. NOTE: all texts are copy-to-phone until Aaron's Twilio/A2P go-live — then they ride the consent-gated pipe like everything else.
## 📦 PARTS COUNTER V1 SHIPPED (Module 18 opens) (July 12, 2026, on `main`)
Aaron: "GO AHEAD AND SET UP THE PARTS" → the three parts fights from docs/FIXED-OPS-ROADMAP.md that need ZERO DMS integration, built on Service Drive v1's exact rails. **lib/parts.ts** (7 tests, 163 total): SOP lifecycle Ordered→Received→Notified→Picked Up with **Returned as a manual off-ramp** (never auto-suggested); the **aging clock starts at receipt** (industry rule — ordered-not-landed and closed SOPs don't age; receivedAt/notifiedAt stamp ONCE, a status bounce never rewinds the clock); tech request queue Waiting→Pulled→Delivered with request-to-fill minutes (clock stops at Pulled); lost-sale log with **stockSuggestions** (3+ asks in 90 days grouped by normalized part# — the phase-in bell); counterStats (waiting/aging/shelf-$, queue, avg fill 30d, lost 30d); sopPickupText (first-name pickup text, deposit mention only when real — the SMS pipe adds opt-out when it rides Twilio later). **/parts page**: NextActionBar priority = waiting techs > oldest aging SOP > tell-waiting-customers > log-lost-sales; 4 MetricCards (Tech Queue + avg fill, On the Shelf + $, Aging 7d+, Lost Sales 30d $) all with Ask-EILA explains; tech queue with one-tap Pulled/Delivered; SOP columns Ordered/Received/Customer Told with one-tap advance, aging chip red at 7d+, **NO DEPOSIT warning chip** (the industry's own #1 SOP defense) + no-deposit nudge line in the form, **"Pickup text" copy button** on Received/Notified cards; lost-sale ONE-TAP quick-add row (Enter submits) + "Stock it" callout when the bell rings; same CAS save contract (conflict banner + adopt) on the single org key **`partsCounter`** ({sops,requests,lostSales}, normalizePartsData tolerates garbage). BOTH LISTS done: allowedKeys + canWrite (Admin/Manager/F&I — same gate as serviceLane until a Parts role exists on user_profiles) **and lib/access.test.ts matrix snapshot extended to BOTH lane keys (serviceLane had never been added to it)**. Nav "Parts Counter" (Package icon) in The Deal group; /parts in commonAccess (page renders read-only for non-workers, buttons gated by canWork). **EILA `parts_counter` tool** (crm route, same lib): board summary + every open SOP with age/deposit/phone + queue with fill avg + stock-it list; prompt updated; partsCounter added to loadStoreData keys. CDP-verified end-to-end on a 430px phone viewport: SOP created → advanced to Received (clock + NO DEPOSIT + pickup-text button) → tech request created → Pulled → lost sale logged → ALL persisted through reload, zero page errors; dev seed data/partsCounter.json removed before commit. **CDP GOTCHA (new)**: MetricCards are BUTTONS — their accessible name swallows card text, so `getByRole('button', {name:/special order/i}).first()` clicks the "On the Shelf … in special orders" CARD, not the action button. Probe with exact names. Also: the container resume hook reinstalls node_modules and WIPES the local xlsx stub — recreate it (package.json+index.js+index.d.ts in node_modules/xlsx) before any tsc/build in this sandbox. NEXT (roadmap order): SOP auto-texting on the Twilio rail + weekly GM aging digest; parts scoreboard v2 off nightly DMS report upload.
## 🚀 THE "1–5" BATCH SHIPPED + FIXED-OPS RESEARCH (July 12, 2026, `b2140e2` + `753d048` + `6543480` + roadmap commit on `main`)
Aaron: "LETS DO 1-5 PLEASE" — all five shipped and live. **(1) "Your Deal" customer page** (`b2140e2`): rep taps "Customer Link" on the lead card → customer opens a clean no-login page with THEIR deal (vehicle, est. payment, docs to bring driven by the actual deal, next steps in customer words). Token = `<orgId>.<32-hex>` minted SERVER-side (own-customers privacy), allowlist payload only (never gross/cost/other customers), rate-limited public GET, revocable (clear token = every link dead). lib/yourDeal.ts + 5 tests. **(2) Per-org texting + honest group PPU** (`b2140e2`): each store's number lives in server-only `commsConfig` ({fromNumber}); sends use the org's number (env = founding-store fallback); the webhook resolves the TENANT from the To number (unrecognized → log + empty TwiML, never guessed) so a STOP lands on the right store; group rollup swaps each store's OWN settings (mergeStoreSettings around metricsFor) so group PPU respects per-store product weights. **(3) Multi-tenant business cards** (`b2140e2`): public `/c/[orgId]/[slug]` (server component, revalidate 300) reads the org's roster + user_profiles email (never fabricated); founding store keeps curated directory + legacy `/card/[slug]` for printed QRs; /business-card is roster-driven with the right public URL per person. **(5) Service Drive v1 — Module 17 opens** (`753d048`): lib/service.ts (visit lifecycle Scheduled→Checked In→In Service→Ready→Picked Up, append-only history, promise-time LATE — done cars never late, laneStats incl. declined-work $ and service→sales flags; 4 tests) + `/service` lane board (4 MetricCards, one-tap advance, RO#/declined-work/promise capture, LATE red, flagged customer → one-tap "Start opportunity" prefilling the Showroom drawer — service drive feeds the sales floor) + `serviceLane` in BOTH lists (allowedKeys + canWrite: admin/Manager/F&I) + EILA `service_lane` tool. CDP-verified end-to-end. **(4) NEXT.JS 16 MIGRATION — LIVE** (`6543480`): next 14.2→16.2, React 18.3→19.2, Turbopack build, middleware.ts→proxy.ts (convention rename), async params (store route + both card pages; deal-view switched to useParams), `next lint` removed→ESLint 9 flat config (eslint.config.mjs, eslint-config-next/core-web-vitals native flat export; react-hooks v7's new compiler rules run as WARNINGS — 43 pre-existing patterns to clean up over time), tsconfig allowImportingTsExtensions + Next 16 auto-updates, 3 stale @ts-expect-error removed (@types/react-dom now installed). **npm audit 5 vulns (4 high) → 2 moderate** (Next's own bundled postcss — waits on upstream). SANDBOX GOTCHA: cdn.sheetjs.com (xlsx tarball) is proxy-blocked — migration verified locally via a type-faithful node_modules stub, package.json/lockfile keep the real 0.20.3 pin, and the REAL proof was the Vercel branch preview (built green with the real tarball) before fast-forwarding main. Prod deployment READY, zero runtime errors post-deploy. Note: merging the branch also landed the July-9 recovery snapshots (`recovery-2026-07-09/`, was backup-only on the branch). **FIXED-OPS RESEARCH (Aaron: service + parts "held to our standard… I've never worked in them")**: 7 parallel research sweeps → **docs/FIXED-OPS-ROADMAP.md** — advisor/parts-counter ground truth, ranked pain points (service: approval phone tag 22h vs 10min by text, #1 CSI complaint = no status update, 45–49% advisor turnover; parts: SOPs = the #1 cash leak/~95% of obsolescence from unfulfilled demand, RIM forced stock, lost sales never logged, techs waiting 20–50 min/day), manager KPI tables, tool landscape (nobody sells the phone-first pocket assistant in fixed ops; Tekion the only one-platform claim), and the ranked build plan: service = Approval Accelerator / Promise-Time Guardian / Declined-Work Recapture / Morning Command / Manager Scoreboard; parts = SOP Mission Control / Tech Parts-Request Queue / Lost-Sale One-Tap / Manager Scoreboard / Bin-Count Micro-Tasks — parts 1–3 and service 2 need ZERO DMS integration; every customer-texting feature waits only on Aaron's one Twilio/A2P go-live.
## 🔬 FULL A-TO-Z AUDIT + FIX SWEEP + OWNER'S MANUAL (July 12, 2026, `86a8967` + `afdce96` + `e7c2bef` on `main`)
Aaron: "full audit a to z, every page every panel every equation… fix it… and a tutorial." Ran 6 parallel specialist audits (pay math, desk/tax math, CRM flows, dashboards, API/security, providers/setup) — ~70 findings, every fix hand-verified against code (and GA tax law against DOR sources) before shipping. THREE FIX BATCHES: **(1) Security/tenant/tax** — deals_backup PII redaction hole closed (was an unredacted side door for Sales/BDC); profile-less authed users denied instead of defaulted into Kennesaw's org on EILA; checkout forces caller's own org; per-user EILA rate keying; admin chat saves now merge (were last-write-wins); **THE KENNESAW BLEED KILLED**: Team/Goal/PayPlan providers stop merging founding-store roster/goals/plans into every org on load (saved data is truth; defaults only seed empty stores; deleted people stay deleted) + all setup providers (Team/Goal/StoreSettings/PayPlan/CompPlan) now CAS-save with adopt-on-conflict; admin roster draft syncs till first edit (deep-link+Save used to write placeholders over the real roster); no fabricated @kennesawmazda.com emails; AppShell brand + business-card org from settings.storeName; gaPacket dealer is a param (unwired feature, tenant-safe); **GA TAX CORRECTED per O.C.G.A. §48-5C-1 / DOR (web-verified)**: NEW-vehicle TAVT base excludes the rebate (was overtaxing ~$140 per $2k rebate; profile flag rebateReducesTaxable="new"), lease TAVT base includes cash down (was under ~$210 on $3k), lemon-law itemized on leases, LeaseDesk uses profile rate, docFee defaults from settings not literal 899, deal dates LOCAL day; docFeeIncome fallback→0 (NO fabricated $899 on missing-fee deals — was inflating gross/PVR everywhere); NEW lib/desk.test.ts (tax engine had ZERO tests). **(2) CRM/dashboards** — name-click no longer force-desks Won/Lost leads; edit preserves creation date + reschedule clears appointmentConfirmed; Deliveries/Sold/Left by statusHistory EVENT day; At-Desk-Now = state; appointments metric matches the Board; "Today's Ups" rename; Lost≠response in speedToLead (was inflating under-5 grade; Lost+uncontacted = clock off); Follow-Up uses the real SpeedToLeadChip (killed divergent local clock w/ invented 30-min tier); day-0 leads not instantly Overdue; **UTC→LOCAL day sweep** (leadScore/appointments/AppShell badge/MorningBrief/home — the after-8pm-ET family); ONE Total Gross definition (= metrics.gross) on home + dailyReport; home PVR/units drills match their cards; GM drills sum the card's own measure; Trade Equity regex-mining removed; F&I Report + Goals same bars; Deal Center gross = finalized only; goal||12 removed; archive tiles tap-to-explain; equity Start-opportunity prefills the Showroom drawer (?new=1&customer=…). **(3) One pay brain** — buildPerformance gates RETAIL (wholesale inflated engine-path units/bonuses vs legacy; $650 hand case) + honors fastStartByDay; scorecard engine path honors bonusEligible forfeiture; EILA estimate_pay uses the rep's OWN SalesPlan (salesPlanToCompPlan) before the template; payCycle semi-monthly payday = day N *within* next period (was paying before period close); grid opportunities show TRUE gain (+$1,025 not +$350 — test updated to hand math); EnginePayPanel rows sum to net; parity fixture → $1,900 + full ladder proven; nonStacked documented deprecated. 147 tests. All routes 200 + zero console errors + prefill flow CDP-verified. **REPORT-ONLY (Aaron decisions, NOT changed)**: (a) My Scorecard pay gates — wholesale pays on scorecard but not leaderboards; FM cash-deal back gross in F&I Report but not FM pay base — needs his pay-plan intent; (b) scorecard doesn't filter deals to plan pay-cycle window (filterToPeriod unwired — matters only for non-monthly cycles); (c) group rollup uses default product weights for PPU (per-org weights need per-org settings loads); (d) business-card/public-card system is founding-store-only (public URL has no org context — needs a rebuild for tenants); (e) sms webhook single-number→DEFAULT org (fine until store #2 texts); (f) billing fails open by design; (g) Next 16 migration parked. **RULING (Aaron, July 12): "WHOLESALE DOES NOT PAY REPS"** — enforced at every Sales pay entry point (`2546538`): scorecard dealsForRole + EILA estimate_pay now gate isRetail (engine intake + legacy calc already did). **RULING #2 (Aaron, July 12): "CASH DEALS COUNT against gross/PVR/PPU unless marked DNQ — it qualifies"** — enforced in the ONE gate (`10aad67`): countsTowardFinance/countsTowardPpu = financeStatus !== "DNQ" (was Classified-only). Cash back gross + empty menus now count in F&I gross/PVR/PPU, the boards, AND the FM pay base (resolves the open FM-pay question); Finance Command "Protected" = DNQ only; EILA's counting rule + audit flag updated (products on CASH are legit; products on DNQ are the mismatch). NO open pay questions remain. **OWNER'S MANUAL shipped**: docs/OWNERS-MANUAL.md + artifact https://claude.ai/code/artifact/5ebe7b69-7ea6-4847-b566-b120f6423804 (setup order, rhythm by role, EILA habits, money-math trust, go-live switches, safety nets).
## ⚡ EILA FIRST-TOUCH DRAFTS (July 11, 2026, `2d753eb` on `main`)
Aaron: "YEA DO IT" on the #1 recommendation → the instant-lead-response loop is closed: fresh up lands → 5:00 clock runs → rep taps Text → **the intro message is already written**. Two-layer draft: `lib/firstTouch.ts` deterministic template (source-aware opener: web note / phone call / walk-in / referral; customer first name; exact vehicle; ONE easy question; signed "this is <rep first name> at <storeName>") lands INSTANTLY with no API call; then `draft-followup` with `firstTouch:true` (crm route) fetches EILA's live personalized version (2 sentences, <280 chars, full store context + per-rep memory) which REPLACES the template only if the rep hasn't started editing (their words always win). TextThread prefills whenever the thread has no outbound yet ("EILA drafted your first touch — edit anything, then send" over the composer); **pre-Twilio the draft still shows with "Copy — send from your phone"** — the feature is useful from day one, and it only renders after text consent is captured (gate is upstream in the modal's branch order: revoked → no-consent → not-connected+copy → composer). Verified live in dev (screenshot: personalized CX-5 draft + copy button). 137 tests. GOTCHA (test-writing): CSS `uppercase` transforms innerText in Chrome — case-sensitive text probes against styled UI lie; the screenshot was the truth. Also: two buttons on the lead card say "Text" (consent chip + action button) — CDP selectors must filter by class (`min-h-10`).
## 🔧 "FIX ALL" HARDENING SWEEP (July 11, 2026, `d9e3d8a` on `main`)
Aaron: "FIX ALL" → self-audited the day's six ships and fixed what surfaced. (1) **Customer-level STOP** (real compliance bug): consent was per-lead, so a customer with two lead cards could be texted from the card that still said granted after STOPping on the other. Webhook now stamps the revoke on EVERY lead sharing the From phone (message lands on newest only); `textRevokedAnywhere` in lib/comms.ts blocks the send pipeline if ANY lead sharing the number carries a text revoke (works even for trails written before this rule). (2) **crmLeads is now CAS-saved**: consent trails + text threads are legal records riding the whole-array crmLeads row, and the client saved it last-write-wins — a stale tab could wipe a server-written STOP. CrmProvider → `saveStoreGuarded` (store route CAS is key-generic); lost race = adopt server copy + red "board changed on another device" banner on Showroom (`conflicted`/`clearConflict` on context, same contract as DealProvider). Verified BOTH paths live in dev: external-write→edit→409→banner+adopt, and clean edit→no banner→persisted. (3) **Follow-Up page Text button** (+count) → same TextThread modal, consent-gated (verified: gate shows for no-consent lead). Note: Text button hides when the lead has NO phone — that's by design. (4) **npm audit reviewed, NOT blind-fixed**: top-level postcss already patched (8.5.16 — the flagged copy is bundled inside next); ALL 5 advisories (next 14.2.35 family) resolve only via **Next 16 major upgrade** — parked as a deliberate migration (several advisories are Vercel-mitigated; npm install also 403s in this sandbox proxy). 133 tests. STILL OPEN (build-shaped): EILA auto-draft first touch on fresh ups (speed-to-lead + texting now both exist for it); per-org Twilio numbers when store #2 texts; Next 16 migration pass.
## 💬 COMMS HUB SHIPPED (Module 13): REAL TEXTING ON THE CONSENT RAIL (July 11, 2026, `d102c9d` on `main`, inert until Twilio keys)
Aaron: "OK KEEP GOING" after Tier A → built the comms hub the research demanded (consent-first, day one). **Architecture:** `lead.messages` thread rides the lead JSONB (SERVER-written only — /api/sms/send + /api/sms/webhook; clients render + optimistic mirror); `lib/comms.ts` = phone normalize/match/E.164, first-outbound "Reply STOP to opt out" rule, STOP/START keyword → consent revoke/re-grant events; `lib/twilio.ts` = REST via fetch (NO twilio SDK dep), env TWILIO_ACCOUNT_SID/TWILIO_AUTH_TOKEN/TWILIO_FROM_NUMBER, X-Twilio-Signature HMAC-SHA1 verify; `lib/smsServer.ts` = THE one outbound pipeline (route + EILA share it): consent gate server-side (**"granted" only — unknown is NOT a yes**), Sales/BDC text own customers only, opt-out notice, thread write via write-stamp-conditional update (race → append on fresh copy, NEVER resend the SMS). **Webhook** (single-number v1 → DEFAULT org; per-org = lookup on To later): signature-verified, lands replies on the NEWEST lead matching the From phone, STOP auto-revokes text consent with no human in the loop, empty TwiML back. **UI:** Text button (+ message count) in the lead-card action row → `TextThread` modal (bubbles; composer only when consent granted; revoked = red stop; unknown = "capture consent first"; no keys = "not connected yet" but history still shows). **EILA `text_customer`**: two-step (preview → confirm:true only after explicit user approval), same pipeline. 132 tests. **🐛 APP-WIDE FIX FOUND DOING THIS:** `.rise` used `animation-fill-mode: both`, persisting a transform on the page wrapper → it became the CONTAINING BLOCK for every `position:fixed` modal (they anchored to the PAGE, centered ~1500px down, off-viewport on tall pages — LostReason/jacket/drill modals all affected). Fix: `backwards` fill (identical entrance, transform releases). **AARON TO GO LIVE:** (1) Twilio account → buy a local number, register A2P 10DLC brand+campaign for the store (carriers require it; Twilio console walks it); (2) Vercel env: TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_FROM_NUMBER (E.164, e.g. +1770…); (3) Twilio number → Messaging webhook: https://missionos.commissioned41.com/api/sms/webhook (HTTP POST); (4) capture text consent on a lead, send from the card, reply STOP from the phone and watch the chip flip red. Until (2), everything is byte-identical inert.
## 🏁 TIER A CLOSED OUT: CONSENT RAIL + MFA + GROUP COMMAND (July 11, 2026, `6f1d454` + `f3418b8` + `2083a43` on `main`)
Aaron: "YES PLEASE FIX" (again) right after Module 5 → shipped the rest of the Tier-A code from the gap report, one commit each. (1) **TCPA consent rail** (`6f1d454`): `lib/consent.ts` = append-only per-channel (call/text/email) consent events with source + recorder on `lead.consent` (JSONB, no schema change); latest-word-wins, "all"-channel DNC, `canContact` gate, 10-business-day suppression math (we suppress IMMEDIATELY — deadline is the legal max). `ConsentChips` on every showroom card (green/neutral/red, tap → capture menu, every tap = audit event stamped `by`); revoked channels get dead red "Do not call/email" pills instead of live tel:/mailto: links on CRM Desk + Follow-Up + Appointments (team-command untouched — that's staff, not customers). EILA `check_consent` tool + prompt rule: consent check before outreach drafts, revoked = hard no. Verified live via CDP incl. tap→grant→persist. **This is the day-one architecture the comms hub (Module 13) must ride on — build texting ON this, never beside it.** (2) **MFA/TOTP** (`f3418b8`): `/security` page (Team nav, every role) = self-serve two-factor with any authenticator app via Supabase auth MFA (enroll QR + manual key + prove-one-code; stale half-enrollments cleaned; turn-off needs aal2 with a plain-words fallback). `MfaChallenge` renders INSTEAD of the app when `getAuthenticatorAssuranceLevel` says aal1→aal2 owed (AuthProvider gate; sign-out is the only way around). **Fails OPEN on errors** — a broken AAL check can never lock a store out; nothing changes until a user enrolls. NOT live-testable in-container (no Supabase) — Aaron should enroll his own account on prod to confirm, and check Supabase dashboard → Auth → MFA (TOTP) is enabled for the project. (3) **Group Command** (`2083a43`): `lib/groupReport.ts` rollup reuses `metricsFor` per store, group totals recomputed from raw sums (group PVR = total gross ÷ total units, never avg of avgs; 5 tests, 126 total). Membership = server-only `groupConfig` app_store row `{name, memberOrgIds, viewers}` deliberately NOT in allowedKeys (clients can't read/forge it; seed by SQL per group); platform owner (isOwnerEmail) sees ALL orgs with no config. `/api/group` + `/group` page (Admin nav "Group", Building2) = totals cards + per-store table, aggregates only (no customer PII crosses store lines), friendly "one store today" empty state; EILA `group_report` tool (Viewer type grew `email` for the per-caller access decision). (4) **docs/PARTNERSHIP-TRACK.md**: the two non-code Tier-A items (Mazda lead-handling certification, 700Credit partnership; Dealertrack/RouteOne parked) as a working checklist for Aaron. **TIER A NOW: speed-to-lead ✅ consent rail ✅ MFA ✅ group reporting ✅ · Mazda cert + 700Credit = paperwork/calls (see the checklist). Next build-shaped thing: the comms hub itself (Module 13) on top of the consent rail.**
## ⏱️ MODULE 5 SHIPPED: FIVE-MINUTE RESPONSE SYSTEM (July 11, 2026, `c7e905c` on `main`)
Aaron: "YES PLEASE FIX" on the #1 Tier-A gap from docs/COMPETITIVE-GAPS-2026-07.md (speed-to-lead is THE industry baseline metric — OEM programs and every competitor grade on it; we had nothing). Now every **New Lead carries a live 5:00 first-contact clock**: **lib/speedToLead.ts** is the one brain — `speedClock` (on_clock secondsLeft / breached minutesOver / responded responseMinutes / not_applicable; creation time = `lead.date` else the `CRM-<ms>` id timestamp), `speedStats` (30-day window: measured, under-5:00 %, avg+median minutes, per-rep grades, live onClockNow/breachedNow), `firstContactPatch` (stamps `lead.firstContactAt` ONCE — never overwrites, so the grade is honest). 10 new tests (112 total). **Clock stops 3 ways:** tel:/mailto: taps in lead detail (Info rows got `onUse`), the chip's one-tap "Contacted ✓", or moving the lead past New Lead (moveLead stamps on the way out; legacy leads grade off their first statusHistory entry). **SpeedToLeadChip** ticks every second in gold `m:ss`, flips red `OVER Nm/Nh` on breach — on drill rows, mobile cards, AND the desktop table. CRM Desk got a **"5-Min Response" MetricCard** (red `N OVER` > gold `N ⏱` > green 30-day %) with drill into the fresh-up list + Ask EILA. **EILA `speed_to_lead` tool** (crm route, same lib, scopeLeads-scoped): "who's on the clock right now" + 30-day per-rep grades. **Hardenings shipped with it** (found when synthetic seeds crashed the desk): `lib/desk.ts` tolerates leads without `products`; `lib/data.ts` normalizePersonLookup/displayFullPersonName/lastName tolerate missing names — partial/imported leads can no longer white-screen CRM Desk. Verified live in dev via CDP (countdown ticking 2:48→2:45, red OVER chip, "2 OVER" red card); test seeds removed from data/crmLeads.json after. NEXT on Tier A: comms hub + TCPA consent, in-app MFA, Mazda lead-handling certification track, 700Credit; group reporting still unbuilt.
## 💳 STRIPE BILLING SHIPPED — THE SAAS LOOP IS CLOSED (July 11, 2026, `215b09d`, fail-open until keys land)
Aaron: "ok lets go" (Stripe). Built end-to-end on the existing stubs (lib/stripe.ts + checkout + scripts/stripe-setup.mjs existed; no webhook/linkage/enforcement did). **Architecture:** per-ORG billing on a server-only `billing` app_store row (deliberately NOT in /api/store allowedKeys — clients can't read/forge it), written ONLY by signature-verified `/api/stripe/webhook`; orgId rides as metadata on every Checkout session AND subscription so lifecycle events resolve to the store; order-independent writes (live sub refetch + eventCreated stale guard, lite's pattern); `status:"comped"` (hand-set via SQL for partner stores) is never downgraded by stray events. **Entitlement brain** lib/billing.ts `decideEntitlement` (7 unit tests): Stripe unconfigured→open (ships inert), DEFAULT_ORG (Kennesaw)→founding_store always, active/trialing/comped→yes, no-record→14-day courtesy window from organizations.created_at (no created_at→grandfathered), past_due/canceled→NO. **Enforcement:** /api/store GET+POST and EILA resolveCaller 402/deny lapsed orgs (server is the gate); AppShell renders a paywall screen for lapsed stores (admins get "Fix billing"→/billing; reps get "ask your manager"), /billing page (admin nav, CreditCard icon) = status + Subscribe (checkout w/ orgId) + Manage (Stripe customer portal via POST /api/billing) + success-poll after checkout. Signup now returns orgId and hands straight to Checkout (courtesy window covers webhook lag/failures). **AARON TO GO LIVE:** (1) Stripe dashboard → run scripts/stripe-setup.mjs or create $499/mo price; set Vercel STRIPE_SECRET_KEY + STRIPE_PRICE_ID; (2) Stripe webhook endpoint → https://missionos.commissioned41.com/api/stripe/webhook (events: checkout.session.completed + customer.subscription.*), set STRIPE_WEBHOOK_SECRET; (3) test a checkout in Stripe test mode; (4) flip NEXT_PUBLIC_SIGNUPS_OPEN=true. Until (1), prod behavior is byte-identical to before. STILL UNBUILT: group reporting.
## 🛠️ "FIX ALL OF IT" MEGA-SESSION (July 11, 2026, all dealer `main`: 3fc37be…5c8097f)
Aaron: "fix all of it" + "i give you permission to do all." Shipped, in order: (1) **CAS deals writes** (`3fc37be`) — reads carry `x-store-version` (row write-stamp; dev file store = mtime), board saves present `x-store-if-version`, server applies an ATOMIC conditional update; stale device gets 409 + current board, client adopts it + shows a re-enter banner (new `conflicted` state in DealProvider, `saveStoreGuarded` in storeClient); headerless legacy clients keep last-write-wins so deploy breaks nothing. Verified full contract on dev (read version → swap 200 → stale 409 → legacy 200) + in-app save. (2) **EILA restore_backup tool** (`b27f91f`) — two-step (describe → confirm:true), Manager/F&I/Admin only, board-first verified write, replaced board becomes new backup. (3) **Per-manager PVR/PPU targets** (`4e2544d`) — `goals.financeTargets[name]{pvr,ppu}`, Goal Setup "F&I Manager Targets" section, Finance Command grades vs personal bar ("(personal)" label, red PPU cell). (4) **Kennesaw REAL jacket checklist** (`d386326`) — Aaron photographed his actual F&I cover sheet; DEFAULT_JACKET_SECTIONS = Deal Pack(19)/Bank Pack(6)/Tag & Title(12)/Delivery(2)/Trade Pack(12) = 51 docs; checklist modal + print sheet render section headers (print verified via CDP PDF); custom store orders still flat. Old jacketDocs keyed to the generic 16 names orphan harmlessly. (5) **Dealer blue folder** (`5c8097f`) — Scan and Sort files the sorted PDF to private `jackets` bucket (org-scoped, 25MB, auto-created like deal-docs) via /api/jacket-file; `deal.jacketFile{path,pages,savedAt}`; modal shows FolderOpen card w/ days left; lib/jacketFile.ts = 90-day math; nightly /api/cron/jacket-cleanup (CRON_SECRET, vercel.json 0 11 * * *). Filing failure never blocks download. (6) app-icon.svg deleted (`eab397b`). **STALE-NOTE CORRECTIONS:** lite UI already says EILA everywhere (Brand.tsx E+ILA tspans = wordmark); lite EILA already has log_deal/delete_deal/update_deal/update_plan_config/clear_sample_data/forget_memory — both audit notes were outdated. **STILL NEEDS AARON:** ANTHROPIC_ADMIN_KEY (spend card), Upstash keys (rate limiting ON), registrar+ElevenLabs MFA, Plaid submit, legacy Supabase key disable (check NEXT_PUBLIC key first), lite project key rotation. **STILL UNBUILT (big):** Stripe billing; group reporting.
## 💸 PROMPT CACHING SHIPPED ON EILA (July 10, 2026, dealer `b3b421a` + lite `a044731`)
Aaron approved after the caching assessment. Dealer crm route: chat already had stable-system + snapshot breakpoints; ADDED top-level `cache_control` (auto-marks the newest message, so tool-loop rounds + follow-up turns re-read the conversation at ~0.1x) and a breakpoint on callClaude's systemBase (single-shot drafts/briefs/actions). Lite: `buildIlaSystemParts` splits the prompt at its stability boundary (persona+operating layer+memory/brain = stable breakpoint; live performance/money snapshot after it, so tool edits never invalidate the persona), chat route sends block system + top-level marker (client-side tool round-trips re-send everything — now cached), owner chat got the top-level marker, and `[ila] cache_read=` is logged per call (dealer already logged it). Identical output guaranteed by Anthropic — cost/latency only. VERIFY on prod: watch Vercel logs for cache_read > 0 on second turns. NOT cached (deliberately): all scan-* routes (unique PDFs/images per call, no repeated prefix). Break-even is 2 requests in 5 min; worst case +25% input on a lone call.
## 🔢 DEALER: STORE PVR LABELED RIGHT + VIN DECODER EVERYWHERE (July 10, 2026, `main` 1eea758 + ea489da)
Aaron caught the Mission Control card computing total gross ÷ delivered while labeled "F&I · PVR" — relabeled **Store PVR** (card + drill title; the drill already showed the front/back split; /archive's "F&I PVR" is genuinely financePvr and stays). And his "I thought we installed a VIN decoder?" was right: lib/vin.ts (NHTSA vPIC) existed but only CRM Desk used it. Now: **Deal Entry** — sold-unit VIN shows a green decode confirmation line the moment 17 valid chars land (red "didn't decode" on typos); **Trade VIN auto-fills Trade Year/Make/Model** (decoder values never blank manual entries). **EILA got `decode_vin`** (crm route, same lib): decodes any VIN and cross-checks whether it's the sold unit or trade on a store deal or on a working lead — redaction mirrors query_deals (Sales/BDC see [hidden] on others' customers, scopeLeads for leads). Verified in the live dev app via CDP with a stubbed NHTSA response (real NHTSA is 403-blocked by this sandbox's proxy — env limitation only; prod/Vercel reach it fine). 95 tests, lint, tsc clean.
## 🧊 THE SKY SHELF — ICON FAMILY UNIFIED COMPANY-WIDE (July 10, 2026, Aaron's explicit pick, all three repos' `main`)
Aaron chose "Sky shelf company wide" from two mocked directions: ALL app faces now live on the ice squircle (soft #f4f8ff→#e7eefb gradient tile) — **Dealer Mission OS = gunmetal chrome M** (mission-icon-512/192, apple-touch-icon, favicon.ico regenerated from mission-mark.png with the brightness(.5) contrast(1.5) treatment; icon URLs bumped ?v=5→?v=6 for installed PWAs), **EILA = blue E squircle** (unchanged today, but its icon.svg/brand svg/app icon.tsx/apple-icon.tsx all carried the OLD slab E and were updated to the redrawn letterform), **Commissioned 41 = gunmetal C41 real artwork** (site's text-drawn C41-on-black icon.tsx/apple-icon.tsx generators DELETED, replaced by static app/icon.png + apple-icon.png from c41-mono-transparent.png). Icon PNGs generated via headless-chromium render of HTML tiles + PIL resizes. NOTE: dealer public/app-icon.svg is a hand-drawn C41-on-black approximation (crops the mark) — unreferenced by manifest/layout; candidate for cleanup/replacement with real art.
## 🔵 LITE: THE BLUE FOLDER — SCAN AND SORT ON THE DEAL CARD, 90-DAY HOLD (July 10, 2026, lite `main`)
Aaron iterated Scan and Sort's home + storage three times in one sitting and LOCKED (via explicit choice): **Scan and Sort lives ON the deal card** (DealDetail row, finance role, links /jacket?deal=<id> so the sort is bound to the customer — download is named for them) and **EILA holds the sorted PDF 90 days as a "blue folder"** on the deal card (FolderOpen icon, sky-400, signed-URL open), then auto-deletes. Infra: private `jackets` storage bucket on the MissionOS Lite Supabase project (fhqxefvpygpxregmpcow) — owner-only RLS by auth.uid() folder, PDFs only, 25MB (SQL kept in supabase/lite_jackets_storage.sql); `deal.jacketFile {path,pages,savedAt}`; lib/jacketFile.ts owns retention math (tested); nightly cron /api/cron/jacket-cleanup (CRON_SECRET, service role) deletes >90d — UI hides expired folders independently via jacketFileFresh so a missed night never shows a dead file; privacy page states the 90-day hold. Filing failure NEVER blocks the local download. NOTE: buckets can't be dropped via SQL (storage.protect_delete) — an earlier ephemeral-direction flip left the bucket in place, which the final direction then used anyway. 273 vitest green.
## 📄 SCAN AND SORT SHIPPED IN BOTH APPS (July 10, 2026, `main` of dealer + lite)
Phase 2 built same-day, Aaron's name: **"Scan and Sort."** Dealer: inside the Deal Jacket panel (Deal Center) — drop the scanned signed-deal PDF, pdf-lib splits pages in the browser, batches hit `/api/ai/jacket-scan` (PDF vision labels pages against the store's order), `lib/jacketScan.ts` (pure, tested) builds the sequence — groups in store order, unknown pages kept at the BACK (never dropped) — client rebuilds the PDF for download/print and bulk-marks found docs as filed (ONE deal update; per-doc loop would stale-read). Ephemeral by design — SSNs, nothing stored. **EILA lite app (missionos-lite): same feature for F&I subscribers** — new `/jacket` screen (finance role + subscription gate, Paywall), producer's own `profile.jacketOrder` (editable, house default), `lib/jacket.ts` + `/api/scan-jacket` (scan-recap gate pattern), linked "Scan and Sort" from the Finance queue header; pdf-lib added to lite deps; 270 vitest green. Also: Deal Center count card split Finalized/Working (tap = focus the ledger, never blended, reads `isSold` — the same brain as metricsFor), and Deal Entry product toggles no longer clip PERMAPLATE. NOTE: neither env here can live-test the AI classify (no ANTHROPIC_API_KEY locally) — Aaron validates on prod; deterministic sort logic is fully unit-tested.
## 📁 DEALER: DEAL JACKET CHECKLIST + PRINT COVER SHEET + EILA TOOL (July 10, 2026, shipped to `main`)
Aaron: F&I cleaning a signed deal should ask EILA what order the file goes in and print it right. Shipped phase 1: **lib/dealJacket.ts** is the one brain (default 16-doc F&I jacket order; per-store override on `storeSettings.dealJacketOrder`; per-deal progress on `deal.jacketDocs` keyed by doc NAME — no schema changes, rides the JSONB). Deal Center: folder icon per deal row → jacket modal (tap cycles missing→filed→N/A, progress badge, **Print Cover Sheet** = white letter page via the worksheet's print-portal pattern, verified via CDP printToPDF), plus an admin/owner-gated order editor (storeSettings write-matrix is admin-only — deliberately matched the gate to avoid the Close-Month 403 class). EILA got the **deal_jacket** tool (crm route) reading the same lib: order + filed/N-A/missing by deal #, customer, or stock #. 8 new tests (91 total green). **Phase 2 agreed with Aaron (next build): "Scan & Sort" — F&I drops the scanned signed-deal PDF on EILA, she classifies pages against the store's jacket order, returns the PDF reordered for print + auto-checks the checklist + flags missing docs.** Plan: client splits PDF (pdf-lib), pages batched to a server route classifying via Claude PDF input, client reassembles; ephemeral processing (SSNs — store nothing in v1); review screen before print because classification won't be 100%. NOTE: local `next build` fails here on the `xlsx` CDN tarball (proxy blocks cdn.sheetjs.com) — pre-existing env limitation, Vercel builds fine.
## ™️ NAMING: THE AI IS **EILA** NOW (July 9, 2026 — Aaron: "EILA is going to be our trademark")
Aaron renamed the assistant **ILA → EILA** himself (commits 0e3688b + 5c9a8c8 from his laptop, 76 files, incl. `public/brand/eila-app-icon.svg`) and confirmed: **EILA is the trademark.** This SUPERSEDES the old "always ILA, never ISLA" rule — from now on the user-facing name is spelled **EILA** everywhere. Internals (file names like `lib/ila.ts`, `ila:ask` events, `IlaCore.tsx`, API paths like `/api/ila-brain`) still use `ila` — renaming those is churn with zero user value; only do it if Aaron asks. Older "ILA" mentions below in this log are historical. ⚠️ The **lite** app (missionos-lite repo) still says ILA in its UI — needs the same rename when Aaron says go.
## ✅ DEALER: JULY BOARD RECOVERED IN PROD (July 9, 2026, evening — docs/RECOVERY-JULY-2026.md executed via Supabase connector)
The recovery runbook ran against prod (`app_store`, org `…0001`) and the end state is verified: **June 2026 is a closed month in `closedMonths` (76 deals, gross $121,455)** and **the live `deals` board holds Aaron's 22 July deals** (back $17,473, front −$12,840, total $4,633 — matches his hand-verified log exactly; built with `buildDeal`, per-row reconciliation + `importTotals` checksums all green). What the inventory found (differed from the runbook's expectations): June had ALREADY been archived at 15:29 UTC that day by a Close-June run under Aaron's name — the archive recomputes exactly against `summarizeMonth` and its 76 deal numbers match `deals_backup_docfix` 1:1, so it was left alone; `deals` was empty (not 21 junk rows — they're gone); `deals_backup` still holds a 62-deal June 1–22 snapshot (June 26) and was deliberately NOT overwritten with the empty board (the runbook's backup-the-current-board step would have destroyed it to preserve `[]`). ⚠️ **One live clobber observed during recovery**: the first 22-deal write (18:41:04 UTC) was overwritten with `[]` eleven seconds later by a client echoing its stale empty board — the exact incident clobber class, still alive on some device. The second write (18:44:43 UTC) stuck and soak-checked stable. Every pre-write value was snapshotted to `recovery-2026-07-09/` (committed on branch `claude/july-2026-recovery-cplgel`) along with the build/verify scripts. Aaron still needs to fully close + reopen the app on EVERY device — one of them clobbered mid-recovery. Non-roster names kept verbatim on the July deals: `ZAMORANO` (F&I, not in `lib/data.ts` team), `HOUSE EMPLOYEE`, `NONE`; `MATTHEW ROCK`→Matt Rock needed a manual alias (lib lookup can't bridge Matthew→Matt).
## 🚨 DEALER: JULY-IMPORT DATA INCIDENT + CLOBBER-CLASS FIX (July 9, 2026, shipped to `main`)
Aaron imported his July log; later the board showed **21 deals, some June-dated, none of his July data** — and both the board's July deals AND the June archive read empty at one point. Diagnosis (couldn't inspect prod DB — Supabase/Vercel connectors connected at org level but `enabledInChat:false` this session): the deal board is a whole-array, last-write-wins store and every device persists its ENTIRE local copy on any change, fire-and-forget — so **a stale device/tab (e.g. open since mid-June with 21 deals) silently overwrote the board** the moment anything touched it. Same clobber class as the lite $20k-goal bug. Shipped the cure:
- **DealProvider hardened**: (1) refresh-on-focus/visibility — a waking device reloads the board BEFORE the user can act on stale data (race-guarded by a mutation counter; identical copies don't re-render; server refreshes NEVER echo back as writes); (2) persists only on real local mutations (`needsPersist`), with one quiet retry then a **visible red "couldn't save" banner** — a dropped save can no longer be silent; (3) new `replaceBoardVerified()` — bulk writes confirmed on the server BEFORE local state changes.
- **Import commit is now a VERIFIED write** ("Done" only ever said about deals that provably landed; previously the final save was fire-and-forget and could vanish on reload after saying Done).
- **"Safety net — last backup" card on /import**: shows the auto-snapshot (count + total gross), one confirm restores it to the board via verified write, and the replaced board becomes the new backup (restore again = swap back, always reversible). Verified end-to-end in the dev file store (restore → Deal Center shows the deals → deals.json written).
Recovery path for Aaron: restore backup (June's 76 if the July import's replace-mode snapshot is intact) → Close June → re-import July → **hard-refresh/close every other device running the app** so no stale copy remains. TODO next hardening step (flagged, not built): server-side compare-and-swap (updated_at) on deals writes; ILA parity tool for restore.
**Container gotcha addendum**: `next dev` corrupts silently after on-disk edits + resume-hook package strips — client chunks 404, hydration dies, pages freeze on SSR login shell even with `__dev_preview` set. Fix: `rm -rf .next` (whole dir, not just cache) + restart.
**ROOT CAUSE FOUND + IMPORT HARDENED (same day, follow-up):** Aaron showed the exact text he pasted — a mobile "expanded rows" DMS table, ONE FIELD PER LINE (~22 lines per deal, "Expand" chrome, parenthetical negatives, two dates). That's parse-hell; the misfired import (21 mangled deals, wrong dates) very likely WAS his July data. Shipped (Aaron: "yes please as long as it doesnt mess anything else up" — all additive/gated): (1) **`linearizeExpandedLog`** in lib/dealImport.ts — deterministically detects that shape (≥3 unmistakable N/U+New/Used+deal# record starts spanning many lines) and rebuilds it one pipe-joined `DEAL ROW:` line per deal BEFORE EILA sees it; any other paste returns null and flows through byte-identical. Verified on Aaron's real paste: 22/22 records, fields together, chrome stripped. (2) **Reconciliation gate**: EILA now also extracts each row's PRINTED total (`totalGross`, never stored on the Deal); review screen lists rows where front+back+docFee ≠ printed total (±$1) and the commit button is DISABLED until an explicit "import anyway" checkbox — a green "All N rows reconcile" line otherwise (shown only when the log actually printed totals). (3) Hand-built Aaron a clean 22-deal CSV of his July log (sent as file; checksums Units 22 · Back $17,473 · Front −$12,840 · Total ≈$4,633 — 11 legit negative-front deals, verified row-by-row). lib/dealImport.test.ts NEW (9 tests, synthetic fixture). 83 tests, tsc, lint, build green.
## 🗓️ DEALER: CLOSE THE MONTH + DIGITAL BUSINESS CARD REDESIGN (July 9, 2026, **MERGED TO `main` = LIVE** at 72ab2ca on Aaron's "ship it to live")
Aaron asked how to roll June→July; the app holds ONE live month (all screens read all saved deals; month label anchors to the latest deal date — no auto-rollover). Old only option = "Clear Deal Data" (permanent delete). Built the fix he loved:
- **Close the Month** (`components/CloseMonthButton.tsx` in Deal Center, Manager/F&I/Admin/Owner): confirm modal with month-end recap → archives the whole month → clears board. Archive write is VERIFIED (saveStore returns bool) BEFORE clearing — a failed backup never costs the month.
- **`/archive`** page: every closed month, locked recap (units/gross/front/back/F&I gross+PVR/PVR/PPU/class mix) + full deal log. `lib/closeMonth.ts`: snapshot stores raw deals (recomputable) + frozen summary via `metricsFor` (tested engine, never re-implemented). New store key `closedMonths` (org-scoped) added to `app/api/store/[key]/route.ts` allowedKeys + PII redaction mirroring live `deals` (Sales/BDC can't read other reps' customers in the archive). Re-closing same monthKey overwrites (no dupes). Nav + AuthProvider managerAccess got `/archive`. Verified end-to-end with 8 seeded June deals: modal→archive→board cleared→archive detail all correct.
- **Digital business card redesign**: old card reused the dark PlayerCard. New `components/DigitalCard.tsx` — deep-blue brand header + Verified badge, photo breaks the seam, bold name/title/dealership, tap Call/Email rows, **Call Now + Text + Save (vCard)**. `lib/vcard.ts` builds a vCard 3.0 data-URL so one tap adds the rep to a customer's phone. **Theme-immunity gotcha:** the card is always white but renders inside the app's theme system, so `bg-white` flipped to ink under Sky — fixed by resetting `--c41-white:255 255 255` inline on the card root. Public `/card/[slug]` rebuilt on light branded backdrop; in-app `/business-card` shows the live card + copy-link/open/QR + photo upload.
- **In-app `/business-card` Sky pass (Aaron: "go ahead and fix business card"):** screenshot-verified the page under Sky AND glass — the select/share buttons/glass QR panel all flip correctly via the theme machinery; the ONE real defect was the QR plate: its `bg-white` flipped to ink under Sky (dark plate = unscannable). Fixed with the same inline `--c41-white:255 255 255` pin the DigitalCard uses + a hairline ring so the white plate reads on the white Sky panel. (QR image blank in-container only — sandbox blocks api.qrserver.com; loads in prod.)
Both: tsc clean, 73/73 tests, prod build green. **LIVE:** fast-forward merged (dafeaa4..72ab2ca) and pushed to `main` on Aaron's "ship it to live" — Close the Month, `/archive`, and the new digital business card all deployed together.
**PROD HOTFIX (b0e2051, same day):** Aaron's first real Close June hit "Couldn't save the archive" — the default-deny `canWrite` matrix in `lib/access.ts` never got a `closedMonths` rule, so prod returned 403 (the safety net held: nothing cleared). Dev never caught it because the file-store path skips the matrix. **LESSON: a new store key needs BOTH lists — `allowedKeys` in the route AND a `canWrite` rule — and the write path must be tested against the real matrix, not just the dev file store.** Rule added (owner/admin/manager, matching the UI gate), matrix tests updated (74 tests), shipped to `main`.
**RESOLVED:** Aaron's post-merge "can't log in" was the stale-PWA-after-deploy artifact — a fresh reopen fixed it ("yea i was able to log in"). Sky standard is confirmed live and working for him.
## ☀️ DEALER: "SKY COMMAND" IS THE STANDARD THEME (July 8–9, 2026, **MERGED TO `main` = LIVE** at 757b9c5 on Aaron's "MERGE IT")
Aaron shared a reference dashboard (white cards, soft blue canvas, one blue accent): "THIS IS HOW I WANT THE THEME IN THE DEALER MISSION OS TO BE" → then "LOVE IT LETS MAKE IT OUR STANDARD ON EVERY SCREEN."
**Mechanism (the whole trick):** `tailwind.config.ts` redefines `white` as `rgb(var(--c41-white,255 255 255)/<alpha>)` → the ~1,700 hardcoded white utilities (text-white/60, border-white/10, bg-white/5, gradients) flip to INK at the same alpha under sky ONLY. `--c41-white: 255 255 255` in `:root` keeps all 9 dark themes byte-identical. Accent buttons self-heal: labels are `text-mission-navy` on `bg-mission-gold`, and navy = the light page base under sky → light-on-blue. Sky token block + scoped `[data-theme="sky"]` overrides re-materialize the Glass HUD surfaces (solid white cards, soft blue-cast shadows, glows/glare/sweeps off).
**Deliberately-dark islands** keep `--c41-white` restored (CSS var cascade): the nav rails (`bg-[#0a0b0e]*` — dark frame carries the silver M), the PlayerCard (`.player-card` hook added), ILA's CommandDeck/orb, the LicenseScanner camera shell, landing mobile menu. Content dark micro-surfaces (`#14161c*` inputs/tiles etc.) become white fields. `.lg-glass` login panel = white under sky.
**Standard:** ThemeProvider default = "sky" + ONE-TIME migration (`c41-sky-standard` localStorage flag — old provider auto-wrote "glass" so stored value ≠ choice; everyone moves once, later picks stick). Sky first in picker; layout themeColor `#f0f4fb`. Fix batch: bg-white buttons w/ hardcoded dark hex text (page.tsx home CTA, CommandDeck send) → `text-mission-navy`; team-command ellipse → token-driven; NEW branded `app/not-found.tsx`.
**VERIFIED ON REAL SCREENS:** dev-preview bypass (`__dev_preview=1`, dev-mode only) + Playwright/Chromium in-container; ALL 39 routes screenshotted under Sky and reviewed via contact sheets (container gotcha: resume hook strips `xlsx` → reinstall `xlsx@0.18.5 --no-save` AND `rm -rf .next/cache` + dev restart or the module-not-found overlay sticks on every route). Aaron approved from the 4-screen set (Showroom/F&I/Goals/Scorecard). 73/73 tests, tsc (pre-existing test-import noise only), prod build green.
**LIVE:** merged (fast-forward 703edfa..757b9c5) and deployed; every device auto-migrates to Sky once on next open, theme picker still offers all dark themes. Possible follow-ups: white sidebar variant (reference had light rail; we kept dark frame), marketing pages (signup dark footer band) stay branded-dark by design, import/setup textarea gray could lighten.
## ✅ LITE: AUDIT FIXES ROUNDS 1+2 — EVERYTHING SHIPPED (July 8, 2026, missionos-lite `main` → `d07a171` + `b31182f`, live)
Aaron: "get it done… keep going dont stop until your done… when your done save everything to memory." ALL 6 audit HIGHs + essentially every MEDIUM/LOW/cobweb fixed and deployed. tsc clean, **263 tests**, prod build green at every step.
**Round 1 (`d07a171`) — the 6 HIGHs:** (1) `<NumInput>` in ui.tsx — decimals type again ("22.5" no longer becomes 225%); adopted in PlanEditor + product menu; tier ids now time-based (length-ids collided after delete-then-add). (2) Lapsed-subscriber paywall actually fires: api/entitlement returns `reason:"no-subscription"`; gate extracted to `useEntitled` (Paywall.tsx) shared by AppShell AND /report (which had NO gate); sign-out clears `lite-ent-ok`. (3) ILA quotes post-tax money (lib/ila.ts passes plan.taxRate). (4) Demo deals filtered from nudge cron (+ silence when nothing real). (5) UTC/local family: `localMonthKey`/`localDayKey` in lib/engine.ts — report page, DealDetail cohort, Performance buckets, fastStart, every balanceAsOf stamp now LOCAL. (6) Dashboard useMemo got its missing profile.daysOff dep. Plus: cross-tab `storage` adoption (two tabs no longer destroy each other) + pagehide flush; dark-ink Wordmark; iOS status bar `default`; "ILA is locked".
**Round 2 (`b31182f`) — parity + the rest:** **ILA parity complete per Aaron's rule** — new tools `log_deal` (core action; products resolved from user menu, auto-clears demo), `delete_deal` (confirm-first; dead-status is the fell-through path), `update_plan_config` (tax/draw/carried-in/guarantee/%s/per-unit/salary, validated, reclassified), `clear_sample_data`, `forget_memory`; update_deal matches+revives dead deals, validates status enum. HandsCtx grew memories/addDeal/removeDeal/clearSampleData/forgetIlaMemory (IlaChat wires live getters). Chat plumbing: pair-aware slice(-20) window repair (`repairToolPairs`), block cap 8→24, read-only truthfulness line when tools off, deals cap, maxDuration 60. **Money engine:** evaluatePurchase window reaches next check (wait-vs-no); quarterly/yearly bills amortized into safe-to-spend + cash curve; daysToIncome ceil; dead moneyBrief/monthlySubscriptions/bankedPay removed. **Store:** reconcile adopts explicit cloud RESET by timestamp (no resurrection); compToPlan stops inventing kickers + keeps grid spiffs; ensurePlan retro-patches dropped guarantees. **Screens:** prospects visible immediately; Face ID sign-out fallback (signed-in only!); minLength 8 only on sign-up; badge = followUpQueue().needsYou; Pip tiles drill; Onboarding step-4 Back; DailyBrief industry units; FinanceCard note resync; CountUp no backwards snap; DailyBudget resume refresh; GoalClimb phantom-band + draw-only summit; branded 404. **Security:** checkout origin from request host (not forgeable Origin header) + session-verified email for self-referral guard; `import "server-only"` on supabaseAdmin/stripe/entitlement (+ vitest stub test/server-only-stub.ts); parse-payplan role/industry allow-list; /api/entitlement rate-limited (429 carries NO reason so it can't paywall); scan-statement total cap; **`lite_rate_limit_hit` RPC EXECUTE revoked from anon/authenticated/public ON THE LIVE LITE DB** (fhqxefvpygpxregmpcow) + SQL file updated. **Cobwebs:** truthful README, types.ts/reset-password/fni comments, dead fileToPayload + void-dealTotals gone.
**Consciously DEFERRED (design decisions, flagged to Aaron):** cross-user shared-brain (ila-brain) prompt-injection hardening + its PATCH/seeding concurrency; server-stamped `updated_at` on lite_state (client clocks still arbitrate sync); sign-out-while-offline still wipes unpushed edits; Face ID fallback intentionally absent for local-only (no-account) users; the duplicated unreferenced mp4s in public/ left alone (may be externally linked marketing files).
## 🔍 LITE: A-TO-Z AUDIT + SAVE-CLOBBER HARDENING (July 8, 2026, missionos-lite `main` → `30ce665`, live)
Aaron: "full a to z audit… every nook and cranny… I want math tested. I want ten deals… front to back, back to front. Tell me the exact truth." Also this session, shipped to lite `main` first:
- **Mission-page declutter** (`d79688c`): bold heroes (ILA greeting, The Climb, Projected paycheck, big Today's Mission board) + everything granular in one collapsed `<Drawer>` ("See all your numbers", new primitive in `components/ui.tsx` + `.drawer` CSS). Aaron approved "Just Mission for now" — other pages only on his word.
- **THE $20k-GOAL REVERT BUG** (`837fc6d`): ILA set the goal, it snapped back to $6k. Root cause: `addIlaMemories`/`forgetIlaMemory`/`clearSampleData` in `lib/store.tsx` spread the stale React `data` closure; the async post-turn reflect write stamped the OLD profile back. Fixed → all mutators read `dataRef.current`. Aaron confirmed $20k now holds.
- **Clobber-class audit fixes** (`2a2bfcf`): same frozen-draft defect in Settings save / DealDetail save / ProductsBlock → new `lib/mergeEdits.ts` (`changedFields`/`mergeUserEdits`) writes only the user's diff over latest.
- **Hardening batch** (`30ce665`, adversarially verified by 5 review lenses pre-ship): (1) ILA hands now read the LIVE store — `store.getData()` + live getters in IlaChat; shadow copies deleted. (2) MoneySetupSheet merge done for real: seed-time baseline + field diff + **row-by-row `mergeListBy`** for bills/goals/budgets (user's rows win; untouched rows follow concurrent edits AND removals); paydays+checkNets move as a glued pair. (3) Draw display honesty: Performance KPIs + Mission hero use `aboveDraw` (carried balance included; hints name "draw + carried balance"). (4) **TEN-DEAL AUDIT SUITE** `lib/payplan/tenDealAudit.test.ts`: 10 deals (incl. $0-back Kenneth-class, $0-front loser, cash deal) front-to-back + back-to-front through 4 plan shapes incl. the real Kennesaw grid, vs an INDEPENDENT hand-math oracle — engine exact to the cent; Kenneth tier-drag −$139 on the real grid is TRUE; $0-back with no tier crossing = exactly $0. 255 tests green.
- **GoalClimb label clip fix** (`1249e04`): "you're here" edge-aware `labelShift()`.
- **Banked question answered** (Aaron: "if im still in the draw shouldnt banked be 0?"): NO — banked = earned commission chipping at the $8k advance (8,885 back × 13% = $1,155; owed 8,000−1,155 = $6,845 = The Climb). Cards now say "earned against your draw · $X left to clear".
- **A-TO-Z AUDIT RAN** (15-agent workflow: 9 area sweeps + adversarial verify of every HIGH; 92 findings: **6 HIGH all confirmed** / 32 MED / 32 LOW / 22 cobwebs). Aaron said "get it done" → **ALL 6 HIGH FIXED + shipped** (`d07a171`, tsc+255 tests+build green; details in that commit message — NumInput decimal fix, useEntitled shared gate incl. /report, ILA taxRate, demo-nudge filter + dark-ink Wordmark + iOS status bar + 'ILA is locked', localMonthKey/localDayKey family, Dashboard daysOff dep, cross-tab storage adoption + pagehide flush). The 6 HIGH were: (1) PlanEditor/Products NumField eats decimal points → "22.5" becomes 225% (money-config corruption); (2) lapsed-subscriber deep-link paywall never fires (api/entitlement returns no `reason`; AppShell condition needs it) + `/report` has no gate + `lite-ent-ok` sessionStorage survives sign-out; (3) ILA's money snapshot passes `taxRate=undefined` (lib/ila.ts:87) → quotes pre-tax checks contradicting screens+her own evaluate_purchase; (4) NO cross-tab sync → two open tabs destroy each other's data locally AND in cloud (last-write-wins whole blob); (5) UTC-vs-local month fork: report page + DealDetail + fastStart slice UTC strings while dashboard uses local month → evening boundary deals land in different months on payroll report vs app; (6) Dashboard master useMemo omits `profile.daysOff` dep → stale pace after days-off change (Performance.tsx has correct deps). Notable MEDIUMs: demo deals trigger REAL push nudges (cron doesn't filter d.demo); reconcile "empty cloud row" branch resurrects an explicit reset; compToPlan invents +0.5% kickers for legacy comps that disabled them; ILA parity gaps (can't log/delete a deal, can't edit plan config, can't clear sample data, can't forget a memory); ila/reflect global-brain = cross-user prompt-injection surface; Wordmark still paints dark-theme white letters on the light theme; README badly stale; quarterly/yearly bills never hit safe-to-spend; prospect deals invisible 4 days; Face ID lock has no fallback. Verdicts: security STRONG (no auth bypass/secret leak; one default-public RPC EXECUTE grant), cobwebs LOW (0 TODOs, 0 dead deps).
## 🧗 LITE: "THE CLIMB" + DRAW POSITION + ILA LIFE-COMPANION (July 7–8, 2026, missionos-lite `main`, live)
Worked in the **missionos-lite** repo this session (added to the session via `add_repo` — `aaronprice0918-collab/missionos-lite`; lite Supabase = "MissionOS Lite" `fhqxefvpygpxregmpcow`, lite Vercel project = `missionos-lite`). Aaron's asks + what shipped to lite `main`:
- **Draw "hole" surfaced.** The projected-paycheck card computed the recoverable, ROLLING draw shortfall then hid it ("remaining check ≈ $0"). Added `drawShortfall`/`drawOwed`/`aboveDraw` to `lib/payplan/{types,calc}.ts` (purely ADDITIVE — existing pay numbers unchanged), + 3 new tests (this-month hole, carried-in rollover, earning past the draw). Aaron's model: **$8k draw, rolls over, no reset** unless stated.
- **Editable fields** in `components/PlanEditor.tsx`: "Take-home goal / mo" and "Draw balance carried in" (draw amount was already editable). Per-user — his goal = **$20k take-home**, his draw = **$8k**; others differ.
- **"The Climb"** (`components/GoalClimb.tsx`) — the BOLD Mission-page hero (goal-gradient/summit pattern, researched): gradient card, big number, an ascending ramp with a glowing "you're here" marker, milestone stops (break-even → intermediate → the take-home-goal summit flag), and a next-move line ("$X to climb out · $Y to the top"). **Shows on its own** (gated on draw OR goal; no goal → climbs out of the draw with a "SET A GOAL" chip). Placed above the paycheck card; paycheck's hole line simplified so the Climb owns that story. **LESSON (Aaron burned me twice): he sends a screenshot of the REAL card and wants the REAL thing fixed + shown in his ACTUAL white theme — a dark hand-mockup was useless. Preview in the real light theme (fg=16 20 30, accent=47 100 209, good=16 118 59, warn=164 63 38, white glass cards) or just deploy and let him see it live.**
- **ILA → sales-first LIFE COMPANION.** Aaron uploaded `missionoslitemainislamergedv1.zip` (his ILA "merged v1"): ILA reworked into coach+strategist+assistant+accountability partner, faith-aware when invited, with strong guardrails — trust rules (never invent facts), automation rules (high-risk actions = send text/move money/commit a meeting need explicit confirmation; drafts stay drafts), sensitive-memory rule (don't durably store private life facts until a memory-confirmation screen exists). Merged `lib/{ila-core,ila,ila-tools}.ts` + `app/api/ila/reflect/route.ts` + new `docs/ILA-LIFE-COMPANION.md`. **HARD RULE from Aaron: the name is ALWAYS spelled "ILA", never "ISLA"** — corrected the one `ISLA` token and renamed the doc. All lite work: tsc clean, 216/216 tests green, deployed to lite `main`.

## 🔑 DEALER SERVICE KEY ROTATED — zero downtime (July 7, 2026, ~5pm)
Rotated the dealer prod Supabase key using the modern key system (project `COMM41`/`wwwibdjgabicenvpmivy` has `sb_publishable_` + legacy keys, so independent secret keys are available). Steps done, all verified: (1) created a NEW `sb_secret_...` key in Supabase; (2) swapped Vercel `commissioned41-os` env `SUPABASE_SERVICE_ROLE_KEY` to the new secret key; (3) redeployed prod (Ready, 1m6s); (4) **verified live** — missionos.commissioned41.com loaded all real data (76/130u, $121,455 gross, team, 22 opps) on the new key. Old legacy service_role key stayed valid the whole time = zero downtime. **App now runs on a fresh key never stored on the laptop.** ⚠️ STILL TO DO (deliberately deferred to a quiet hour): (a) **disable the OLD legacy service_role key** in Supabase to fully neutralize the previously-laptop-exposed key — BUT first confirm the prod front-end uses the `sb_publishable_` key and NOT the legacy anon JWT (disabling legacy kills both anon+service_role together; if the client is on legacy anon it would break — check `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`'s value in Vercel before disabling); (b) delete the old `SUPABASE_SERVICE_ROLE_KEY` line from the laptop `.env.local`; (c) same rotation still TODO for the **lite** project (`fhqxefvpygpxregmpcow`, separate repo/Vercel). Once (a) is done, risk #1 (laptop-held keys) is fully closed.

## 🔐 MFA COMPLETE + PLAID QUESTIONNAIRE + VERCEL/SUPABASE CONNECTED (July 7, 2026, live)
Long working session with Aaron (non-technical, wants me to "have eyes and hands"). Outcomes: (1) **All work shipped to `main` and verified LIVE via the Vercel MCP** — the ILA timeout fix + tap-to-explain + env-doc fixes + warm-start hook all deployed READY (latest prod = `a2c6675`). Held back (Aaron's explicit call): `feat/universal-pay-engine` (real pay math), the stale June branches, the Next.js CVE bump. (2) **Connected Vercel + Supabase MCP connectors** (Aaron authorized in claude.ai) — I can now see deploys/projects and the DB directly. Vercel team `team_tXukadBSYoTjMq7VRxpNk9UE`; **the LIVE dealer Supabase project is `COMM41` = `wwwibdjgabicenvpmivy`** (19 users, 2 orgs, real data, RLS on every table) — NOT the empty `COMMISSIONED41`/`djnvkypjswtwqzuysmxg`. (3) **Supabase security scan** (`get_advisors`): turned ON leaked-password protection (verified cleared); remaining WARN = `current_org_id()` SECURITY DEFINER callable by anon/authenticated — LOW risk, **fix on STAGING first per Aaron's rule, do NOT improvise on prod**; `organizations` RLS-no-policy is INFO (server-only = safe). (4) **🎉 MFA NOW COMPLETE on all core provider accounts** (Risk #2, one of the two HIGHs, mitigated): Google/`aaronprice0918@gmail.com` 2-Step since 2021 (the reset root); GitHub passkey + GitHub Mobile; **Supabase/Vercel/Anthropic sign in via "Continue with Google" so they inherit the Google 2-Step**; Stripe = Google SSO + authenticator app. Updated `security/` docs (RUNBOOK Part 1 checked off, SECURITY-POLICY §4, RISK-ASSESSMENT #2 → 3 MED) to match — **honestly left domain-registrar + ElevenLabs MFA unchecked (not yet verified).** (5) **Plaid security questionnaire** (dashboard.plaid.com — vendor due diligence for prod access): Q1 contact = Aaron; Q2 = "documented policy + procedures + operational program" + uploaded the clean external PDF (`scratchpad/Commissioned41-Information-Security-Policy-v1.0.pdf`, generated from SECURITY-POLICY.md via headless Chromium — internal TODO tracker + the AI-agent-credential candor stripped, nothing false added; §3 = access-control policy, reused for Q3 doc ask); Q3 access controls = check RBAC + documented policy + centralized IAM (Supabase Auth), leave the rest; Q5 MFA = now an honest **YES**. **STILL OPEN:** finish/submit the Plaid form; the OTHER HIGH risk = laptop-held Supabase service-role keys (RUNBOOK Parts 3–5, rotate + remove from `.env.local` — untouched); domain-registrar + ElevenLabs MFA; the `current_org_id` staging fix. **Delivered to Aaron for a developer handoff:** `ILA-source-bundle-COMPLETE.md` (26 files) + `ILA-DEVELOPER-GUIDE.md` (advise NDA + GitHub access + their OWN dev keys, never prod).

## 🏥 ILA HEALTH REPORT + TIMEOUT FIX (July 7, 2026, branch `claude/session-continuity-context-bdum1u`, pushed NOT merged)
Aaron asked for a full health report on the ILA product: up-to-date, secure (dealer + our customer data), and stress-tested. **Findings:** (1) build/tests green — 73/73 tests, tsc clean (only the known test-file TS5097 noise), `next build` all 42 routes green, run against `origin/main` (branch == main, 0 apart, so this WAS the live code); (2) **security audit came back airtight** — every store route requires a real Supabase bearer token, all queries `org_id`-scoped from the *authenticated* profile (no org from request → no cross-tenant reads/writes), Sales/BDC get store leaderboards but other reps' customer + VIN are **redacted**, ILA enforces the same at tool level ("Pay is private"), license/insurance scans confirmed **zero persistence** (transient), no client-exposed secrets / no hardcoded keys (all `process.env` server-side), `canWrite` matrix is default-deny (service-role bypasses RLS so the matrix is the only gate), deal-docs in a PRIVATE bucket behind 120s signed URLs pathed by org; (3) **break test** — booted the *production* build with no Supabase and hammered it: no-token/forged-token/owner-only reads → 503, ILA/users no-auth → 403, injection keys (`__proto__`, `deals;DROP`) → 404, path traversal blocked, malformed JSON → graceful (no crash/stack leak), 5MB payload survived, rate-limit 429'd after the cap, all pages 200, **zero stack traces, server never fell over**. **THE FIX (this commit):** the 3 ILA brains (crm/hq/core) plus import/setup/perf-map/ila-brain had **NO `maxDuration`** while doing non-streaming, multi-round (crm = up to 6 sequential) model calls → could 504 mid-answer on a complex ask (same class as the payplan-parse gap fixed July 6). Added `maxDuration` to all 8 uncovered AI routes (brains + parses = 60s, voice TTS = 30s); now **every** AI route has a limit (scans 30, payplan-parse 120, cron 300). 8 files, +27 lines, additive-only; 73/73 tests + build green. **Open flags for Aaron (from the report, NOT code-fixable by me):** (a) `npm audit` = 4 high + 1 moderate CVEs, ALL in Next.js 14.2.35 — framework DoS/cache-poisoning/XSS classes, mostly mitigated by Vercel-managed hosting; plan a bump to a patched Next before selling to dealer groups (SOC 2); (b) branch `tap-to-explain` (Jul 7, 2 commits, "tap any computed number → ILA explains it") is unmerged, not live, and NOT in this memory log — decide ship or drop; (c) 6 already-merged branches safe to prune (chore/dev-server-autoport, dealer-motion-phase3-deep, dealer-motion-ring, harden-p0, ila-core-migration, p1-tests).
## 👆 TAP-TO-EXPLAIN: EVERYWHERE (July 7, 2026, branch `tap-to-explain` 2nd commit — pushed as backup, NOT merged)
Aaron: "I want it everywhere — ILA has to be the smartest person in every room." Extended the rollout to EVERY primary screen with a computed number. New shared `components/ExplainChip.tsx` (the always-visible "ask ILA why" chip; stopPropagation; never nested in a real button/anchor — wrappers holding it become `div role="button"`, links navigate via `router.push`). **Wired:** home Mission Control (hero pulse units-ring + total-gross, all 6 FeatureWindows via `explain` prop), gm-command (store-pace ring, 4 top MetricCards, all 5 GmPanels, 3 Risk-Watch lines — RDRs/invoices/product-entry are audit gold), command Sales-by-Type (units + gross per class), rdr-center (4 cards incl. punch-rate %), desking board (3 counts, were dead cards), deal-scorecard (4 per-deal cards, prompts carry deal.customer + goals), deal-center (summary strip → buttons, prompts scoped to the active search), finance-desk (Est. Payment + Gross-Recap Total Gross — worksheet is UNSAVED state, so the live numbers ride inside the prompt for ILA to check), follow-up (ScoreRing → explain the lead score), appointments (score pill per appt card), equity (timing pill → why they're on the radar), recognition-feed (every moment card taps through — the claim is a computed number). Skipped on purpose: team-command (directory, no computed numbers), appointment/equity column counts (they ARE the lists — lite's lesson), goals Team-Unit-Goal (editable input). tsc + build green. **Next:** Aaron reviews branch → merge to `main`.

## 👆 TAP-TO-EXPLAIN ROLLOUT (July 7, 2026, branch `tap-to-explain` — pushed as backup, NOT merged, awaiting Aaron's review)
Rolled missionos-lite's app-wide tap-to-explain law (lite `0821843`) into the dealer OS: any computed number on a primary screen hands off to ILA with a specific "Explain my <number> — walk the real math in plain words" prompt. **Handoff mechanism:** new `lib/askIla.ts` fires an `ila:ask` CustomEvent; `CommandDeck` (mounted on every screen via AppShell) listens, expands, and sends the prompt through the normal chat path — so voice, tool chips, and history all just work. **Primitive pattern (the lite law):** primitives take an optional `onClick`/`onExplain` and render as buttons only when a prompt is passed; when a card BOTH drills and explains, the explain is a small always-visible "ask ILA why" chip (phones don't hover) that `stopPropagation()`s past the drill; wrappers holding a nested chip are `div role="button"` (a real `<button>` can't legally contain one — Tile, F&I-manager cards). **Wired:** my-scorecard (hero Est-Pay → pay-engine walk, 4 KPI tiles, Pace Check panel, every Breakdown pay line + F&I grid stats via `explainScorecardLine(label)`), crm-desk (all 5 MetricCards), goals (team-pace ring, Delivered/Remaining/Goal-Pace cards; Team Unit Goal stays plain — it's the editable input, not computed), finance-command (4 MetricCards + per-FM "ask ILA why"). **ILA guidance** added to DEALER_PROMPT: tapped-number explains = real math from live data, short sentences, their numbers; if the user says a number is wrong, NEVER defend it — find the off input (gross, invoice, finance status, split, product entry, the goal) and say exactly what to fix where. tsc + build green (screens are behind auth, so verification bar per Aaron was tsc+build). **Next:** Aaron reviews on the branch → merge to `main` to go live; consider extending to team-command / gm-command dashboards.

## 🚀 SHIPPED TO MAIN — TIMEOUT FIX + TAP-TO-EXPLAIN (July 7, 2026, Aaron: "finish it… ship it to the live")
Aaron said ship the finished work live but explicitly trusted the call on WHAT was safe. Merged to `main` (deploys): (1) the maxDuration fix (above) and (2) BOTH tap-to-explain commits — verified safe first (both are clean descendants of main; the only server change is a single plain-English DEALER_PROMPT line, no logic/auth/privacy touch; the rest is a client `ExplainChip` + `askIla` CustomEvent). Full re-verification on the MERGED tree before pushing: 73/73 tests, tsc clean, `next build` all 42 routes green. **Deliberately HELD back (told Aaron, awaiting his explicit yes):** `feat/universal-pay-engine` (changes REAL commission math — money-correctness is sacred, and he'd chosen to keep the verified `calculateSalesPay` as the live default); the stale June branches (`glass-redesign`/`new-theme`/`ila-core-c41-logo`/`multitenant-rollout` — superseded/divergent, would break prod); and the Next.js CVE bump (real fix = a major-version upgrade, too risky to blind-ship, mostly mitigated by Vercel). **Still open for Aaron:** decide on the pay engine; schedule the Next upgrade before dealer-group sales; prune the 6 already-merged branches.

## 🔐 SECURITY PROGRAM v1 (July 6, 2026, dealer `e8cd265` + lite `72909bc`, live)
Aaron asked about ISO 27001 / SOC 2 / HIPAA compliance. The real answer: **HIPAA doesn't apply; GLBA/FTC Safeguards Rule DOES** (dealers are "financial institutions"; deal recaps = consumer NPI; Commissioned 41 = their service provider); SOC 2 = the sales artifact for dealer groups (target: Type I via Vanta/Drata when pitching groups, ~$20–50k/yr). Landed `security/` in the dealer repo: SECURITY-POLICY.md (incl. an honest AI-agent-access policy + data inventory: scan images are TRANSIENT, never stored), RISK-ASSESSMENT.md (10 risks; the 2 HIGHs = laptop-held service-role keys + provider MFA), INCIDENT-RESPONSE.md (FTC 30-day/500-consumer rule; notify-dealers-honestly standard), RUNBOOK-KEYS-MFA.md (**waiting on Aaron's hands**: MFA on all 8 provider accounts, rotate BOTH Supabase service keys then delete from laptop `.env.local`, rotate Anthropic/Stripe/ElevenLabs/ILA_BRAIN/VERCEL tokens, FileVault). Privacy policies updated in BOTH apps (full subprocessor list incl. Stripe+ElevenLabs, scans-not-stored disclosure, GLBA/Safeguards commitment) — verified rendering. **Open:** Aaron executes the runbook; DPA acceptance dates to record; quarterly access review due Oct 6, 2026; in-app MFA on the product roadmap before selling beyond Kennesaw.

## 🎯 THE −$350 STEGALL READING — phantom finance bonus (July 6 late, missionos-lite `74f7611` live + Tony's row repaired)
Tony DID re-upload on the fixed app (plan parsed as proper perDeal, source "Kennesaw Mazda salesman pay plan.pdf") but the deal then read **−$350**. Root cause pinned to the penny with his real cloud data: the signed plan's finance bonus is "$500 with **10+ units AND back PVR ≥ $1,300**" — lite's `BonusRule` held ONE condition, so the parse kept only `pvr ≥ 1300`, and with basis "front" lite derived `pvr` from FRONT gross. His 3-deal month front-PVR $1,530 → phantom $500 fired; adding Stegall (−$1,360) dropped front-PVR to $808 → bonus un-fired → +150 − 500 = **−350**. **Fix (lite `74f7611`):** `condition: Condition | Condition[]` (AND), new metrics **`backPvr`** (back/units, basis-independent) + **`fastStartUnits`** (derived from deal dates ≤ 15th in `perfFromDeals` — fast start is now fully representable, no longer "unsupported"), parser SCHEMA updated (array conditions, never split/drop a leg; guaranteeFloor = MONTHLY only, hourly → unsupported). Regression test = his real July numbers → +$150. 186 tests. **Data surgery (with backup):** repaired his saved plan row via service role (finance bonus → AND pair, fast start → fastStartUnits, guaranteeFloor 15→0 — the "$15/hr" had parsed as a $15 monthly floor); original row saved to scratchpad `tony-lite-state-backup.json`; recomputed his month from the LIVE row post-repair → Stegall shows **+$150.00**. He just restarts the app (cloud stamp newer → device pulls). **Flags for Aaron:** (1) Tony's month has a DUPLICATE deal (two identical $2,032.19 "Wills" entries) inflating units/gross — delete one; (2) his doc parsed the 24-unit ladder tier as **$1,900** but the dealer repo + live scorecard encode **$1,800** — RESOLVED July 6 night: it's **$1,900** — Aaron said go with the plan doc, and every saved prod sales plan already carried 1900 (the June 1800 transcription was stale); default+engine+tests corrected in `f8d077d` (dealer) / `6caeef0` (lite audit); (3) ILA honestly flagged unmodeled doc rules (acquisition $300, review bonus, SP-of-month/year, Christmas Club, E-plans, $15/hr guarantee) in the plan's needs-review list.

## 🚀 MERGED TO MAIN — BOTH REPOS LIVE (July 6, 2026 evening; Aaron: "merge everything… check, double check, triple check")
**Dealer** `main` → `c358efc` (universal pay engine + Studio multi-page upload + pay audit) and **lite** `main` → `bca303e` (per-deal pay fix + audit) — both pushed = deployed. Three verification layers ran on EACH repo before its push: (1) **topology + diff audit** — both merges pure fast-forwards; `lib/pay.test.ts` (exact-dollar guard), `lib/salesPay.ts`, `app/my-scorecard/page.tsx`, `components/PayPlanProvider.tsx` untouched across the whole merge range; only pre-existing-test change anywhere = lite `engine.test.ts` perfFromDeals shape (+dealRows field, zero dollar expectations); (2) **full proof on the MERGED trees pre-push** — dealer tsc clean + 73/73 + build green; lite tsc clean + 181/181 + build green; (3) **fresh-seed fuzz on the merged trees** — 52,500 scenarios across 10 brand-new seeds, **0 mismatches** (incl. 7,500 "no deal adds less than the $150 mini" property checks). ⚠️ Process catch to remember: the local `feat/universal-pay-engine` ref was STALE (this worktree pushed via `HEAD:branch`, updating only origin) — the first merge silently grabbed the old tip; caught by re-checking `git log -1` before push. **Always merge `origin/<branch>` when pushes went via `HEAD:<branch>`.** Lite's superseded miniDeal WIP preserved in `git stash` on the lite main checkout (not deleted). **Still open:** (a) Tony must RE-UPLOAD his pay plan — until then his saved plan is the old flattened % and the Rodney deal shows the honest negative; after re-upload it reads +$150; (b) lite can't express the finance-PVR AND-bonus or fast-start (flagged in `unsupported`, dealer app computes both fine).

## 🔢✅ FULL PAY-MATH AUDIT — both Kennesaw plans, both engines (July 6, 2026; dealer `lib/payAudit.test.ts`, lite `lib/payplan/kennesawAudit.test.ts` — both now on main)
Aaron (store is live, trust on the line): "plug and play with numbers, make sure the math works according to the pay plans." Method: an **independent oracle** transcribed line-by-line from the signed plans (shares zero code with the engines), then (a) every sales band edge + ladder step hand-computed ($1/−$300/$3,000/$600-mini edges etc.), (b) all **56 F&I grid cells** probed at/just-past every PVR & PPU threshold, (c) bonus/penalty/deduction/draw edges (PVR>1900 strict, VSC>50 strict, menu<95, CSI −5/−8/−11 escalation, $200/uncashed, $8k draw), (d) hand-verified anchors (Rodney Stegall −$1,750→$150; F&I 50u/$70k/PPU2.0/VSC60→13.5%=$9,450→$1,450 check), (e) **~12,500 seeded fuzz months** (same LCG seeds in both repos → lite engine, dealer universal engine, and the LIVE scorecard `calculateSalesPay`/`calculateFinancePay` all proven equal to the one oracle), (f) drift-guard asserting `KENNESAW_SALES_COMP_PLAN` numbers === `defaultSalesPlan`, (g) F&I grid constants diffed **byte-identical** across repos. **RESULT: ZERO engine defects.** The only 5 failures in the first audit run were the auditor's own hand-math (misread PVR-$1,500 column as 13.0%; it's 13.5% — engine right, human wrong; kept as a comment in the test). Known/intentional semantic differences documented in the audit files: lite clamps gross at $0 & caps draw offset at earnings (display), dealer lets estCheck go negative vs the $8k draw; lite can't express the finance-PVR AND-bonus or fast-start (single-condition BonusRule, no fastStartUnits metric) — those land in `unsupported` on parse, flagged not silent. Dealer suite 73 tests, lite 181, all green.

## 🚗💸 LITE PER-DEAL PAY — Rodney Stegall $0 bug (July 6, 2026, **missionos-lite** branch `fix/per-deal-pay` `999264b`, pushed NOT merged — awaiting Aaron)
Tony scanned the Rodney Stegall recap (Kennesaw deal #1567, 2026 CX-5): all fields landed, but "what this deal pays you" said **$0**. The recap shows front gross **−$1,750 payable** (loser deal), back $1,525 — and the DMS's own commission line says **$150 (the mini)**. Three stacked causes in lite: (1) the engine was **aggregate-only** — % of the MONTH's front gross, so this deal *subtracted* ~$437 and also dragged every forecast number; (2) `DealDetail` clamped the negative marginal with `Math.max(0, …)` → silent $0; (3) the signed Kennesaw sales plan is **per-deal** (New pays FLAT BY BAND on each deal's own gross: $400 ≥$1 / $250 ≥−$300 / $150 below; Used 25%, 30% ≥$3k, $150 mini — see `lib/salesCompPlan.ts` in THIS repo) and lite's PayPlan model couldn't represent it, so the parse had flattened it into a monthly %. **Fix (lite):** new `perDeal` rule (segments by deal category, bands highest-min-wins, pct/highMin/highPct, per-deal `minFlat` mini) in types+calc; `perfFromDeals`/`scaledPerf`/`combine` carry `dealRows` (front, category, stage weight) so month/forecast/pace/marginal all price each deal on its own gross; parser SCHEMA extracts perDeal ("NEVER flatten per-deal pay into a monthly %") + sanitizer; DealDetail unclamped + "mini deal" note; PLAN_LABELs. 135 tests (incl. Rodney regression: −$1,750 New deal = **+$150** marginal, never subtracts), tsc + build green. **⚠️ Coordination:** built in an isolated worktree because the main lite checkout had 15-min-old UNCOMMITTED WIP from a parallel session (`miniDeal` = pct-floored-at-mini — would misprice New cars, which pay banded flats not a %); that WIP is superseded and should be discarded, then merge `fix/per-deal-pay` to lite main. **After merge Tony must RE-UPLOAD his pay plan** so the parser stores the perDeal structure (his saved plan is still the flattened %).

## 📸 STUDIO MULTI-PAGE UPLOAD — lite fix ported (July 6, 2026, on `feat/universal-pay-engine`, pushed not merged)
Closed the gap the lite hotfix (below) flagged for the dealer app: Pay Plan Studio took ONE PDF only — no photos, no multi-page — while real pay plans run several pages and phones shoot one photo at a time. Ported lite `a2f776d` **plus its same-day hardening** (`87887c5`/`880f567`: route `maxDuration = 120` — this route had NONE, the exact gap that bit lite live; client size cap counts accumulated text; unreadable/HEIC-undecodable pages reported by NAME, never silently dropped; server OK_MEDIA backstop on `files[]` so one bad media type can't 400 the whole request): new **`lib/payplanUpload.ts`** (`filesToPayload` — text rides as text, PDFs as base64, photos **compressed on-device to ~1568px JPEG ~300KB** so a stack of raw ~3MB iPhone shots fits the request cap); Studio (`app/pay-plan-studio/page.tsx`) now **accumulates pages** (photos and/or PDFs, ≤10, remove misfires, "Add another page", ~4MB client total cap = Vercel body limit headroom) and can send pasted text + pages together; **`/api/ai/payplan-parse` accepts `files[]`** (≤10, ≤14MB total base64, page-labeled `Page N of M`, images as vision blocks, "read them ALL as ONE document" instruction) — **legacy single `fileBase64`/`mediaType` still works** (normalized to a one-page list). No ILA-tool gap: this is input plumbing for the existing parse capability. Verified: tsc clean (only the 18 pre-existing test-file TS5097s), 64 tests, build green, and walked live in a dev preview (3 mixed pages accumulate → compress → one `files[]` POST; remove works; legacy curl path 200-shape). Built ON `feat/universal-pay-engine` (branch `claude/modest-turing-a687dd` = fast-forward of it), NOT main.

## 🚑 LITE HOTFIX — multi-page pay-plan upload + repeat-onboarding (July 6, 2026, **missionos-lite** `a2f776d`, pushed to main/live)
First real-user field report (Tony): (1) pay-plan upload took ONE photo — his plan is 4 pages — AND the server's 200KB cap silently rejected even a single raw iPhone photo (~3MB base64) → he was quietly handed a default plan; (2) signing back in re-ran the whole onboarding (sign-out wipes the device by design, but the page showed Onboarding before the cloud pull answered). **Fixes (all in lite):** new shared `components/PayPlanUploader.tsx` (onboarding + Settings) — accumulate pages one photo at a time, remove misfires, on-device compression to ~1568px JPEG (`lib/payplan/upload.ts` `filesToPayload`); `/api/parse-payplan` accepts `files[]` (≤10, ~4.2MB total, page-labeled, legacy single `file` still works); `lib/store.tsx` gains **`cloudChecked`** (true after the first post-sign-in reconcile; reset on SIGNED_OUT) and `app/page.tsx` holds the splash while signed-in + no local profile + !cloudChecked — so a returning user gets their cloud profile, not the questions. Verified: tsc clean, 122 tests, build green, full onboarding walked in a sandboxed (Supabase-env-stripped) dev server. **Lesson: any phone-photo intake needs multi-page + client compression from day one — same gap may exist in the dealer Pay Plan Studio (PDF-only input today; Aaron spun off a session to port the fix).**
- **Follow-up hardening the same morning (`87887c5`, `880f567`):** Tony's PDF then hit the fallback live → root cause `parse-payplan` had NO `maxDuration` (only vision route without one; multi-page vision runs 90s+) → now 120s. Adversarial review of the diff found 4 real bugs, all fixed: server dropped TEXT pages when photo pages were present (half-plan = wrong numbers); client size check ignored accumulated text + text chip hidden while photos existed; **cloudChecked gate failed OPEN on a failed cloud read** (network blip at sign-in → onboarding → default plan clobbers the real cloud copy) → now a "Couldn't reach your data / Try again" screen (`CloudRetry` in `app/page.tsx`), only a real cloud answer opens the gate; Word files silently vanished from multi-picks (never parsed, ever) → reported by name + copy de-promises Word. Rate limit on parse = 8/min/email — a hammering retry user sees 429.

## 💰 LITE MONEY FIX — ILA no longer "acts broke" (July 6, 2026, **missionos-lite** `50533c4`, live)
Aaron: checks entered on the 1st/10th/15th, ILA still treated him as broke ("like those are the only checks I'm ever gonna get"). Three causes in lite: (1) **ILA's money briefing had NO income line** — `moneySection` (lib/ila.ts) gave safe-to-spend/balance but never the payday schedule, amounts, or "these recur monthly" → added an INCOME SCHEDULE line + marked safe-to-spend as cash-on-hand-only + explicit "thin balance before a payday is a TIMING gap, never brokeness"; (2) **engine averaged checkNets across paydays** → new `scheduledChecks()` in lib/money/engine.ts keeps each check's amount glued to ITS day (verified: NEXT CHECK $800 on the 10th, not the $2,267 avg); (3) **evaluatePurchase said flat "no"** to anything today's cash couldn't cover → new **"wait" verdict** (fits after the check on <date>; timing, not can't-afford). UI: check-amounts field takes one net per payday ("3000, 800, 3000" ↔ paydays "1, 10, 15"); ILA tool gains `check_nets` aligned to `paydays` (sorted as PAIRS — resolvePaydays sorts, alignment must survive). 125 tests.

## 🌍 UNIVERSAL "ANY-INDUSTRY, ANY-CYCLE" PAY ENGINE (July 5, 2026, branch `feat/universal-pay-engine`, pushed not merged)
Made the pay system a true **one-size-fits-all pay-plan generator** — any industry, any pay cycle, custom wording — while keeping Kennesaw's numbers byte-identical (existing engine tests still assert the exact $9,450 etc.). Built in 5 additive phases; **64 lib tests pass, tsc clean, `next build` green**. NOT merged — awaiting Aaron's review.
- **Phase 0 — cycle + vocab as data.** `lib/payEngine.ts` gains optional `CompPlan.cycle` (`PayCycle`: fixedLength/calendarMonth/semiMonthly/quarterly/**custom boundaries** + earned-vs-paid `payOffsetDays`/`payDayOfNextPeriod`) and `CompPlan.vocab` (`PlanVocabulary`: currency/locale/unitNoun/periodNoun + per-metric labels/formats). `DrawRule` gains `amount`/`per` (legacy `monthly` still honored). New pure helpers: `lib/payCycle.ts` (`periodFor`/`periodsBetween`/`filterToPeriod`/`describeCycle`) and `lib/payFormat.ts` (`makeMoney`/`metricLabel`/`formatMetric`). `computePay` now speaks the plan's currency. `referencedMetrics(plan)` lists a plan's perf inputs.
- **Phase 1 — data-in seam.** `lib/paySource.ts` `PerformanceSource` + generic `manualSource` (sums mapped rows, computes derived ratios). `lib/sources/dealershipSource.ts` wraps the existing deal builders as one source among many.
- **Phase 2 — templates, not hardcode.** `lib/planTemplates.ts` `templateForRole()` is the single fallback; the two automotive plans now carry cycle+vocab. `StoreSettings` gains org-level `payCycle`/`vocab`. `lib/migrateSalesPlan.ts` converts a legacy `SalesPlan`→`CompPlan` (parity-tested); **legacy SalesPlan/PayPlan scorecard calc kept as a safety-net fallback — DELETE in a follow-up PR once engine parity is proven** (Aaron's call).
- **Phase 3 — wired through UI/parser/ILA.** `EnginePayPanel`/`describeRule` speak vocab + show pay period/check date; Pay Plan Studio derives test fields from the plan's own metrics + shows a cycle line; **parser (`payplan-parse`) now extracts cycle+vocab for ANY industry**; ILA `estimate_pay` reports the pay period + check date. **New spreadsheet importer:** `lib/spreadsheet.ts` (SheetJS) + `/api/ai/perf-map` (ILA maps columns→plan metric keys) + a Studio "Import performance" panel (owner/admin, rate-limited).
- **Phase 4 — proof.** `lib/crossIndustry.test.ts`: staffing recruiter (8% margin, weekly, check +5d), SaaS AE (tiered ARR, semi-monthly, **EUR**), home-services tech (per-job %, custom 10-day cycle) — all correct, zero automotive terms.
- **DECIDED (Aaron, no-preference → my call):** my-scorecard stays dealership-native for now (generic scorecard = fast-follow); non-dealership tenants get pay via **Studio + import + ILA** (all already generic). Importer lives in the Pay Plan Studio.
- **OPEN / next:** merge to `main` after review; then the follow-up PR to retire the legacy sales calc; optional generic scorecard screen; derived-ratio hints from the parser so a raw-count CSV can feed a ratio-based grid.

## 🗣️ ILA NOW KNOWS PAY (June 28, 2026, `008803b`)
Gave ILA (the in-app CRM brain, `app/api/ai/crm/route.ts`) the compensation engine — new **`estimate_pay` tool**: runs a person's month through `computePay` (SAME engine + plans + `buildPerformance`/`buildDealRows` as the scorecards) → rate, gross, penalties/deductions, net after draw, and the **single best move to earn more this month**. ILA uses it for any "what's my pay / how do I make more" question and to coach the money. Uses the **activated CompPlan** for the role if set, else the built-in F&I grid / Kennesaw sales plan. **Pay is private** — a Sales/BDC caller only gets their OWN (mirrors the scorecard rule). Loads the `compPlans` store key into ILA's dataset. So the pay system is now end-to-end: engine → parser → Studio → activation → live scorecards → **ILA**.


## ⚙️➕ ENGINE COVERS PER-DEAL PLANS (June 28, 2026, `1ee6e1d`) — "best results" call
The engine was aggregate-only (great for the F&I grid) but couldn't model **per-deal** commission — how most **sales** plans pay (New = flat by each car's CGP, Used = % of each car's CGP). Added: **`perDeal` rule** (bands/percent-of-value per deal, segmentable by vehicleClass, mini floor; deal rows × split share summed); **AND-bonuses** (`when` accepts a Condition array — e.g. finance bonus needs ≥10 units AND back PVR ≥ $1,300); `buildDealRows` + **role-aware `buildPerformance`** (Sales share-weights splits, emits `fastStartUnits`); scorecard passes deal rows so an activated per-deal plan computes right. `lib/salesCompPlan.ts` = Kennesaw's sales plan for the engine, **test-proven** (New bands, Used %, mini, splits, AND-bonus, full month). **36 tests pass.**
- **🔑 BEST-RESULTS DECISION (money-correctness is sacred):** F&I runs on the engine LIVE. **Sales keeps its verified `calculateSalesPay` as the live default** (zero risk) — the engine sales path is proven + available to **activate** in the Studio, but NOT auto-defaulted (a subtle mismatch would change real pay). Net: the engine now handles **flat / tiered / grid / per-deal / hybrid + bonuses / penalties / deductions / draws** — any real dealership plan — and every live scorecard stays dollar-correct.


## 🔊 iOS VOICE FIX — ILA was SILENT on iPhone chat (July 1, 2026)
Aaron after launch: "cant hear her." ElevenLabs verified healthy (direct API test 200 + real mp3, Zoey id). Root cause = **iOS autoplay policy** in `lib/voice.ts`: `speak()` created a `new Audio()` seconds AFTER the user's tap (when the reply arrives), which Safari/iOS blocks → total silence in chat. (The June 24 audition page worked because it plays ON the tap.) **Fix:** one-time gesture "blessing" — on the first touch/click/key anywhere, play a tiny silent WAV on ONE persistent `<audio>` element; `speak()` reuses that blessed element (`audio.src = blobUrl`), and `stopSpeaking` clears src but KEEPS the element. Falls back gracefully where priming never happened. **Bonus fix in the same file:** the speechSynthesis fallback voice still preferred MALE voices (Jimmy-era leftover) — now prefers female (Samantha/UK-female/etc.). ILA is female. Note for future voice work: iOS cannot start audio outside a user gesture — always route playback through the blessed element.

## 🧬 ILA CANONICAL CORE MIGRATION + "POWERHOUSE" PERSONALITY — ✅ LIVE (July 1, 2026, merged to main; lite + finance pushed the same day)
Aaron's decree: ONE ILA identity/personality/voice across every Commissioned 41 product ("one bag of Doritos should taste like another bag of Doritos"). Source of truth = `missionos-lite/lib/ila-core.ts`, copied **byte-identical** into `missionos-finance/src/lib/ila-core.ts` and (new) **`lib/ila-core.ts` in this repo** — any edit to the core must be mirrored to all three or "same ILA everywhere" breaks (`cmp` them).
- **Personality upgraded (Aaron, July 1): ILA is a POWERHOUSE** — the blend of Beth Dutton (fearless fire), Billy Graham (conviction), Tony Robbins (energy/belief), Mary Poppins (warm, composed, practically-perfect command). "Says the hard thing to their face because she believes in what they can become." This replaced the old plain "calm/confident coach" line IN THE CORE, so it landed in all three apps at once. **The pure Beth Dutton persona is fully retired.**
- **`app/api/ai/crm/route.ts`:** old `BASE_PROMPT` (identity + Beth personality) → `ilaCore(viewer.employeeName || "this team")` + new `DEALER_PROMPT` capability layer ("WHAT YOU DO HERE"). Capability did NOT shrink: GSM role, master-of-every-job expertise, data watchdog, spoken-out-loud phrasing, go-deep-on-HOW/audits, data-access + tools guidance, deal statuses, math & audit rules, coaching memory — all preserved. `COACHING_PROMPT` (playbook/learning loops) untouched. ⚠️ Cache note: the stable system block now embeds the caller's name, so the prompt cache is per-USER (across their turns), no longer shared store-wide — comment in `callILAChat` updated.
- **`app/api/ai/hq/route.ts`:** owner bridge is the SAME ILA → `ilaCore("Aaron")` + "WHAT YOU DO HERE" layer; the two jobs (run the business / run the product), update_prospect guidance, APM-honesty line, and the Recommended Action / Why It Matters / Next Step footer all preserved.
- **NOT touched on purpose:** `app/api/ai/core/route.ts` (MissionOS Core = separate owner product, explicitly NOT ILA); import/setup/scan routes (functional data-intake prompts, no persona); voice route (Zoey stays).
- **Verified live (sandboxed):** ran the real routes on port 3021 with Supabase env stripped (no prod reads/writes; memory tools no-op) — both crm chat and hq chat answer in the new voice: leads with the hard truth, warm, driving, grounded (hq REFUSED to invent numbers when data blocks were unavailable). `tsc --noEmit` error count unchanged vs main (6 pre-existing test-file errors). Added `ANTHROPIC_API_KEY` to this repo's `.env.local` (gitignored) so ILA works in local dev.
- **✅ LAUNCHED (Aaron: "launch", July 1):** merged to `main` and pushed here; lite (`3eaa99f`) and finance (`eda65d2`) mains pushed too — the powerhouse ILA is deploying to all three live apps.

## ⚙️ UNIVERSAL COMPENSATION ENGINE (June 28, 2026, `e21779a`)
Refactored pay from bespoke per-plan code into ONE rules engine — `lib/payEngine.ts`. A **`CompPlan`** is a normalized list of rules: **base** (flat / tier / grid), **bonus**, **penalty**, **deduction**, **draw**, **true-up**, + effectiveDate + sourceDoc. **`computePay(plan, performance)`** runs any plan and returns rate, gross commission, bonuses, penalties, deductions, draw offset, **net pay**, **next-tier opportunities** (best move + est added pay), a **plain-English explanation**, and **confidence + warnings** — never empty without a stated reason. **`classifyPlan`** tags flat/tiered/grid/hybrid/unsupported.
- **Grid:** two-axis (X=PVR tiers, Y=PPU/PPT tiers, cell=%), `tierIndex` clamps below-grid to the floor. Opportunity gaps computed for next X and next Y tier.
- **Aaron's F&I grid** is now `FINANCE_COMP_PLAN` (in `financePayPlan.ts`); `calculateFinancePay` **delegates to the engine** — numbers regression-match the old bespoke calc ($1400×2.0 → 13% base, +0.5 VSC → 13.5% → $9,450, net $1,450 after $8k draw).
- **Tests:** `npm test` (added **tsx** devDep + `node:test`) — `lib/payEngine.test.ts`, **9 passing**: flat, tiered, grid (incl. the strict >$1,900 edge), penalties+deductions, opportunities, hybrid, unsupported-flagged, missing-input→low-confidence. Existing flat plans proven still working (acceptance #9/#10).
- **UI:** the F&I scorecard panel now shows **"Best move this month"** opportunities + a **"How this was calculated · <confidence> confidence"** disclosure listing every line.
- **✅ DOCUMENT PARSER + STUDIO — BUILT (`c808dbc`):** `/api/ai/payplan-parse` — ILA reads a pasted plan or uploaded PDF (owner/admin, rate-limited, forced tool-use, transcribe-exactly prompt with the full rule spec; ambiguities → `notes` + lower confidence, never invented) and emits a `CompPlan`. **`/pay-plan-studio`** (Admin nav, owner/admin): paste/upload → parse → review classified rules in plain English (+ confidence/notes) → **test with real numbers** through `computePay` (rate/gross/net/best-move/explanation) → **save** to the `compPlans` store key. Renders verified.
- **✅ FINAL WIRING — DONE (`1350d51`):** an **activated** CompPlan now DRIVES the live scorecard pay for its role (any role), through the engine. `CompPlanProvider` (added to layout) stores authored plans + which is active per role (`activePlanFor`); the Studio's **"Save & activate for <role>"** sets it. `lib/buildPerformance` turns a person's deals into engine metrics; the scorecard runs `computePay(activePlan, perf)` and renders the generic **`EnginePayPanel`** (rate, breakdown, best-move, rules, explanation). **No active plan → unchanged** (F&I → built-in grid, Sales/Manager → existing calc; verified non-regressive, scorecard renders clean). So: upload a plan in the Studio → activate → that role's scorecards compute from it. The FULL pay system (engine + parser + Studio + activation) is complete.


## 💵 REAL F&I PAY PLAN MODELED — PVR×PPU grid (June 28, 2026, `0cbfa53`)
Aaron sent his actual Kennesaw Mazda **Finance Manager pay plan** (eff. 8/1/2025) — the generic base+flat+back% model was WRONG for F&I. F&I is paid a **% of net profit set by a PVR×PPU grid** + bonuses. Built `lib/financePayPlan.ts`:
- **Grid** (rows PPU 1.4–2.5, cols PVR $1050–$1700, each step +0.5%, floor 9.5% / ceiling 16.0%) → `gridLookup` clamps below-grid to the 9.5% floor.
- **Commission = gridRate × net profit.** Net = back-end gross (⚠ individual chargebacks-trailing-12mo NOT tracked → uses gross; surfaced on-screen).
- **Bonuses:** PVR **over** $1,900 → +0.5%; VSC penetration **over** 50% → +0.5% (strict `>`; exactly $1,900 does NOT trigger).
- **Draw:** $8,000 semi-monthly (handwritten Daryl override of printed $6,000) → $16k/mo advanced, recouped from commission. est check = commission − monthly draw (can be negative = deficit carried).
- **Manual/not-auto (policy, shown as caveats):** menu <95% → −5%; CSI below region → −5% (−3%/consec mo); $200 fine per contract uncashed >20 days.
- **Scorecard:** F&I role now renders `FinancePayPanel` (PVR/PPU/VSC, effective rate, money breakdown, the live grid with the active cell lit) instead of the generic plan. Math sanity-checked ($1400×2.0=13%, +VSC=13.5% → correct).
- **⚠️ DEPENDENCY:** only shows when the signed-in user resolves as **F&I** — i.e. Aaron must be in the **finance-managers roster** with a name his login fuzzy-matches (the resolve fix `e02248b` handles fuzzy). As owner/Admin he maps to Manager tier unless he's in that roster. **Aaron to verify the grid numbers transcribed right + that the panel appears.**


## 🆕 NEW FEATURES: Manager-TO lost-reason + Speed-to-Lead clock (June 28, 2026, `97e1735`)
- **Lost-reason capture** (crm-desk): every status→Lost now routes through `moveLead` into a reason modal (Payment/Trade/Credit/Vehicle unavailable/Competitor/Spouse/Think/Bad experience/No contact/No show) before committing. Persists as **`lostReason`** (JSONB) + shows on the Left-Showroom drill. "No lead dies blind." (VISION #18 deepened.)
- **5-minute speed-to-lead clock** (Follow-Up Center): live ticking elapsed timer on New Lead rows — green <5m, amber "respond now" to 30m, red "overdue — escalate" past. (VISION #5.) Uses `leadCreatedMs` (id timestamp) + a `useNow` 20s tick.
- **✅ Customer 360 / Journey timeline (VISION #4) — NOW BUILT (`a8fb597`):** after adding `statusHistory` logging (`4e62c51`), built `components/CustomerJourney.tsx` — a real timestamped lifecycle (created → each move → appointment → won/lost + lost reason) shown as a **"Journey" tab** in the lead drawer when editing. Honest: only shows what actually happened; older leads (pre-logging) show creation + current state and fill in as they move.

## 🆕 NEW FEATURES: Appointment Board + Equity Mining (June 28, 2026, `d72a94d`)
Building out the VISION roadmap on existing data (no comms/inventory deps). Both pass the 10-Second Rule + one-tap actions.
- **Appointment Board** (`/appointments`, `30a9f2a`): every scheduled appointment in 4 buckets — Today / Needs Attention (passed→no-show/reschedule) / Upcoming / Showed. Each card: lead intent score + one-tap **Confirm** / Call / Open. Confirm persists via a new **`appointmentConfirmed?: boolean`** on the lead (JSONB key, NOT a schema change). Nav badge counts today's still-to-confirm.
- **Equity Mining** (`/equity`, `d72a94d`): an HONEST trade-up radar — **lease maturities** (exact from `leaseTermMonths`) + the **18–54mo retail ownership window**. Deliberately does NOT fabricate an equity $ figure (no live vehicle-value feed); labels it timing-only + "confirm at appraisal." Ties to the inventory-feed roadmap.
- Both wired into nav (The Deal group) + `commonAccess` (every role). AuthProvider access lines staged via targeted patch so the uncommitted dev bypass stays out.
- **🔍 Palette finding:** the audit's "swap public gold/green → steel-blue" was a FALSE POSITIVE — the rebrand already remapped the *token values* (`--mission-gold: 138 180 255`, `--mission-green: 96 150 255` = steel-blue in the live theme). "gold/green" are legacy token NAMES holding blue values. No palette work needed; skipped.


## 🔐 PRODUCTION AUDIT (security + correctness + UX) — June 26, 2026 (`5844b5d`)
Aaron asked for an 11-phase production-readiness audit + fixes. Ran 3 parallel senior-level audits. **Honest scope:** Phases 5/6/8 (relational-DB internals, foreign keys/indexes/orphans, 10k-user load test) don't apply — the app stores per-org **JSONB blobs in `app_store` + browser state**, not a relational schema, and a real load test can't run here. Phase-4 AI features that are unbuilt/blocked (call summaries, trade-rec, equity mining, inventory search, tap-to-text) can't be "tested/fixed." Audited what's real.
- **VERDICT (strong):** tenant isolation **SOLID** (every `app_store`/storage/user query scoped to the caller's server-derived `org_id`; no IDOR, incl. the deal-docs signed-URL path which checks `path.startsWith(orgId/)`); **no committed secrets** (service-role + all 3rd-party keys server-only, `.env*` gitignored); **money math VERIFIED correct** (calculateDesk doc-fee single-count, TAVT reconstruction exact, all denominators guarded — no NaN/Inf); **no P0 crashes, no dead links, workflows intact** (lead→desking→finance→deal→RDR); dev-auth bypass safe (client-only, every route re-verifies the bearer).
- **FIXED this pass (`5844b5d`):** (1) **security fail-closed** — `ai/license-scan`, `ai/insurance-scan`, `ai/voice` now return 503 in prod if the Supabase server client is null, instead of bypassing the auth gate (were fail-OPEN → open cost-bearing AI proxy if the service-role key ever went missing). (2) **my-scorecard** F&I/Manager deal sub-tables got mobile-card fallbacks (last wide tables with no <md alternative). (3) **money:** GA $3 lemon-law fee now included for Lease units. (4) finance-command By-FM grid single-col on phones.
- **✅ RATE LIMITING — DONE (`886e914`):** `lib/rateLimit.ts` (Upstash Redis via REST, **no new dep**, **fails OPEN**) applied to ai/crm (40/min/org), ai/import + ai/setup (10/min), license/insurance-scan + voice (20–30/min/IP), public waitlist (5/min) + telemetry (60/min). **🔑 AARON TODO to switch it ON:** create a free Upstash Redis DB (upstash.com) and set `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` in Vercel (documented in `.env.example`). Until then it's a no-op (app unchanged).
- **🔜 STILL DEFERRED (lower-priority hardening + architectural):** `ai/voice` voiceId allowlist (P2); telemetry POST is unauth (P2, capped+owner-read+now-rate-limited). Back-office wide tables (admin/setup/import) left desktop-only (acceptable). True production-for-10k-dealers still needs: Stripe billing live, the comms layer (Twilio/A2P), the inventory feed, and load/uptime infra — all previously-known roadmap, not bugs.

## ✅ UX-STANDARD ROLLOUT — COMPLETE (June 26, 2026) — all waves shipped to `main`
Aaron: *"Don't stop till you're done with the entire project."* Done. Every primary screen now leads with action, not a report. Shipped across `6dde631`→`a771649`:
- **Reusable primitive:** `components/NextActionBar.tsx` (ILA read + one Do-This-Next CTA) + `components/MorningBrief.tsx` (GM good-morning brief).
- **Wave 1:** goals (read + killed spreadsheet→pace cards), finance-command (read + glossary disclosure), command (clickable rows), gm-command (clickable Risk Watch).
- **Wave 2:** **AppShell nav attention badges** (fresh leads / deals-in-desk / open RDRs + global "N needs you" pill); **home** leads with NextActionBar + in-app confirm (no window.confirm); **Showroom/crm-desk** ILA "work the floor next" banner + tap-to-call/email + one-tap **Manager TO** (VISION #18).
- **Wave 3:** finance-desk (ILA queue read), deal-scorecard ("how to fix this deal" + smart default), deal-center (worklist band + un-hid row actions), **admin** (741-line wall → 3 tabs), team-command (tap-to-call/email + Message), my-scorecard (tappable Today tasks, table→cards, hid $0 rows).
- **Wave 4:** welcome + pricing copy **de-CRM'd** + tagline *"Know the Lead. Execute the Mission."* + OS thesis (verified live); recognition feed (Biggest Deal + Gross Leader moments).
- **🔑 Palette finding was a FALSE ALARM:** default `:root` tokens already render `mission-gold`/`mission-green` as steel-blue (138 180 255 / 96 150 255) — the audit agent judged by class NAME not computed value. No swap needed; public screens already on-brand.
- **Consciously deferred (cost/risk, not standard-failures):** auto-opening CrmAiPanel per floor card (would fire a paid ILA call per lead on load); converting remaining owner-only `window.prompt`/`confirm` (Clear Deals, Remove user) to glass modals; crm-desk 1320px desktop table slim-down (mobile uses cards). Blocked modules unchanged (comms/Twilio, inventory feed, lead-source integrations).

## 🎯 UX-STANDARD ROLLOUT (audit + punch list) — June 26, 2026
Aaron: *"Everything I just uploaded is our new standard. I want everything to reflect it. Everything."* (the Dealer-OS vision + the UX Laws / 10-Second Rule). Ran a 4-agent audit of every primary screen vs the standard. **Recurring verdict: screens show STATE but don't drive ACTION** — they answer "where do I click?" not "what do I do next?" The fix pattern repeats: lead each screen with a one-line **ILA read + ONE "Do this next" CTA**, make every interactive-looking thing actually work, kill spreadsheets, surface attention globally.
- **▶️ Reusable primitive built:** `components/NextActionBar.tsx` (ILA read + one one-click next-best-action). Drop it atop any screen. The GM `MorningBrief.tsx` is the fuller reference.
- **✅ WAVE 1 DONE (`977a0ae`):** `goals` (worst offender — added ILA read; replaced the horizontal-scroll spreadsheet with clean pace cards) · `finance-command` (ILA read + glossary→disclosure) · `command`/Sales-by-Type (inert top-grosser rows now link to the deal) · `gm-command` (Risk Watch alerts now click through to the fix; dropped static "GM Notes").
- **🔜 REMAINING PUNCH LIST (prioritized, all buildable on existing data — NO Twilio/inventory/lead-feed deps):**
  - **WAVE 2 (P0, demo-critical):** **`crm-desk`/Showroom** — add an ILA "work the floor next" banner (who to greet/TO/open now) + **tap-to-call/`tel:` & `mailto:`** on cards + a one-tap **Manager TO** button (VISION #18); collapse the 11-col desktop table to ~5 scannable cols; raise `CrmAiPanel` from collapsed→inline. **`app/page.tsx` home** — lead with a "Do this next" action (currently opens with branding, not action); replace `window.confirm` (line ~116) with an in-app confirm; traffic-light tones on FeatureWindows. **`AppShell` nav** — live attention badges (fresh leads / deals-in-desk / open RDRs — counts already computed in MorningBrief) so the chrome answers "what needs me?"; mobile bottom-tab or open rail; kill the duplicate theme selector.
  - **WAVE 3 (P0/P1):** **`admin`** (741-line wall) → split into 3 sub-views (People & Logins / Pay Plans / Roster); replace `window.prompt`/`confirm` with glass modals. **`my-scorecard`** — move "Today" tasks ABOVE pay + make them tap into the work; replace the `min-w-[900px]` deal-credits table with cards; hide $0 bonus rows. **`finance-desk`** — replace 3 fake metric cards with an ILA queue read (urgency-sorted, finalize blockers inline). **`deal-center`** — top action band (not-office-clean / not-punched tiles) + drop hover-reveal row actions. **`deal-scorecard`** — add ILA "how to fix this deal" block. **`team-command`** — one-tap Message / View Scorecard per teammate; de-dupe the 3 roster renderings.
  - **WAVE 4 (public + polish):** **`welcome`/`pricing`** — de-CRM the copy (kill "CRM Desk"/"CRM that follows up" lead framing), add the tagline *"Know the Lead. Execute the Mission."*, lead with the "OS that tells your store what to do next" thesis. **Palette:** public screens still run gold/green as primary accent — swap to steel-blue (crimson = alerts only) to match the locked identity. **`finance-command`/`my-scorecard`/etc.** minor. **`recognition-feed`** add 2-3 earnable moments.
  - **Bright spots to KEEP as the template:** `import`, `setup`, `business-card` already pass (AI-does-the-work + one clear action).

## 🧭 POSITIONING LOCKED: NOT a CRM — a DEALER OPERATING SYSTEM (June 26, 2026)
Aaron sent the master vision: **stop positioning as a CRM.** A CRM gets compared to VinSolutions/DealerSocket/Tekion/DriveCentric/eLead; a **Dealer Operating System** creates a new category (Apple→iPod ecosystem, Tesla→car OS). Legacy CRMs answer *"where is my customer?"* — **Dealer Mission OS answers "what should my dealership do next?"** One platform, every dept, one customer record, one AI brain (ILA), continuously thinking. **Full north star written to `VISION.md`** (positioning, tagline *"Know the Lead. Execute the Mission."*, the AI-brain doctrine, the 10 roles, the 21-module map with honest BUILT/PARTIAL/BLOCKED/NET-NEW status, the C41 family roadmap). Added to `CLAUDE.md` vision section.
- **🔑 THE FOUR-PART TEST (now law, in CLAUDE.md):** every screen must **save time, increase profit, improve accountability, or elevate the customer experience** — or it doesn't ship. This is the anti-clutter discipline that keeps it an OS, not another dealer platform.
- **Grounded gap read (current app vs the 20-module vision):** strong **back half** already exists — Showroom (physical-presence gate), Desking + Finance + GA lease/tax engine, Deal Center + RDR, Goals/Scorecards, F&I Report, nightly brief, ILA's 4-layer brain. Biggest **net-new = the CRM front half**: unified AI-prioritized **Lead Inbox**, **5-minute speed-to-lead** w/ escalation, **AI Follow-Up Center** (Day 1/3/7/14/30 cadence), **Lead Scoring 0–100** on cards, **Appointment Board**, **Manager TO/lost-reason**, **Equity Mining**, **Service-Drive Opportunities**. Lead model already carries source/status(8-stage)/credit/trade/appointment, so most front-half screens are buildable on existing data.
- **⛔ BLOCKED on external deps (don't start until the dep lands):** Communication Hub + AI Call/Text Review (needs **Twilio/A2P** — deferred for cost); Inventory Match Engine (needs the **live inventory feed**, Aaron's pending CSV).
- **▶️ RECOMMENDED first slice (demo-undeniable for Kennesaw July, zero blocked deps): the AI-brain "what's next" surfaces** — (1) **Mission Dashboard + GM "Good Morning" brief** (the proof-screen of the whole thesis; morning twin of the nightly brief) and (2) **Salesperson Daily Plan** ("do these 12 first"). Both run on data we have. **Awaiting Aaron's pick on where to start.**

## 🏷️ RENAME: MissionOS → "DEALER MISSION OS" — ✅ LIVE (June 26, 2026, `2fd406a`)
Aaron: *"change the name from MissionOS to 'Dealer Mission OS' because Commissioned 41 is going to offer more than one type of operating system — the one we built is for dealers."* So **"Mission OS" is now the platform family**; this product is the **dealer** line.
- **Wordmark:** Aaron picked a **"DEALER" eyebrow** *above* the locked chrome **MISSION** + steel-blue **OS** signature (core mark untouched → reusable when future OSes ship). `MissionWordmark` now takes `eyebrow` (default true); eyebrow is sized in **`em`** so it scales at every placement. **`eyebrow={false}`** on the tiny app-header mark (11px — qualifier illegible there). Hero (`app/page.tsx`) got a matching "DEALER" eyebrow above the `MissionLockup` image. Verified on login + welcome in preview — renders clean.
- **Text sweep:** renamed every **dealer-product** reference (script that protected "MissionOS Core" + skipped the personal-OS files) across metadata (`app/layout.tsx`), **PWA manifest** (`name` "Dealer Mission OS", `short_name` "Mission OS"), marketing/legal copy (welcome/pricing/privacy/terms/signup), ILA's prompts (crm/hq/setup/import routes), and lib helpers (data/email/appPulse/dailyReport/stripe/states). Also renamed the internal uppercase `MISSIONOS …` data-section labels (appPulse/crm/hq/PlayerCard) in sync.
- **⚠️ LEFT UNTOUCHED ON PURPOSE — "MissionOS Core"** = the **owner-only PERSONAL executive OS** at `/mission-core` + `app/api/ai/core/route.ts`. It's a *different* OS in the family, not the dealer product. (Also separate: the **MissionOS Finance** repo.) **OPEN Q for Aaron:** does Core want its own distinct name too (e.g. "Mission OS Core")? Left as-is for now.
- Domains/URLs (`missionos.commissioned41.com`) NOT changed — that's the live subdomain; revisit with Aaron if he wants the URL to carry "dealer".

## 🔍 FULL-SYSTEM AUDIT + FIX SWEEP — ✅ LIVE (June 26, 2026, `7d4361b` + `efb9da2`)
Aaron: *"run a full audit on our entire system... everything working to the standard... and what am I missing to be #1."* Ran 5 parallel sub-agent audits (routes/UX, money, AI/features, build/security, competitive). Verdict: app is whole, crash-free, build green (44 routes), tenant isolation holds, no committed secrets, dev-bypass correctly NODE_ENV-guarded + uncommitted. **Fixed every finding (`7d4361b` money/security/bugs, `efb9da2` UX):**
- **💰 MONEY (sacred):** GA packet `lib/gaPacket.ts` printed MV-7D TAVT diverged from the desk engine (omitted doc fee, deducted rebate, applied trade credit unconditionally) → now computes from `calculateDesk()`. Three `docFee || 899` traps (`finance-desk`, `desking`, `deal-entry`) forced a legit **$0** doc fee to $899 → `?? / != null`.
- **🔒 SECURITY:** the two AI vision endpoints (`license-scan`, `insurance-scan`) had NO auth → added a signed-in user gate (401 if not authed).
- **🐛 BUGS:** nightly-brief manager-inbox delivery never fired — `lib/dailyReport.ts` read the `team` store key as an array when it's an object `{salespeople,managers,financeManagers,lienholders}` → fixed shape. Deal Scorecard was hardcoded to `deals[0]` → added a deal picker.
- **🟡 UX STANDARD ("if it looks interactive it must BE interactive"):** 6 dashboards had static stat cards that looked tappable → new shared `components/DealsModal.tsx`; wired drill-downs on **finance-command** (4 top cards + per-FM cards), **goals** (Delivered Units), **command** (per-class Units/Gross), **rdr-center** (4 status cards), **my-scorecard** (4 personal KPI tiles → your credited deals), **team-command** (org-chart rows now focus the matching directory card).
- **🏆 TOP-3 COMPETITIVE GAPS (Aaron-owned, the roadmap to #1, besides reply-to-text):** (1) **live inventory feed** (his "tomorrow" CSV export — unlocks ILA's deal-goal→exact-trim/stock#), (2) **ILA outward** (outbound email/web-chat + AI voice so she reaches customers, not just staff), (3) **DMS/data pipe** (auto-sync deals instead of manual import). Deferred infra Aaron still owes: Resend (`RESEND_API_KEY`/`EMAIL_FROM` + verify domain) + `CRON_SECRET` in Vercel to switch on nightly-brief email.

## 🚀🟢 LIVE IN PRODUCTION (June 24, 2026, `5fe2ea9`) — glass-redesign MERGED to main
**Aaron said "finish and publish live." `glass-redesign` (97 commits) was fast-forward-merged to `main` and is LIVE** on missionos.commissioned41.com + commissioned41.com (verified HTTP 200, ILA present). This shipped the WHOLE redesign at once: the navy+neon-green "living glass" identity + living border + lime-OS wordmark; **ILA** (renamed from Jimmy — female, Beth Dutton persona, ElevenLabs "Zoey" voice) running each store's floor AND Aaron's owner-only **Mission Control** in the C41 HQ screen (adoption + data-health + self-hosted runtime-error sensor); the **license scanner** (PDF417 camera → autofill); the **Showroom rebuild** (in-showroom physical-presence gate, deliveries pot came-in/remote, slide-in tabbed intake drawer); the multi-state tax engine; Stripe MVP; Georgia lease. The 5 seeded TEST leads were removed pre-launch (store back to 29 real leads).
- **⚠️ FUTURE WORK: branch off `main` again** (glass-redesign is now merged; main = source of truth). Same rule as before: branch, push as backup, only merge to main on Aaron's go.
- **⚠️ NOT runtime-verified by me before launch:** ILA's chat (no local ANTHROPIC key) and the authed Showroom render (local auth-gates) — built + tsc/lint/build green + routes serve, but Aaron should eyeball the live Showroom + try ILA; hotfix fast if anything's off.

Last updated: **June 24, 2026** — SIGNATURE IDENTITY locked & built (`716ee37`): the neon-green **living border** + chrome-MISSION/lime-**OS** wordmark are now THE MissionOS logo (Aaron: "I want everything mission related to be the logo moving forward"). Login fully redesigned to navy glass; wordmark + recolor rolled across welcome/pricing/mission-core/header. Earlier this session: National multi-state tax ENGINE (Layer 1), Stripe billing MVP + /pricing, Georgia LEASE end-to-end. All on branch `glass-redesign`.

## 👁️ ILA'S EYE + FULL REBRAND — ✅ LIVE ON MAIN (June 25, `c21ee0b`, deploy READY)
- **ILA's mark is now a PHOTOREAL ROBOTIC EYE IMAGE** — `public/brand/ila-eye.jpg` (1000px/471KB, AImage Aaron generated + **owns outright**; the 2.9MB source was dropped, original is in his Downloads). Glossy black pupil, fibrous circuit-laced iris, steel-blue with **amber-gold** data nodes. `components/IlaLivingCore.tsx` renders it as a circular bg with a pure-CSS **living layer**: breathing glow corona, a pulsing pupil core, a slow sensor-scan over the iris. Same `IlaLivingCore({className, intensity})` API → used in the hero (`app/page.tsx`) + the CommandDeck FAB + panel avatars.
- **🔑 WHY a real image (the hard lesson):** I spent ~12 rounds hand-building the eye in SVG/CSS (procedural iris, then a metallic camera-aperture). Aaron's verdict stayed *"still looks super fake."* **SVG has a hard ceiling — it will never read photoreal.** I leveled with him; he generated an owned photoreal image; I made it alive with CSS. WebGL was rejected too (headless preview can't render it → can't iterate; fragile on iOS PWA). **For "real," use a real owned/licensed/AI-gen image + a CSS life layer — not SVG, not WebGL.** (Procedural SVG eye history is in git if ever wanted.)
- **FULL PALETTE REBRAND — lime green RETIRED, app is now black + steel-blue (gold lives ONLY in the eye).** Off Aaron's reference images: he first locked an iridescent-violet liquid theme, then the photoreal eye reset the palette to **its** colors (blue iris + amber traces). Final: token-driven in `app/globals.css` default `:root` — `--mission-green: 96 150 255` (steel-blue, the primary accent), `--mission-gold: 138 180 255` (luminous light steel-blue — was amber `232 178 98` but Aaron: *"bland like dark Dijon mustard"* on the UI, so amber stays only in the eye), bg deepened to near-black blue. Swept ALL hardcoded lime literals (`#39f58c`, `57 245 140`, etc.) across every component (Command Deck, ambient blobs, team role colors, welcome headline gradient…) → blue. **Living border** recolored to a bright blue comet w/ white-hot tip (Aaron *"really miss the live border"* — it had gone faint; brightened it).
- **Verified:** clean prod build; audited home / Showroom / Team / public landing — cohesive, no lime, nothing broken. Live across `0ea332c` (eye+theme), `b99d6b1` (drop amber→blue), `c21ee0b` (living border). **NOTE:** must STOP the dev preview server before `next build` or it throws "Cannot find module for page /api/ai/core" (stale .next collision).
- **Also done:** retired/removed the old procedural `IlaCore`/`buildBolts` + `ILAAvatar.tsx` (dead); hero copy already de-cold'd ("running {N} behind pace…").
- **LATER (June 25 eve):** hero now shows the **MISSIONOS wordmark** (not the eye) + an uplifting/motivational read (Aaron: hated the negative "you're behind" message). The **eye lives in the Command Deck FAB + panel avatars**, now wrapped in a spinning **electric-blue ring** (`.ila-ring`) + soft glow (Aaron asked). The `live` HUD theme = her palette; alt themes (graphite/showroom) left as-is.

## 🧠 ILA'S BRAIN — learning system + nightly brief + watchdog — ✅ LIVE (June 25 eve, all on `main`)
**All in `app/api/ai/crm/route.ts` (her chat brain) unless noted. Zero new infra cost — rides the Opus calls she already makes. Stored per-org in `app_store`. Can't runtime-test locally (no ANTHROPIC key); verified by build + mirrors the proven rep-memory.**
- **4-layer learning memory** (Aaron: "always learning... learn from customers AND salespeople... patterns"): rep (`repProfiles`, pre-existing — now also captures **personality + life motivation**), **customer** (`customerMemory`, tool `remember_customer`), **store playbook** (`storeMemory`, `remember_pattern`), **mistakes** (`mistakeMemory`, `remember_mistake` — learns from EVERYONE's mistakes incl. her own, logs the warning SIGN, catches repeats). All loaded into her context (bounded: recent-40 customers, last-80 patterns/mistakes). Directive: "figure it out then SAVE it so you never solve it twice."
- **Standing directives added to COACHING_PROMPT:** always drive the bottom line (honest gross max), always improve the process, **a daily action plan per rep** (personality + their number), **watch the sales manager's back** (flag un-followed-up leads + missing rebates the customer qualifies for vs `monthlySetup`), **build a stuck deal** for a manager, and **inventory→exact trim/stock#** ⚠ GATED (never invent a stock# until real inventory is loaded).
- **Nightly End-of-Day Brief** for managers (`lib/dailyReport.ts` + `/api/ai/daily-report` GET/POST + `/api/cron/daily-report` + `vercel.json` cron `0 1 * * *` ≈ 9pm ET). ILA writes a tight manager brief (where we stand / highlights / takeaways / what to fix / tomorrow's plan). Delivery (nightly only, not manual refresh): GM-screen card (`components/NightlyBrief.tsx`), each manager's MissionOS inbox, AND **email via Resend** (`lib/email.ts`, fail-soft). **PENDING: Aaron must set up free Resend (account + verify domain + `RESEND_API_KEY`/`EMAIL_FROM` in Vercel) to switch on email; needs `CRON_SECRET` in Vercel for the cron.**

## 📊 REAL DEALS IMPORTED + DOC-FEE AUDIT FIX — ✅ June 25 eve (`ed7da5d`, LIVE)
- Aaron pasted Kennesaw's real **June F&I Manager Deal Log** (76 deals) and imported it (replaced the demo deals). I **audited both ends** (pulled live deals via service-role, ran MissionOS's own `lib/data` math vs the DMS totals): import was **dollar-accurate** (76u, front $1,481, back $119,969, total **$121,450** all matched per-FI-manager too).
- **🔑 BUG FOUND + FIXED (money-correctness):** MissionOS showed total **$189,779** — it was adding the **$899 store doc fee × 76 = $68,324** on top of an imported Front that **already includes the doc fee** (DMS logs bake it in). Fix: `lib/dealImport.ts` `buildDeal` now defaults imported `docFee` to **0** (not the store default) when the source has no separate doc-fee column. Patched the 76 live deals in place (now $121,455 ≈ DMS; snapshot saved to `deals_backup_docfix`). **Lesson: DMS "Front" already contains the doc fee — imports must NOT re-add it; manual deal-entry still adds doc fee normally.**
- **🔜 PENDING (Aaron, tomorrow):** send the **inventory export** (stock#/VIN, year/model/trim, MSRP, miles, new/used) — kennesawmazda.com is a **Dealer.com site that 403s scrapers** + is JS-rendered, so a CSV export (or a verified Chrome-MCP session) is the path, not server scraping. Once loaded → build ILA's inventory brain (given a deal goal → exact trim + stock#). Note: public site has MSRP/price but **NOT invoice** (DMS only). Stock# format on Kennesaw deals looked like Mazda dealer stock #s (M133321, N602873), NOT "last 7 of VIN" — reconcile against the real inventory feed.

## 📸 ILA VISION DOC CAPTURE — ✅ SHIPPED TO MAIN (June 25, `6abee2a`, LIVE)
- **The pivot (Aaron):** the PDF417 barcode scanner was fragile (zxing/zxing-wasm/paid SDK all flaky on mobile). Aaron: *"Can't we just make it to where the salesperson can take a picture of the customer's license, drop it into Isla, and Isla can fill in the information?"* Then: *"the salesman should also be able to take a picture of the insurance card... she'll put all the insurance information in the deal... that way we'll always have a picture of the customer's driver's license and their insurance card on file."*
- **What shipped (2 commits, `8dd8092` license + `6abee2a` insurance/storage):** ILA reads a PHOTO of the FRONT of the DL **and** the insurance card via Claude vision → fills the customer + insurance fields. **Both photos are kept on file.**
- **Files:** `app/api/ai/license-scan/route.ts` + `app/api/ai/insurance-scan/route.ts` (Claude `claude-opus-4-8` vision, raw fetch, return structured JSON fields). `components/LicenseScanner.tsx` → generalized to `DocScanner` (modes `license`|`insurance`; exports `LicenseScanner`/`InsuranceScanner`); photo downscaled client-side to 1600px JPEG, handed back so the deal can file it. `app/api/deal-docs/route.ts` = **private Supabase Storage bucket `deal-docs`**, paths `${orgId}/${leadId}/${kind}.jpg`, served back ONLY via 120s signed URL (GET verifies the path starts with the caller's orgId → tenant isolation); bucket auto-created on first upload via service role. `CrmLead.driverLicenseDocPath`/`insuranceCardDocPath` store PATHS only (no image bytes → JSONB blob stays small). `crm-desk`: insurance scan button in the Insurance tab, `DocOnFile` "on file / view" rows, `uploadDealDoc` on save (pending photos upload once the lead has a real id).
- **⚠️ Verify on Aaron's phone (prod-only):** vision needs the prod `ANTHROPIC_API_KEY`; storage needs the service role to be allowed to createBucket/upload. Everything is **fail-soft** — if a photo upload fails, the field-fill still lands and the deal still saves. Build green (43 routes), both scanner UIs verified rendering in local preview, deploy `6abee2a` READY.
- **🔜 Cleanup pending (low priority):** remove the now-unused `zxing-wasm` dep + `public/zxing_reader.wasm` + dead `parseDriverLicenseScan`/`aamvaDateToIso`/`normalizeSuffix` in crm-desk.

## 🟢 SIGNATURE IDENTITY — ✅ BUILT (June 24, `716ee37`, glass-redesign)
- **The look Aaron locked** (off a mockup he loved, then the real login screenshot — "I absolutely love this... bravo... I want my logo to look like this"): a **living border** (neon-green→white comet continuously laps a glass panel's rim) + the **wordmark logo** = chrome "MISSION" + lime-green "OS". See `~/.claude/.../memory/c41-design-language.md` for the full lock.
- **Reusable pieces:** `app/globals.css` → `.living-border` (`@property --lb-angle` + conic-gradient masked to the rim via `mask-composite:exclude`; `prefers-reduced-motion` → static green rim). `components/BrandMarks.tsx` → `<MissionWordmark/>` (the official logo; use it for EVERY brand placement, not body prose).
- **Applied:** login (`components/LoginPanel.tsx`) fully redesigned navy-glass + living border + wordmark + green focus glow (old `bg-[#050506]`/chrome/white-OS retired). Living border also on Jimmy's hero window (`app/page.tsx`) + Command Deck answer panel. Wordmark in app header (`AppShell`), welcome hero, mission-core, pricing. **Recolored the 2 off-palette public pages:** welcome (gold #f6e7b0/#bda14a/rgba(212,175,55) → navy+green) and pricing (all `cyan` → `mission-green`).
- **Verified:** tsc+lint+`next build` (41 routes) clean. Login + welcome confirmed live in local preview (wordmark OS = rgb(57,245,140), living border conic+lbSpin+mask present, no console errors). Pricing code-verified (zero cyan left) but sits behind the AppShell auth gate locally → eyeball on the Vercel preview. NOT merged to main.
- **🔜 NEXT:** review with Aaron; optionally carry the living border to other hero surfaces; the broader page-by-page cohesion pass (most inner pages already ride MetricCard/theme tokens, so they're largely navy+green already).

## ▶️ RESUME HERE — say "comm 41" / "let's work on Commissioned 41"
When Aaron says that, READ THIS FILE top to bottom (it's the memory of record — Aaron's home `~/.claude` memory is root-owned & unwritable, so this repo file is it; the fix is `sudo chown -R pricefamily:staff ~/.claude`, not yet run). Repo: `/Users/pricefamily/commissioned41-os`. Then read `CLAUDE.md` + `BRAND.md`.
- **Current branch state:** ALL recent work is on **`glass-redesign`** (latest `ab319c3`), pushed to origin, **NOT merged to main, NOT live** — waiting on Aaron's review. `main` is the deployed/live store and is untouched.
- **✅ DONE this session (June 23–24):** **Georgia LEASE — the whole thing** (see "🚗 GEORGIA LEASE" section): lease engine (money factor + residual + GA TAVT taxed on the leased portion, capitalize/upfront), glass-standard `/lease` + `components/LeaseDesk.tsx`, **Retail|Lease tabs on Desking AND Finance** (Finance auto-detects from the desk push), trade tax-credit manual checkbox, lease **finalizes into a tracked deal** (`isLease`, counts as a New unit, tracks in the F&I report like a new car, "Lease" badge in Deals), and **two print sheets** — clean **Customer** proposal (payment/due/term/miles/price/rebate, NO dealer math) + detailed **Manager** recap. Also: **Monthly Setup** (Jimmy's rate-sheet/incentive/residual intake), dashboard units drill grouped New-then-Used with neon dividers + clickable-to-deal names.
- **🔜 NEXT (nothing lease left):** (1) **Aaron to review `glass-redesign` and decide on merge to main** (going live). (2) Stripe billing — **MVP DONE (subscribe → checkout, test mode); next layer = webhook to auto-provision/activate a store's org on payment + public (logged-out) pricing page.** (3) tiny footnote: confirm with the DMS whether cash-down belongs in the GA lease tax base (left OUT for now). (4) Group reporting (deferred). (5) key rotation (low impact).
- **⚠️ Verifying on the Vercel/preview hits PROD data** — if you mutate prod to test (e.g. push a deal), snapshot first and RESTORE after (done cleanly this session).

## 🇺🇸 NATIONAL / MULTI-STATE TAX ENGINE — ✅ LAYER 1 BUILT & VERIFIED (June 24, on glass-redesign)
- **Why:** Aaron's directive — "I need this to work in all 50 states." The blocker to going national is TAX, not the rest of the app (CRM/desking/tracking/AI are already national). GA-locked code lived in 3 spots: `lib/desk.ts` (`georgiaFees` + `calculateDesk`), `lib/lease.ts` (GA TAVT lease), `lib/gaPacket.ts` (MV-7D form).
- **🔑 ARCHITECTURE RULE (Aaron, June 24): every state is INDIVIDUAL and self-contained.** One state = one file (`lib/states/<code>.ts`). NOTHING shared between states — no common rate constant, no cross-import. Changing one state can NEVER move another state's number. A state is "locked in" when its `status: "verified"`; it only changes if someone edits that state's own file.
- **🔑 SAFETY RULE: no unverified number ever ships.** `assertQuotable(profile, kind)` throws unless `status === "verified"` AND the needed rate is non-null. Unverified states are listed as "coming soon" but physically cannot return a quote. (Aaron's "build & verify ourselves, light up states as proven" — chosen over buying tax-data API, June 24.)
- **What's built (`lib/states/`):** `types.ts` (the `StateTaxProfile` contract + `assertQuotable`), `ga.ts` (**Georgia, VERIFIED** — exact pre-refactor values: TAVT 7%, full trade credit, doc $899 uncapped, eTitle $199/title $18/reg $25/lemon $3-new; lease method `payment_sum`), `fl.ts` + `tx.ts` (**review stubs, rates null → cannot quote**; TX scaffolds the genuinely-different `upfront_full_price` lease method), `index.ts` (registry: `getStateProfile`, `isStateVerified`, `listStates`, `DEFAULT_STATE="GA"`).
- **Engine refactor (behavior-preserving):** `calculateDesk(lead, profile = GA)` now reads rate/trade-rule/doc-cap/fees from the profile; `georgiaFees` is now DERIVED from the GA profile (single source of truth). `lib/lease.ts` gained `calculateLease(input, profile)` that routes by `profile.lease.method` (GA `payment_sum` → the untouched `calculateGeorgiaLease`; other methods throw until verified).
- **✅ VERIFIED via temp `/api/taxcheck` route (built, run, deleted):** `deskMatch: true` (new desk === old GA formula, byte-for-byte), `leaseMatch: true` (`calculateLease(GA)` === `calculateGeorgiaLease`; sample base $438.27 / TAVT $1,104.44 / monthly $468.95 = last night's known-good), `guardBlocksUnverified: true` (Florida throws instead of guessing). tsc clean across project.
- **🔜 NEXT (multi-state):** (1) **plumb `state` (and ideally buyer registration ZIP) onto the deal/lead** so the engines get the right profile — today everything defaults to GA. (2) **Verify & light up real states one at a time** (deep-research the 50-state matrix: tax method, trade credit, lease model, doc-fee caps → verify each → flip status). (3) **Local (county/city) rate layer** — many states tax at the BUYER's address, thousands of jurisdictions; deferred (this is where a rate API would eventually come in). (4) Per-state paperwork/forms (generalize `gaPacket.ts`) — lower priority. (5) Store Settings UI to pick the store's state; honest "supported states" list from `listStates()`.

## 💳 STRIPE BILLING — ✅ MVP BUILT & VERIFIED (June 24 morning, on glass-redesign)
- **Account:** Aaron created the Stripe account himself (night of June 23). Keys live in `.env.local` ONLY (git-ignored), entered via clipboard so no secret ever hit the chat: `STRIPE_SECRET_KEY` (sk_test_…, server-only — NO NEXT_PUBLIC), `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` (pk_test_…), `STRIPE_PRICE_ID`. **TEST MODE** — no live key yet, no real money. Live key + Vercel env vars are a later step (only when going to production).
- **Price decision (Aaron, June 24):** **$499/month per store** — CONFIRMED, staying at $499 (Aaron reconsidered $699 on June 24 and chose to keep $499 as a great "easy yes" price point). One product "MissionOS", one recurring price. **Founding-rate idea (noted, not built):** list price could later go to $699+ (MissionOS replaces a $1,500–2,500/mo dealer CRM plus desking/reporting — it's underpriced), while Kennesaw + early AMSI stores lock a founding rate ($499) forever. Revisit when closing Kennesaw. Aaron's call for now: keep it simple at $499, stay "lean and mean" on cost instead of raising price.

## 🤖 JIMMY (the AI) — COST + EFFICIENCY (June 24)
- **Jimmy = Claude Opus 4.8** ($5/M input, $25/M output). Lives in `app/api/ai/crm/route.ts` (+ core/hq/setup/import routes). Raw `fetch` to the Anthropic API (not the SDK). `ANTHROPIC_API_KEY` is **prod/Vercel-only — NOT in local .env.local**, so Jimmy can't be runtime-tested locally; verify Jimmy changes via tsc/lint/build + the deployed Vercel preview (check Vercel logs).
- **Cost analysis (June 24):** the sales playbook (`lib/salesPlaybook.ts`) is **~10,200 tokens** and rides on every CHAT message along with Jimmy's base brain (~1K) + store economics (~300) + the live store snapshot (~4.5K) → **~16K input tokens per chat message**, all re-billed (no caching before today). So: a full chat ≈ **~10¢** (~18¢ if remember_rep fires and re-sends), a quick action (health/draft/next-action, no playbook/snapshot) ≈ **~1¢**. At scale a busy store ≈ **$200–450/mo** uncached. Current real spend is tiny (pre-launch, Aaron-only testing).
- **✅ PROMPT CACHING — DONE (June 24, glass-redesign), the "lean and mean" win.** `callJimmyChat` now sends `system` as content blocks with `cache_control: ephemeral`: block 1 = STABLE brain (BASE_PROMPT + storeContext + COACHING_PROMPT/playbook, ~11.5K tokens, shared across every rep at the store + every turn), block 2 = the volatile snapshot (reused across turns in a rep's session). Tools render before system so the breakpoint caches `REMEMBER_TOOL` too. Same model/prompt/output — **cost change, not behavior change**; cuts chat input ~70–80% → a busy store drops to ~$60–130/mo (75–88% margin at $499). Added a `[AI/CRM] cache_read=… cache_write=… in=… out=…` console.log to confirm hits in Vercel logs. NOTE: the LIGHT-action prefix (~1.3K tokens) is below Opus's 4096-token cache minimum, so those don't cache — but they're already ~1¢, fine. tsc+lint clean; NOT yet exercised live (needs the preview). NOT merged to main.
- **✅ ADAPTIVE THINKING — DONE (June 24, glass-redesign), after Aaron's correction.** I first cut thinking for "lean/fast" — Aaron pushed back: **"Jimmy still needs to be able to think and reason."** Right call. The chat path (`callJimmyChat`) now sends `thinking: { type: "adaptive" }` + `output_config: { effort: "medium" }`; chat `max_tokens` bumped 1400 → 4000 for reasoning headroom. Adaptive = Jimmy thinks hard only when the question deserves it (deal structuring, audits, objections) and barely on a quick lookup; the visible reply stays tight (prompt-enforced) since reasoning is internal. The existing tool loop already echoes full content (incl. thinking blocks) back + extracts the final text block, so it composes with `remember_rep` correctly. effort is a dial — "medium" keeps him snappy for the floor; bump to "high" for harder grinding. Light actions (callClaude) stay non-thinking (callClaude reads content[0] = would need a text-block find before thinking could be added there). Caching keeps INPUT cheap; thinking keeps him SMART — not in tension.
- **✅ LIVE-QUERY TOOLS — DONE (June 24, glass-redesign), the "knows everything" demo wow.** Refactored the snapshot loader: `loadStoreData(orgId)` pulls the raw map ONCE, `buildSnapshot(map,...)` formats the capped text, and a shared `dealLine()` formatter (now also shows lease info — bonus fix). Two new tools on `callJimmyChat`, dispatched in the tool loop with try/catch guards, running over the already-loaded `data` (NO extra DB calls) with the same Sales/BDC privacy redaction: **`query_deals`** (filter the FULL uncapped deal list by salesperson/financeManager/stage/vehicleClass/dealType[finance|cash|dnq]/issue[missing_invoice|negative_gross|products_on_cash|missing_docfee] + limit; returns matching deals via dealLine + a units/front/back total) and **`rep_detail`** (one rep's sales board + F&I board + their full deal list + coaching-memory profile). `JimmyContext = {orgId,settings,viewer,data}` threads through; tool loop bumped 4→6 iterations for chaining; BASE_PROMPT nudges Jimmy to reach for the tools before saying a number isn't available. tsc+lint+`next build`(all routes) clean. NOT runtime-tested (needs preview — no local key). NOT merged to main.
- **✅ LOOKUP_RATE TOOL — DONE (June 24, glass-redesign).** `handleLookupRate` reads `ctx.data.monthlySetup.rateSheets.lenders` (shape from `lib/monthlySetup.ts`: LenderRates → RateTier → RateRow{termMonths,buyRate,maxAdvancePct?,year?,minAmountFinanced?}) and filters by lender/tier/term/year (all substring/loose except term=exact), returns precise buy-rate lines + reserve reminder; if nothing loaded → tells them to load Monthly Setup; if no match → lists loaded lenders/tiers so Jimmy can guide. Capped at 80 rows. Wired into the tools array + dispatch; prompt nudge updated. The killer F&I demo line: "LGE buy rate for a 2024 at 740 for 66 months." Jimmy now has 4 tools: query_deals, rep_detail, lookup_rate, remember_rep. tsc+lint+build clean. NOT runtime-tested (needs preview).
- **✅ COMMAND DECK — Jimmy's front door, BUILT (June 24, glass-redesign).** Problem Aaron hit: there was NO way to ask Jimmy store-wide — the floating orb (`JimmyAvatar.tsx`) was retired June 23 and never re-mounted; the dashboard Jimmy window is one-way; the only chat was buried per-lead in CRM Desk. Aaron rejected reviving the orb ("it overtook content + feels outdated") and a plain box; wanted a "wow." **Design = the "Command Deck"** (mocked via the visualize tool, Aaron "love this"): a docked glass console along the bottom edge — idle = a thin breathing line; ask it (type OR talk) and it RISES into a translucent answer panel, then settles back; never floats over content (main has `pb-24`). New `components/CommandDeck.tsx` mounted globally in `AppShell` (authed pages only); reuses `authHeaders` + `useVoice`; cyan (#5EE7FF) glass to match the mock; shows "⚡ Drilling the deals / Reading the rep / Pulling the rate" chips per the tools Jimmy fired (route now returns `tools` alongside `reply` — additive). **🔑 VOICE (Aaron, June 24): Jimmy speaks his answer OUT LOUD by default** (one-tap mute, persisted to localStorage `jimmyVoice`); the sigil pulses (`cd-on`) while speaking. Improved `lib/voice.ts` voice picker to prefer natural/neural voices. tsc+lint+build clean. NOT runtime-tested (needs preview).
  - ⚠️ Known refinement for the preview pass: on mobile the docked deck (bottom-center, near-full-width) may overlap the AuthProvider "Aaron Price | Admin" pill (bottom-4 right-4, z-50). Check + nudge on the preview.
- **🔜 NEXT for Jimmy:** (1) **streaming** the chat path + wire CrmAiPanel/JimmyAvatar to render tokens live (the "feels alive" win). **⚠️ Deliberately deferred (Aaron-safety call, June 24):** streaming changes the chat's response CONTRACT (JSON→token stream) on Jimmy's most-used path + touches client components, and CAN'T be runtime-tested locally (no local key) — too risky to ship blind right before the July Kennesaw demo. Do it when we can watch it work on the Vercel preview together. (2) later: **action tools** (send the text / update the lead, with a confirm gate) and **vehicle/product knowledge** (needs inventory data the app doesn't have yet — Kennesaw runs vAuto separately; real integration project). Consider migrating raw fetch → `@anthropic-ai/sdk` when touching this.
- **What's built:**
  - `scripts/stripe-setup.mjs` — idempotent setup script (reads `.env.local`, creates/reuses the MissionOS product + $499/mo price by `lookup_key: missionos_monthly_499`). Re-runnable, won't duplicate. Created `prod_…` + `price_1Tlrq…` in test mode.
  - `lib/stripe.ts` — `getStripeServerClient()` (mirrors `getSupabaseServerClient()` null-pattern) + `getMissionOsPriceId()`.
  - `app/api/checkout/route.ts` — POST creates a Stripe Checkout Session (`mode: subscription`, the $499 price, `allow_promotion_codes`, absolute success/cancel URLs from request origin), returns `{ url }`. Verified: returns a real `cs_test_…` checkout URL (200 OK).
  - `app/pricing/page.tsx` — glass-standard, mobile-first pricing card ($499/mo, 6 feature bullets, Subscribe button → POST /api/checkout → redirect to Stripe). Handles `?status=success` (emerald "You're subscribed!" banner) and `?status=cancel`. **Gotcha fixed:** `glass-sweep` is a CHILD light-sliver overlay (absolute, 70px), NOT a container class — putting it on the panel collapsed it into a tilted strip; correct usage is `<span className="glass-sweep" aria-hidden />` inside the panel. Added `pb-32` so the footer clears the floating admin bar.
  - Installed `stripe` npm pkg (v22). (The "5 vulnerabilities" npm warning is pre-existing in OTHER deps — did NOT run `audit fix --force`.)
- **Verified in preview (test mode):** /pricing renders (mobile), Subscribe → /api/checkout → live `cs_test_` Stripe URL, success banner shows. No console errors. tsc/build not re-run this session — do before merge.
- **🔜 NEXT layer (not built):** (1) **Webhook** `app/api/stripe/webhook/route.ts` — on `checkout.session.completed` / `customer.subscription.*`, provision/activate the store's org (tie into existing `lib/provision.ts` + the `NEXT_PUBLIC_SIGNUPS_OPEN` gate) so paying actually opens the account. Needs `STRIPE_WEBHOOK_SECRET` + Stripe CLI (local) or deployed URL. (2) **Public pricing page** — right now `/pricing` renders inside the authenticated app shell (nav + "Aaron Price | Admin" bar); a prospect-facing page should be logged-out chrome (like login/signup/terms opt out). (3) Wire signup flow: pay → then provision, instead of the demo-closed gate. (4) Go-live: add live keys + set env vars in Vercel.

## ✅ STATUS RECONCILE (June 23, late) — branch is AHEAD of the old "NEXT" notes
Everything below is committed + pushed to `origin/glass-redesign` (clean tree, NOT merged to main, NOT live). The earlier "NEXT = queue items 4–6" is now mostly done:
- **Item 4 — retire floating Jimmy bubble → ✅ DONE (`d7b3d33`).** JimmyAvatar orb removed from `AppShell`; Jimmy lives in the Dashboard "watching the floor" window + the CRM panel only.
- **Item 5 — Recap/back-screen fields → ✅ DONE (`687a41a` + finance rework).** `app/finance-desk/page.tsx` now carries `buyRate`, dealer `reserve`, `weOwe`, charges netting out of gross, Deal↔Recap back-layer on one screen.
- **Item 6 — monthly setup page (PDF → Jimmy extract → confirm → live) → ✅ BUILT (June 23, latest session; NOT yet committed).** Jimmy's "fuel" page, end-to-end:
  - `lib/monthlySetup.ts` — types + registry for 3 doc types (`rateSheets` / `incentives` / `residuals`), `summarizeExtraction`, `extractionIsEmpty`, and `formatSetupForJimmy()` (bounded render of the live setup into Jimmy's prompt — buy rates/incentives/residuals, capped so a big rate book can't blow the context).
  - `app/api/ai/setup/route.ts` — owner/admin-gated extraction endpoint. One forced tool per doc type (`submit_rate_sheet`/`submit_incentives`/`submit_residuals`) so Claude (`claude-opus-4-8`) returns exactly the lib shape. Accepts a PDF (base64 `document` block) OR pasted/CSV text. Size caps (400k chars / ~10MB PDF). Same prod-only `ANTHROPIC_API_KEY` constraint as Import — the live parse can only be exercised on the **Vercel preview**, not local.
  - `app/api/ai/crm/route.ts` — Jimmy snapshot now loads the `monthlySetup` store key and injects `formatSetupForJimmy()` so Jimmy quotes from THESE current rates/rebates/residuals instead of guessing. (Import of `formatSetupForJimmy` was the one loose end at 9:45pm — added; tsc clean.)
  - `app/api/store/[key]/route.ts` — `monthlySetup` added to `allowedKeys` + admin-only `canWrite`.
  - `app/setup/page.tsx` (NEW) — doc-type cards (gold-active, show Loaded/Empty + effective month), drop-PDF-or-paste, "Send to Jimmy" → preview table ("here's what Jimmy pulled" + checksum line) → **Confirm — make it live** (merges into `monthlySetup`, keeps other doc types, stamps `updatedAt/By/sourceFile`). Nav entry "Monthly Setup" (Fuel icon) in the Admin group, owner/admin-only.
  - **Verified:** tsc ✅, lint ✅, next build ✅ (39 routes), preview render ✅. **Committed + pushed** to `glass-redesign` (`b2c68b0` page/route/wiring, `5ff8dd3` year fix). NOT merged to main / not live.
  - **🔑 SCHEMA FIX (`5ff8dd3`) — rate sheets carry vehicle model-year.** Validating a real LGE CCU indirect sheet exposed that bank/CU sheets grid the buy rate by THREE axes: credit tier × term × **vehicle model-year** (a 2024 66mo A+ = 4.50% but a 2020 66mo A+ = 7.35%). v1 schema had only tier×term → would've lost the year and mis-quoted. Added `RateRow.year` (+ `minAmountFinanced`); tool schema/system prompt now emit one rate per filled year×term cell (skip blank/N-A/TBD); `formatSetupForJimmy` renders year + higher per-tier caps; `/setup` preview has a Year column. **When loading any year-tiered rate sheet, year is now a first-class dimension.**
  - **✅ VALIDATED on a real sheet (June 23).** Ran the LGE CCU indirect sheet (effective June 12, 2026; the rate sheet for LGE-financed deals — one of the ~19 lenders) through the same model+schema the endpoint uses: extracted cleanly — 5 tiers with rates (A+ 740+ … D 640-659; tier E "below 640" = rate-at-approval, correctly omitted), 7 year bands (2024+→2018), 103 buy-rate cells, all the reserve/GAP/max-advance/eligibility rules as lender notes. **Aaron eyeballed it against the paper and confirmed correct ("cool looks good").** Extraction logic is proven on real data.
  - ⏳ **Still pending (low-priority, not blocking):** (a) the deployed Vercel-preview click-through (upload→parse→save end-to-end with the prod key) — Aaron didn't want to navigate Vercel this session; do it whenever convenient or after merge. (b) wire per-tenant Jimmy **fee schedule** from `storeSettings` (separate sub-task from rate/incentive/residual setup). NOTE: Aaron is non-technical about git/Vercel and gets anxious about "breaking" the live site — reassure that branch work can't touch prod, keep browser/deploy steps minimal, and prefer doing such steps for him over walking him through them.
- **Also landed since the notes below (not yet folded into older sections):** Finance one-screen Deal↔Recap rework (`687a41a`, `0773aa5`, `e9fd25d`, `31f0bd6`); Showroom scoped to TODAY's floor (`82ea56c`, `a02c8a5`, `70d5681`); Deals clean-glass list, retired the 1900px grid (`4c1ec82`); full-width layout app-wide + Sales Manager on Deals (`56d033d`); Admin remove-a-staff-login + roster delete chips (`ccb6291`); Sales-by-Type top grossers (`1e01466`); F&I Report managers side-by-side + cash in back gross (`a39740b`, `f3e3a9e`).
- **✅ DEFERRED MULTI-TENANT ROSTER FIX — DONE (June 23, late; this session).** Both name-leak sources now load THIS org's roster, not the hardcoded Kennesaw `team` seed:
  - `app/api/ai/import/route.ts`: `callerIsAdmin` → `resolveImportCaller(req)` returns `{ ok, orgId }` (reads `user_profiles.org_id`, mirrors crm route); new `loadOrgRoster(orgId)` reads the org's `team` store key (`{salespeople, managers, financeManagers}`); `rosterPrompt(roster)` is built from it. Fallback to the Kennesaw seed ONLY for the default org / local dev — a 2nd tenant with no roster yet gets an EMPTY roster and the parser keeps names verbatim (no cross-tenant mis-map). Generalized the Kennesaw-specific name examples out of `SYSTEM` + `rosterPrompt`.
  - `app/finance-command/page.tsx`: dropped the module-level `team.filter(role==="F&I")`; now `const { financeManagers } = useTeamLists()` (per-org via TeamProvider), filtered through the new `isHouseBucketName()`. **House-bucket decision (Aaron, June 23): filter them out** — house/unassigned/N-A names stay in the roster for deal attribution but DON'T earn a per-manager scorecard card. New reusable `isHouseBucketName(name)` in `lib/data.ts` (matches house/unassigned/n-a, case-insensitive). F&I Report now shows only the 2 real FMs again; store-level totals (Finance Deals 50, Gross $97,504) are unaffected (they read from `deals`). Verified in preview: House Employee card gone, Bo + Aaron unchanged.
  - Verified: tsc ✅, lint ✅, `next build` ✅ (35 routes), preview render of /finance-command ✅ (Aaron 22/$50,450/$2,293, Bo 36/$56,217/$1,562 unchanged). Import route can only be fully exercised on the Vercel preview (ANTHROPIC_API_KEY is prod-only).
- **✅ JUNE DATA LOAD — DONE (verified live June 23).** Prod `deals` = **62 deals, all June 2026, all import-sourced** (`imp-` ids, every one has a dealNumber). FM split matches the plan exactly: Aaron 22 · Bo 36 · Daryl NeSmith 1 · House Employee 3. Aaron's grid IS merged (20/22 of his deals carry products + VINs, PPU 4.36); **Bo is still product-blind (0/36) — the ONLY remaining gap, waiting on Bo's personal grid (data only Aaron/Bo can supply, not a build task).** The old "~55 stale deals" are gone. The earlier "pending, not executed" note was written before the load ran and never cleared — ignore it.
- **Open / next, in priority order:** (1) **🚗 GEORGIA LEASE SUPPORT — NOT BUILT, has a money-risk bug (see dedicated section below).** Scope + build a real lease deal type. (2) Item 6 deployed-preview click-through (low-priority) + per-tenant Jimmy **fee schedule** from `storeSettings`. (3) Stripe billing (Aaron's keys). (4) Group reporting (deferred until a real group). (5) Key rotation (low impact). [Plus, data-only when it arrives: merge Bo's grid to fill his products.]

## 🚗 GEORGIA LEASE — ✅ BUILT & VERIFIED (June 23–24, on glass-redesign)
- **🔑 CUSTOMER PRESENTATION RULE (Aaron, June 24):** the sheet the salesperson hands the customer shows ONLY — **payment, due at signing, term, miles, selling price, rebate.** NOTHING else. **Hide money factor, residual, depreciation, rent charge, gross cap** — all dealer math (esp. money factor = the rate/reserve markup) stays on the MANAGER recap only, never the customer copy (mirrors the retail rule "customer copy excludes interest rate", `lib/desk.ts`). Typical lease terms at Kennesaw: **24–36 months**. The detailed `buildLeaseWorksheetHtml` is the MANAGER recap; a separate clean CUSTOMER sheet is the presentation. **✅ DONE (June 24):** `buildLeaseCustomerSheetHtml` (lib/lease.ts) renders the clean customer "Lease Proposal" — store header, vehicle, big Monthly Payment + Due at Signing (with term · miles), then Selling Price / Rebate / Term / Mileage, signature lines; NO dealer math. LeaseDesk gained a `milesPerYear` input (default 12k) + TWO print buttons: **Customer sheet** (clean) and **Manager recap** (detailed). Verified: customer HTML contains the 6 fields and leaks NO money factor/residual/rent-charge; manager recap keeps them. tsc/lint/build clean.
**A working Georgia lease desk now exists** — Aaron's "I'm not going to bed until we can do a GA lease" ask. `lib/lease.ts` (`calculateGeorgiaLease`) + `/lease` page (nav "Georgia Lease" under The Deal; managers/F&I/admin/owner). Lease payment = depreciation `(adjCap−residual)/term` + rent charge `(adjCap+residual)×MF`; **GA TAVT = (base payment × term) × 7% — full rate, leased portion only, NOT full value**; tax figured on the pre-tax base so capitalizing never re-taxes itself; capitalize-vs-upfront toggle (default capitalize). Prefills residual%/MF/term from any lease residual loaded in Monthly Setup. **Verified live:** $35k/$33k/58%/.00125MF/36mo → base $438.27, tax base $15,777.72, TAVT $1,104.44; capitalized $468.95/mo, upfront $438.27/mo +$1,104.44 at signing. tsc+lint+build(40 routes) clean. NOT merged to main.
- **🔑 LEASE TRACKING RULE (Aaron, June 23):** a lease **tracks exactly like a new-car deal** — same unit count, same gross, same scorecards; there is NO separate lease reporting lane. **A lease is ALWAYS a New Mazda** → `vehicleClass` stays **"New"** (counts as a new unit, which is correct), residual/MF come off the **Mazda Financial** residual sheets (loaded via Monthly Setup). So the old `"Lease"→"New"` collapse was RIGHT for counting — it just threw away the lease flag + taxed it as a purchase. FIX = add an **`isLease` marker** on the deal that still counts as New everywhere in reporting, but flags it so the lease math/tax/forms apply. Do NOT build a separate lease deal type through the reporting math.
- **🔑 TRADE TAX-CREDIT = MANUAL CHECKBOX (Aaron's design, June 23) — ✅ DONE.** Rather than auto-detecting why a trade doesn't qualify, a single manual checkbox in the trade-in section — **"Customer gets tax credit for this trade"** — lets the F&I manager decide. Covers ALL no-credit cases at once: a **leased trade** (doesn't own it), a trade **titled in someone else's name** (e.g. wife buys in her name, trades husband's car, he's not on the new loan), etc. The app shouldn't outsmart the manager. Implemented on `taxCreditEnabled` (already drove `lib/desk.ts` `taxCredit`): clear relabeled checkbox + hint added to BOTH `app/desking/page.tsx` and `app/crm-desk/page.tsx` trade sections. (Scrapped an earlier `tradeIsLease` auto-flag in favor of this simpler control.) Still auto-off when the NEW car is a lease (separate, kept).
- **Follow-on status (June 23–24 night):** (a) ✅ visual polish to glass standard. (b) ✅ **printable lease worksheet** — `buildLeaseWorksheetHtml` in `lib/lease.ts`, "Print worksheet" button on the lease desk (customer-facing recap: payment, due-at-signing, full breakdown, GA tax method). (c) ✅ **DONE — the whole lease is finished (June 24).** Finance auto-detects the type (Retail|Lease tab AUTO-SET from `lead.vehicleClass === "Lease"`, FM can override) and a lease opens the embedded `LeaseDesk` ABOVE the shared F&I close flow. **A lease now FINALIZES into a deal record:** `Deal` gained `isLease` + `leaseMonthlyPayment`/`leaseTermMonths`/`leaseDueAtSigning`; `LeaseDesk` reports its summary up via an `onResult` callback (held in a ref → no render loop); finance-desk's finalize() stamps `isLease: dealType === "lease"` + the lease summary, with `vehicleClass "New"` so it **counts as a New unit and tracks in the F&I report exactly like a new car** (NO reporting/comp-math changes needed — that was the whole point of the lease-tracks-as-New rule). `deal-center` shows a "Lease" badge (amber) beside the New/Used pill. **LIVE-VERIFIED end-to-end on prod then fully reverted:** pushed Milly Wells as a lease → Finance auto-opened lease → finalized (Mazda Financial) → deal recorded `vehicleClass:"New", isLease:true, leaseMonthlyPayment:468.95, term:36, due:1367.95`, showed NEW+LEASE badges in Deals → deleted the test deal (back to 62) + restored the lead (Desking/New). tsc/lint/build(40) clean.
- ORIGINAL DESIGN NOTE — **Aaron (June 24): the Finance screen does NOT ask — it AUTO-KNOWS.** When a deal is pushed from the CRM/desk, it carries its type (the Retail/Lease choice made at desking, persisted on the lead), so Finance opens straight into the right mode — a lease deal lands on the lease desk (reuse `components/LeaseDesk.tsx`), a retail deal on the existing retail finance flow; the FM never re-picks (can still allow an override toggle). Then persist the lease STRUCTURE (MF/residual/term/payment) onto the deal record + flag `isLease` so the F&I report tracks it (still a New unit per the tracking rule, shown/handled as a lease). Today finance-desk collapses `vehicleClass "Lease"→"New"` on push (`app/finance-desk/page.tsx:181,229`) and loses the flag — fix that to set `isLease` instead. (d) ✅ **DONE via tabs (better than the planned guard):** Desking now has a **Retail | Lease segmented tab** (Aaron's design) — picking Lease swaps the whole desk to the lease structuring (correct lease TAVT), so a lease is never taxed as a purchase. The redirect-banner idea was scrapped for this. (e) ✅ leased-trade → manual tax-credit checkbox. (f) ⏳ confirm cash-down-in-tax-base with the DMS (left OUT by default).
- **🔑 RETAIL/LEASE TABS (Aaron's design, June 24) — ✅ DONE:** the deal headlines Retail or Lease (`vehicleClass` persists the choice — Lease ⇒ stored "New"... actually stored "Lease"; counts as New unit per the tracking rule), details swap behind the tab. Shared `components/LeaseDesk.tsx` (extracted from `/lease`) is used by BOTH the standalone `/lease` page and the Desking Lease tab — identical math + glass look. Lease tab prefills customer/vehicle/selling price from the lead. Verified live: tab swaps desk, prefill works, toggling persists the header label, no console errors; tsc/lint/build(40 routes) clean. NOTE: switching the tab sets `vehicleClass` Lease↔New immediately (persists); Retail default is "New" (a Used retail re-picks Used elsewhere).
- ORIGINAL ASSESSMENT (kept for the gap detail): the app could FLAG a lease but the old desk/packet still STRUCTURE/TAX it as a purchase.
- **What exists (partial):** CRM Desk has a "Lease" vehicle-type option (`app/crm-desk/page.tsx:521`); the desk correctly suppresses the trade tax credit on leases (`lib/desk.ts:25`, GA gives none); lease residuals/money factors can be loaded as Jimmy reference data (the Monthly Setup `residuals` doc type) but nothing consumes them.
- **🔴 Gap 1 — no lease payment math.** `calculateDesk()` → `estimatePayment()` (`lib/desk.ts:34-48`) is loan amortization. A lease = depreciation `(capCost − residual)/term` + rent charge `(capCost + residual) × moneyFactor`. No money factor / residual value / cap cost / cap-cost reduction / acq fee / mileage allowance fields exist on the deal/lead. A "Lease" deal today quotes a FINANCE payment (wrong).
- **🔴 Gap 2 — GA lease TAVT computed wrong (over-charges).** Desk (`lib/desk.ts:27`) and the official packet (`lib/gaPacket.ts:142-148`) tax full selling price × 7% = PURCHASE TAVT. **Aaron's authoritative GA lease rule: TAVT base = (base monthly payment × # of months); rate = full 7%.** KEY CONCEPT (Aaron stressed): the 7% is the FULL rate, but it applies ONLY to the leased/rental portion (the payment stream), NOT the full vehicle value like a purchase — "he's only paying tax on the portion he's renting." So a lease is taxed on ~the sum of payments, not the car's price. Example: $400 × 36 = $14,400 × 7% = $1,008. Cash down: Aaron does NOT recall it being in the base → leave cash down OUT unless his DMS shows otherwise (one-line toggle). "Base payment" = the PRE-TAX lease payment (depreciation + rent charge). **COLLECTION METHOD (resolved June 23):** customer's choice, DEFAULT = capitalize the TAVT into the lease (rolled into cap cost / spread across the payment); OPTION = customer pays it upfront at signing (cash due, kept out of the payment). Build as a toggle on the lease deal: *Capitalize TAVT (default)* vs *Pay upfront*. The TAVT is always computed on the PRE-TAX base payment first, so capitalizing it does NOT re-tax itself (no double-dip). (Lessor/lessee split: not pursued — treat as customer pays the 7% on the leased portion; revisit only if it comes up.)
- **🔴 Gap 3 — leases collapse to "New".** finance-desk rewrites `vehicleClass "Lease" → "New"` on push (`app/finance-desk/page.tsx:181,229`), so leases aren't tracked/reported as leases.
- **🟡 Gap 4 — no lease paperwork.** Packet builds purchase forms (MV-1, MV-7D TAVT); no GA lease agreement / lease tax worksheet.
- **Build plan when greenlit:** real `Lease` deal type end-to-end; money-factor/residual payment engine fed by the loaded `residuals` setup; GA lease TAVT (payment×term×7% upfront) in BOTH desk + gaPacket with a clear lease/purchase branch; lease forms. Lean on Aaron for exact tax basis (he does these weekly) before shipping the math — it's customer money.

## 🆕 JUNE 23 (later session) — Import / Jimmy Intake built, on branch `glass-redesign`
All committed + pushed to `origin/glass-redesign` (NOT merged to main, NOT live — review pending). This session, on top of the glass branch:
- **F&I Report** (`/finance-command`): replaced the deal list with the two finance managers (role "F&I" = Aaron Price, Bo Tshuma) **side by side**, full stats each (finance deals, back gross, products, PPU, back PVR vs goal).
- **Sales by Type** (`/command`): each type column now shows **Top Grossers** (sorted by total gross), not the first four deals.
- **🚀 Jimmy Intake — `Admin → Import` (`/import`), the big one.** Paste ANY deal log → `POST /api/ai/import` sends it to Claude (`claude-opus-4-8`) with the store roster + a forced `submit_deals` tool schema → returns structured deals → preview table + **totals checksum** → **Replace / Merge / Add**. Files: `app/import/page.tsx`, `app/api/ai/import/route.ts`, `lib/dealImport.ts` (`buildDeal`/`mergeDeals`/`applyImport`), `importDeals` on `DealProvider`, `Deal.dealNumber` field, nav entry in `AppShell` (owner/admin only). Captures per-deal **products** (VSC/GAP/Maint→same, **"NAS Combo"→utp (5-unit)**, "Other"→permaplate), VIN, doc fee, splits, "Do Not Qualify"→DNQ. `frontGross` EXCLUDES doc fee. **Merge** matches by `dealNumber` and enriches (products union; non-empty incoming wins) so a rich personal grid layers onto product-blind DMS deals without dupes. Replace/Merge back up current deals to the `deals_backup` store key first.
- **⚠️ Can't run the parse locally:** `ANTHROPIC_API_KEY` is only in the Vercel/prod env, not `.env.local`. The Import screen renders locally but the actual Jimmy parse must run on the **Vercel preview of `glass-redesign`** (or after merge).

### 🎯 GTM DIRECTION (Aaron, June 23, LOCKED) — universal intake is THE onboarding wedge / selling point
The Import screen is not just a utility — it's potentially the tip of the spear. Pitch: **"take the files/exports from whatever system you have now (Reynolds, CDK, Dealertrack, eLeads, vAuto…), drop them in, and MissionOS builds your whole store — then run it live day to day."** This is the concrete delivery of the locked "standalone-first / sits on top of the DMS / no rip-and-replace / Phase-1 file-drop onboarding" GTM. Why it wins: kills the #1 deal-killer (migration/integration pain) → minutes not weeks; it's a demo that closes itself (dealer pastes THEIR real log, sees their real numbers + Jimmy live); the AI is the unlock (handles arbitrary formats no rigid importer can). **Trust = the feature:** preview + checksum + backup before anything saves — a silent misread of their money is worse than no import; lead with "see exactly what it parsed before it saves." **Marketing guardrail (keep):** "bring your data, start in minutes" is honest; do NOT say "never touch your DMS again" until integrations are real.
- **Import roadmap toward the wedge (next, in order):** (1) **file upload** (`.xlsx`/`.csv`/`.pdf`, not just paste — dealers have files); (2) **format memory** — confirm a source's column mapping once, every future import from that system is instant + reliable; (3) recognized templates for the big DMS exports with AI fallback for the unknown; (4) "drop your last 90 days" onboarding flow; (5) graceful "couldn't read these N rows — fix them?" so a bad parse never breaks trust.

### 📥 June data load — ✅ DONE (verified live June 23; see status-reconcile section up top)
Both steps ran on prod via the Import screen: Replace (62 DMS deals) + Merge (Aaron's grid → products/VIN). Live confirms 62 June deals, all `imp-` sourced; Aaron 20/22 with products, Bo still product-blind (awaiting his grid). The "not yet executed" framing below is HISTORICAL — kept for the source/checksum detail only. Original plan was:
- **Reynolds F&I Manager Deal Log** = all **62** deals (Aaron 22, Bo/"Bongani" Tshuma 36, Daryl NeSmith 1, House Employee 3). Report totals checksum: 62 units · Back $107,674 · Front $1,633 · Total $109,307. Product-blind (lump back gross only).
- **Aaron's personal F&I grid** = his ~23 deals WITH per-deal products (✔/✘), VIN, doc fee, splits — richer than the DMS log. **Bo's products are unknown** (Aaron doesn't have them) → Bo shows product-blind until his grid comes.
- **Planned flow:** Import DMS log (Replace, 62) → Import Aaron's grid (**Merge by deal #** → products/VIN attach to his deals). Same Merge flow fills Bo later when his grid arrives. Product penetration off Aaron's grid: VSC 81.8%, GAP 36.4%, NAS Combo 50%, Maint 63.6%, Other 4.5%, PPU 4.36.

### 🔒 PRE-PRODUCTION AUDIT (June 23) — ran before going live; fixes shipped
Aaron called for a full audit before the production push (good call — it caught a data-loss landmine). Gates: tsc ✅, lint ✅, `next build` ✅ (note: a `next build` while `next dev` holds `.next` throws a false `PageNotFoundError` — kill dev / `rm -rf .next` for a true build). Fixes shipped (`565414a`):
- **🔴 Backup was fake in prod:** `deals_backup` wasn't in the store route `allowedKeys`, so the pre-Replace backup POST 404'd and was silently dropped while the UI claimed "backed up." Added the key; `saveStore` now returns ok; Import **aborts** Replace/Merge if the backup write doesn't confirm.
- **🔴 mergeDeals** didn't dedupe duplicate deal#s WITHIN one batch (both appended). Fixed via `appendedByNumber`.
- **🟡 input cap** added to `/api/ai/import` (300k chars).
- **⏳ DEFERRED — blocks the multi-tenant wedge:** `/api/ai/import` `rosterPrompt()` AND the F&I Report manager list (`app/finance-command/page.tsx`) both use the **hardcoded Kennesaw `team` seed**. Safe while Kennesaw is the only live tenant (signups gated), but a 2nd dealer's import would mis-map names to Kennesaw staff + leak those names. **MUST move to per-org roster (load the caller's `team` store key by orgId) before onboarding store #2.** Also pre-existing (not introduced tonight): the destructive `deals` write is reachable by Managers via the store API; owner-email super-admin is hardcoded.
- **💵 DOC FEE rule (Aaron, June 23):** doc fee stays OUT of the gross managers are measured/paid on — at Kennesaw sales managers don't get paid on the $899 doc fee, so total gross = front + back (matches the Reynolds "Total" column, ~$109k for June; NOT ~$165k). The app already does this (the aggregation runs on `activeStoreSettings` where docFee=0, so `totalGross`'s `+ docFeeIncome` term is 0). ⚠️ This is a **per-store rule, NOT nationwide** — when multi-tenant store settings are wired in, make "count doc fee in manager gross" an explicit per-org toggle (default OFF = Kennesaw rule), and make sure threading the org's real $899 into the math doesn't silently start including it in gross for stores that exclude it.
- **F&I Report per-manager back gross now INCLUDES cash deals** (`financeManagerStats`/`financeManagerBoard`, commit `f3e3a9e`) — matches the DMS FM log (Aaron 22/\$50,449/\$2,293 back PVR, Bo 36/\$56,215/\$1,562). Cash-deal back end is real F&I income. Penetration kept as a footer (X/Y finance). Store top-row "Finance Gross / Finance Deals" stay finance-only (penetration) by design.

## ▶️ RESUME TOMORROW — "pick up the glass redesign" / "comm 41"
Read this section first, then `CLAUDE.md` + `BRAND.md`. State as of end of June 22, 2026:

### 🎨 GLASS REDESIGN — IN PROGRESS on branch `glass-redesign` (NOT merged, NOT live)
- **Where the work is:** ALL of today's UI work is on branch **`glass-redesign`** (last commit `6db9599`, June 23). **`main` is unchanged at `6e8ba23` and is what's deployed — the live store has NOT seen any of this.** Do NOT merge to main until Aaron reviews the whole thing on the branch/preview and approves.
- **June 23 session — knocked out queue items 1 & 2:**
  - **✅ Rename pass (`55d455e`):** nav + screen titles de-militarized — Dashboard / Store Overview / Showroom / Deals / RDR / Team / Sales by Type; inner "___ Command" GM panels → New Vehicles/Used Vehicles/Wholesale/Service/Parts; Team page "Chain of Command"→"Org Chart", lanes → Management/Finance/Service/Parts; killed "Inventory battlefield"; theme "Platinum Command"→"Platinum"; private-chat "command channel"→"channel"; `lib/data.ts` military ranks (Lieutenant/Captain/Signal Officer/Command Staff/Fixed Ops Command…) replaced with each person's real `title`; "whole battlefield"→"whole store". (`rank` field is unused in UI — pure data hygiene.) Verified: tsc clean + dev-preview nav/titles, zero militarized strings.
  - **✅ "Finalized" deal-complete label (`fca2b02`):** `dealStageLabel()` in `lib/data.ts` — stored `stage` Delivered READS as "Finalized" (Desk/Contracted/Funded unchanged); STORED values untouched so `isSold`/units math is unaffected. Applied to stage badges (deal-center card, deal-scorecard) + stage `<select>`s (deal-center inline, deal-entry) with `value=` preserving the stored value. Verified in preview: 55 deals, Delivered→"Finalized", units count unchanged.
  - **✅ Office-clean "Ready to Post" gate (`6db9599`) — queue item 3, the bounce-back killer.** ADVISORY (warns, never blocks — Aaron's call June 23: "give warnings, but let the manager continue"). Label is **"Ready to Post"**, NOT "Send to Accounting" — because **MissionOS is NOT the DMS**; it's the clean gate that sits IN FRONT of the dealer's DMS/accounting (Reynolds at Kennesaw). The office keys a verified deal into the DMS once instead of getting it bounced back. `lib/data.ts` `officeChecks()`/`officeCheckSummary()` + `Deal.readyToPost`/`readyToPostAt`/`officeChecklist`. Auto checks (from data we already hold — no DMS integration): gross reconciles, lender/cash set, F&I mgr assigned, New-car invoice/holdback, products presented on financed deals, doc fee. Manual TAP checks (no underlying data): taxes & fees verified, lender stips met, every doc signed. `components/OfficeCheckCard.tsx` (reusable). Wired into **deal-center** (per-deal Ready badge on mobile + desktop → opens gate in a modal on any of the 55 stored deals; persists via `updateDeal`) and **finance-desk Recap** (live preview-deal gate in the finalize rail; persists onto the deal at Finalize). Verified in preview on real deals (warnings correctly fire on missing-invoice + zero-product Bo Tshuma deals). ⚠️ DMS write-back is Phase 3 (Reynolds last) — until then "Ready to Post" is a readiness/hand-off flag, it does NOT write to the DMS.
  - **⏳ NEXT = queue items 4–6:** (4) retire the floating Jimmy bubble (Jimmy in WINDOWS only); (5) Recap/back-screen fields (buy rate + auto-reserve from spread, bank/acq fee, We-Owe/WIO, RO); (6) monthly setup page (PDF → Jimmy extract → confirm → live). See list below.
  - **🔑 F&I FLOW — Aaron's authoritative description (June 23), drives items 5 + the F&I build. There are TWO F&I screens, not one:**
    1. Desk pushes the deal to F&I (CRM/Showroom → Desking → Push to F&I; already exists). Salesman physically brings a folder with all paperwork to F&I.
    2. **Finance deal screen** (the F&I working screen — TODAY this is `/finance-desk`, currently mislabeled "Recap"): FM sees what was pushed, confirms everything matches, then **sells product from a MENU** (← NEW: an F&I product menu to offer/sell out of; best-practice "100/100/100"), **structures the products into the deal**, and **finalizes**. Prints **Bill of Sale** (+ contract only for special cases like an LGE deal — most are e-contracted, rarely printed). ⚠️ At Kennesaw the **Bill of Sale prints out of Reynolds**, so BoS/contract printing is NOT a priority for us — only that the deal flows CRM → finance, FM does the menu, puts products in, finalizes.
    3. **Recap screen** (the BACK-END true-up — done AFTER the deal is complete, customer has left; currently does NOT exist as a separate screen — must be built, = queue item 5): FM plugs in the cost side — product **cost** vs sold, **buy rate** vs sell → **dealer reserve** from the spread, **bank/acquisition fees**/charges, **We-Owe/WIO**, **RO**. The recap screen **talks back to / syncs with the finance deal screen** so everything updates. Then **print the recap**, hit **Finalize Deal**, and hand to the **business office** (accounting). The office-clean **"Ready to Post" gate** is the final step here.
    - **Implication:** the current `/finance-desk` ("Recap") is really the **Finance deal screen** (point-of-sale finalize). The real **Recap** (back-end) is a NEW screen downstream of it. Nav today reads Showroom → Desking → Deals → RDR → Recap (Recap last). Likely need a distinct "Finance" (deal) screen + "Recap" (back-end) — confirm screen/nav split with Aaron before building. `/finance-command` ("Finance", in Performance) is the separate reporting screen.
- **The vision (Aaron's words):** sleek/modern, **"living, alive, breathing"** glass — "sheets of glass upon sheets of glass," **Minority Report** feel. Bar: **"easy enough a fifth-grader could use it, sleek enough for Steve Jobs/Elon Musk's boardroom."** Every number/name is a doorway (clean surface, full truth one tap away, permission-scoped). NOT military (he disliked "Command"/ranks). Breathing, NOT floating (drifting blocks = motion sickness — avoid).
- **DONE on the branch (commits):**
  - **Glass design system** (`app/globals.css`): `data-theme="glass"` (default on branch) — deep cyan atmosphere, lighter blue. Classes: `.glass-panel`/`.glass-card` (see-through: low tint, light frost, bright top edge, depth shadow, ::after specular that follows `--mx/--my`), `.glass-tactile` (grab cursor), `.glass-float`/`.glass-live` (breathe via EDGE-LIGHT only, no positional drift), `.glass-sweep` (glare rides ABOVE content via z-index+screen blend), `.glass-num`/`.glass-num-xl` (glowing numbers, hero glow breathes), `.glass-orb` + orbDrift, `.live-dot`, `.glass-scan`, `.think-dot`. `components/Tilt.tsx` = pointer-driven 3D tilt + specular-follows-finger (reduced-motion safe).
  - **Mission Control** (`app/page.tsx`) = the reference screen: floating-free glass, count-up numbers, growing bars, pulsing LIVE, Jimmy living WINDOW (computed "most behind rep" read + nudge), and **drill-downs** (`DrillOverlay`): tap Units→New/Used/Wholesale + every deal; Total/Front/Back gross→by rep / by F&I with Jimmy's read; tap a name→that rep's live action report (units/pace, open ups, Jimmy coaching, one-tap nudge).
  - **App-wide alive:** global living atmosphere (orbs) in `AppShell`; upgraded SHARED `MetricCard` (now renders through `Tilt` — tilts, light follows finger, sweep, breathes) + `.glass-card` material → all 23 screens came alive at once. Lighter-blue bg.
  - **Terminology:** "Classified"→**Finance / Cash / DNQ** (display only via `financeStatusLabel()`; STORED `financeStatus` values unchanged; Jimmy speaks it). F&I Desk screen renamed to **"Recap"** (nav + header; URL stays `/finance-desk`).
  - **Road-to-the-sale checklist** (`components/DealProgress.tsx`, `CrmLead.progress`): 8 ordered check-off bubbles on every Showroom lead card — Needs Analysis → Demo → Test Drive → Early TO → Numbers → Pull Credit → Final TO → Push to Finance (X/8, green when done, persists).
  - **"Owner"→"Salesperson"** everywhere; added **Sales Manager notes** (`managerNotes`) + the existing notes is now the **salesperson follow-up notes**; both show on the card and feed Jimmy's lead context for follow-up.
- **APPROVED but NOT yet built (do these next, in order):** *(items 1 & 2 DONE June 23 — see session note above; remaining: 3–6)*
  1. ✅ **DONE (`55d455e`).** **Rename pass** (approved): nav + screen titles → Dashboard (was Mission Control), Store Overview (GM Command), Showroom (CRM Desk), Deals (Deal Center), RDR (RDR Center), Team (Team Command), Sales by Type (New/Used/Wholesale); de-militarize all "___ Command" inner labels (New Vehicles, Service, Parts, Management…); kill kickers like "Inventory battlefield"; and **replace military ranks** in `lib/data.ts` (Lieutenant/Captain/Signal Officer/Command Staff…) with people's **real job titles**. (Keep: Desking, Finance, Recap, Recognition, Messages, Goals, My Scorecard, Admin, Store Settings, MissionOS Core, Commissioned 41 HQ.)
  2. ✅ **DONE (`fca2b02`).** **"Finalized" deal-complete status** (approved) — separate from Finance/Cash/DNQ; label a completed/delivered deal "Finalized" (display via a `dealStageLabel()`, keep stored `stage` values Desk/Contracted/Funded/Delivered intact). Pairs with the office-clean gate.
  3. ✅ **DONE (`6db9599`) — built as "Ready to Post," ADVISORY (warns, never blocks); MissionOS is the clean gate in front of the DMS, not the DMS.** **Office-clean "deal-ready" gate** + the F&I **"Send to Accounting"** bubble — THE bounce-back killer; Jimmy runs the office's checklist (gross reconciles, tax/fees/holdback right, products match menu, lender stips met, every doc signed) before a deal can post. Buildable NOW on existing data (no DMS integration needed). Already mocked.
  4. **Retire the floating Jimmy bubble** (approved) — Jimmy lives in WINDOWS only (`JimmyAvatar` orb still renders; replace with the window pattern).
  5. **Recap/back-screen fields** (`/finance-desk`): add **buy rate** (only sell rate exposed today) → **auto-compute reserve from the buy/sell spread** (manual override), **bank/acquisition fee**, **We-Owe (WIO)**, **RO**. (Note: `CrmLead` already has `buyRate`/`sellRate`.)
  6. **Monthly setup page** (Jimmy's fuel): drop in a PDF (rebates / lender rate sheets / lease residuals) → Jimmy extracts → "here's what I pulled, confirm" → goes live. Then wire per-tenant Jimmy fee schedule from `storeSettings`.
- **Key decisions (Aaron):** "Powered by Claude" advertising is OK with guardrails (factual, no implied endorsement/partnership, disclose AI use, confirm wording w/ Anthropic + attorney) — but LEAD with the Jimmy brand, Claude in the fine print. Jimmy = ambient guardian: silent unless he finds something, then ONE pane surfaces; speaks car lingo, never "classified."
- **Flow/efficiency (researched + confirmed):** biggest time leaks = (1) desk waiting on numbers, (2) F&I menu + chasing rates, (3) funding paperwork at delivery, (4) **office bouncing deals back — his #1 daily time-sink.** Best-practice backs us: single source of truth, track "contracts-in-transit," menu 100/100/100, start the deal online (~45 min saved). The office-clean gate is the proven fix.
- **🔑 GTM / SCALING DECISION (LOCKED June 22):** scale to retail **standalone-first — integrations are NOT required to sell or scale.** Position MissionOS as **"the AI brain + accountability layer that sits ON TOP of the dealer's existing DMS — no rip-and-replace,"** NOT a system that replaces everything. The differentiated, integration-free value (Jimmy, the office-clean gate, accountability/leaderboards/scorecards, desk speed) is what we sell now; it onboards a new store immediately on manual + file-drop data. **Handle the double-entry tradeoff** with file/export bridges (read side) + one clean hand-off to the DMS (work the deal in MissionOS → enter the final clean numbers into the DMS once, right the first time). **Phasing:** Phase 1 = no integration (sell the brain); Phase 2 = friendly connections (RouteOne credit, vAuto inventory, website leads, file sync) to kill read-side re-keying; Phase 3 = deep DMS write-back (Reynolds last, hardest). **⚠️ MARKETING GUARDRAIL:** do NOT say "no more 50 screens / replaces everything" until the tentacles are real — that promise is integration-dependent. Lead with "makes your store faster, tighter, smarter."
- **Kennesaw stack (tentacles, for the "brain" plan):** Reynolds & Reynolds DMS (🔴 closed/hard), eLeads CRM (🟡), RouteOne credit (🟡), vAuto inventory (🟡), Mazda OEM portal (incentives via scan), KennesawMazda.com (🟢 feeds), 19 lenders (Wells Fargo, 5/3, Ally, BofA, CapitalOne, Chase, Exeter, Flagship, GLS, Grow, Huntington, LGE CU, Lendbuzz, Mazda Financial, PNC, Regional Acceptance, TD, Truist DFS, US Bank — tiered captive/prime/near-prime/subprime/niche in chat). Architecture = generic adapters; Kennesaw is the reference, not hardcoded.
- **PREVIEW/TEST notes:** dev server via preview tools (`.claude/launch.json` "dev", port 3000). Owner login for preview: `aaronprice0918@gmail.com` / `Kellcey4ever!` (hits PROD). Sidebar collapses at preview width → open hamburger (`header button`) or use eval to navigate; `location.reload()` keeps session, cross-route `href` nav drops it. ⚠️ When clicking elements via eval, scope selectors tightly — a loose bulk-click toggled `progress` on all 29 prod leads once; backed up to `/tmp/crmLeads_backup_*.json` and cleaned. Prod data is clean.

## 🚀 PROD CUTOVER COMPLETE — June 22, 2026 (multi-tenant is LIVE on main)
The `multitenant-rollout` branch is **merged to `main` and deployed** (main `3c69032`). What changed:
- **Prod DB migrated:** ran `0004_app_multitenant.sql` on prod (`wwwibdjgabicenvpmivy`) via the Supabase SQL editor (Aaron). Verified after: `organizations` table exists, **Kennesaw = org `00000000-...-0001`**, all 8 app_store rows tagged to it, **55 deals / 29 crmLeads / team / goals intact**, `current_org_id()` live, RLS policies applied. Pre-cutover backup: `/tmp/prod_app_store_backup_2026-06-22T18-51-37.json`. **DO NOT re-run the migration thinking it's pending — it's done.**
- **Signups are CLOSED (demo-only until Stripe).** New flag **`NEXT_PUBLIC_SIGNUPS_OPEN`** (unset/`false` = closed). `/api/signup` returns 403; `/signup` page redirects to `/welcome#join`; all public CTAs say "Get a Demo" (not "Start Free Trial"). To open self-serve later: set `NEXT_PUBLIC_SIGNUPS_OPEN=true` in Vercel (only once billing gates access). Provisioning code (`lib/provision.ts`, `/api/signup`) is built and intact behind the flag.
- **Marketing honesty:** Dealer Group pricing tier tagged "Coming soon" (multi-store visibility / group reporting are NOT built — still aspirational).
- Verified live (curl): /api/signup→403, welcome shows Get a Demo + Coming soon, /login→200.

### ✅ PER-TENANT JIMMY DONE — June 22, 2026 (main `0668502`, deployed)
Items (2) and (4) from the open list are shipped:
- **Jimmy is now org-aware.** `app/api/ai/crm/route.ts` `resolveCaller()` reads the caller's `user_profiles` (org_id, role, employee_name) + that org's `storeSettings`, and `storeContext(settings)` injects THIS store's doc fee / holdback % / tax / product weights / targets / name into the prompt — no more hardcoded Kennesaw/GA. Snapshot + `repProfiles` (coaching memory) are scoped to the caller's org. Removed the DEFAULT_ORG pin.
- **Open to every rep, not owner-only.** Gated by org flag `storeSettings.aiAssistantEnabled` (omitted/true = on; false = off; owner always allowed). UI gates (`AppShell` JimmyAvatar, `crm-desk` CrmAiPanel) now use `useStoreSettings().settings.aiAssistantEnabled !== false` instead of `isOwner`. Admin on/off toggle added to `/store-settings`. Delivers the "coaches every rep" claim. When Stripe lands, drive this flag from subscription state.
- **PII guard (important):** Jimmy's snapshot is role-aware — Sales/BDC see the store-wide leaderboards but NOT other reps' customer names (shown as `[hidden]`), and Sales sees only its own CRM leads. Mirrors the store API's `filterForUser` redaction. Don't regress this when editing the snapshot.
- **Answer style (Aaron):** "tell the time, not how to build the clock" — Jimmy leads with the direct answer/number and only gives a plan/steps when asked. Baked into `BASE_PROMPT`.
- **Shared `mergeStoreSettings()` in `lib/data.ts`** — client provider + server both use it (was duplicated in StoreSettingsProvider).
- Verified: tsc clean, full build (35 routes), deployed; unauth 403 message flipped live. NOT yet exercised logged-in as a rep — Aaron to confirm Jimmy shows for a Sales login and answers with store numbers, concisely.
- **STILL OPEN:** (1) **Stripe billing/paywall** — needs Aaron's keys (he's researching Stripe vs alternatives first); then flip `NEXT_PUBLIC_SIGNUPS_OPEN=true` + drive `aiAssistantEnabled` from subscription. (3) **Multi-store/group reporting (Dealer Group tier)** — DEFERRED on purpose: best built against a real second store, and it means relaxing the per-org RLS we just shipped (a group-admin reading multiple orgs) — don't bolt speculative cross-tenant access onto freshly-isolated prod with zero group customers. Marketing already honest ("Coming soon"). Build the group model + rollup on STAGING when a group is actually signing.

### ✅ FULL AUDIT + FIXES — June 22, 2026 (4 parallel audits, then fixes shipped)
Ran a full audit (tenant isolation, security, marketing-vs-reality, admin data-entry). Verdicts: isolation SAFE; marketing claims all DELIVER now; found 1 critical security hole + 1 critical readings gap, both fixed.
- **🔴→✅ Jimmy client-lead PII leak (commit `02e88bd`):** the per-lead actions (health-check/draft/next-action/first chat) rendered the lead from the REQUEST BODY with no ownership check — a Sales rep could POST another rep's lead. Fixed: `resolveLead()` loads the lead from stored crmLeads by id and a Sales user can only act on their OWN leads (mirrors store API). When editing Jimmy's snapshot, keep this — don't trust client lead objects.
- **🔴→✅ Sales pay ignored the pay plan (commit `b746693`):** Sales pay was hardcoded to Kennesaw's plan; admin edits didn't affect it. Fixed: new structured `SalesPlan` type + `defaultSalesPlan` (= Kennesaw's exact numbers) in `PayPlanProvider.tsx`; `my-scorecard` Sales pay reads it (`calculateSalesPay`); admin Pay Plan editor has a full Sales-plan editor. VERIFIED new engine === old on all 55 prod deals + bonus grids (0 mismatches) via `/tmp/verify_pay.mjs`. Manager/F&I pay was already plan-driven.
- **✅ Admin create-login + role mgmt (commit `80b3c9d`):** `/api/users` was owner-only AND un-scoped; now org-scoped + admin-capable, with a CREATE action (Supabase login + org profile). Admin page "Staff Access & Logins" panel opens to any store Admin with an "Add an Employee Login" form. Closes the gap where new staff could only be added via the Supabase dashboard (which mattered since self-serve signup is closed).
- **✅ Hardening (in `02e88bd`):** waitlist GET now org-scoped; `provisionOrg` seeds `storeSettings.storeName` so a new tenant isn't labeled "Kennesaw Mazda".
- **Jimmy answer-style (Aaron, in `0668502` prompt):** "tell the time, not how to build the clock" — direct answers, plans only when asked.
- **Audit LOW/MED items NOT yet done:** rotate the old anon/publishable key found in git history (`lib/supabaseBrowser.ts@b7af793`; RLS-gated, low impact — needs Supabase dashboard); rotate the Vercel token (not in repo, advisable); no per-rep PVR/PPU targets (only store-wide + per-rep units); `CLAUDE.md` is stale (still says multi-tenant "branch only").

## ▶️ RESUME TRIGGER — "let's get started on Commissioned 41" / "comm 41"
When Aaron says this, THIS FILE is the full memory — read it top to bottom, plus `CLAUDE.md` and `BRAND.md`. Quick state as of June 21, 2026:
- **Product:** MissionOS = the Dealership Operating System (the live flagship). **Company:** Commissioned 41 (the core; tagline "Know Your Mission. Execute With Purpose."). MissionOS tagline: "Live Data. Real Accountability." Name meaning + manifesto in `BRAND.md`.
- **Domains:** `commissioned41.com` = marketing (→/welcome), `missionos.commissioned41.com` = the app, `hq.commissioned41.com` = Aaron's owner/company portal. Repo: `/Users/pricefamily/commissioned41-os`. GitHub: aaronprice0918-collab/commissioned41-os.
- **AI surfaces:** Jimmy (dealership coach, in-app), ILA (Commissioned 41 company AI, owner-only, in HQ), MissionOS Core (Aaron's personal exec OS, owner-only). All Opus 4.8. Jimmy is a translucent breathing Tiffany-blue orb.
- **Shipped to prod (main):** marketing site, legal (/terms,/privacy), CEO card, branding, CI build-guard, Jimmy voice + playbook + per-rep drills, messaging (direct+group), MissionOS Core, Commissioned 41 HQ pipeline + ILA.
- **BIG WORK ON BRANCH `multitenant-rollout` (NOT in prod yet):** multi-tenancy (org-scoped app_store + RLS, proven on staging) + self-serve idiot-proof signup wizard (`/signup`) + **per-org Store Settings (NEW, built this session — see section below).** **NEXT:** (1) Stripe billing — needs Aaron's keys; (2) prod cutover (run migration 0004 on prod + merge branch — careful, off-hours, backup first); (3) per-tenant Jimmy (crm route still pinned to DEFAULT_ORG + GA fees hardcoded in its prompt — wire it to read the caller-org's storeSettings). See "Build it as a legit company roadmap" + "SECURITY & MULTI-TENANCY" + "PER-ORG STORE SETTINGS" sections below.
- **Aaron's working style:** wants it built like a legit company, "do it right the first time"; keep building autonomously; only bring him decisions he'd genuinely weigh in on, or things only he can do (keys/accounts).
- ⚠️ Global cross-session memory (`~/.claude/projects/.../memory/`) can't be written — `.claude/projects` is root-owned. Fix: `sudo chown -R pricefamily:staff ~/.claude`. Until then THIS file is the memory of record.

## Mission / Stakes
**Kennesaw Mazda is the FIRST in-store trial run.** It must be flawless — the
goal is they say YES and recommend Commissioned 41 to every other store. This is
the proof-of-concept that sells the product. Every decision is judged against:
does this make the trial bulletproof and the "yes" more likely?
- Wins a store: accurate numbers they trust, faster than their current process,
  premium feel, doesn't break, staff actually adopt it (mobile-friendly).
- Non-negotiables for trial: correct math everywhere (done), real store data
  seeded, auth/roles + privacy working, the daily loop (deal → board → scorecard)
  bulletproof, clean deploys.

## Who You Are
- **Name:** Aaron Price
- **Role:** Admin / F&I Manager, Kennesaw Mazda
- **Email:** aaronprice0918@gmail.com

## The App
- **Name:** Mission (UI/brand as of June 20, 2026) — repo/project slug stays `commissioned41-os`
- **URL:** https://commissioned41.com (canonical; `www` + both `*.vercel.app` 308-redirect here)
- **GitHub:** https://github.com/aaronprice0918-collab/commissioned41-os
- **Stack:** Next.js PWA deployed on Vercel
- **Vercel team:** team_tXukadBSYoTjMq7VRxpNk9UE
- **Vercel project:** prj_jIBTbvOOZypHuQCatSbSBFQznniW
- **Backend:** Supabase — `app_store` table, JSONB key-value storage (no migrations needed for new fields)
- **Workspace folder:** /Users/pricefamily/commissioned41-os

## Brand
- Logo: black background, silver/chrome "C41" mark, "COMMISSIONED 41 / LIVE DATA. REAL ACCOUNTABILITY."
- Brand file: `public/brand/commissioned41-brand.jpg`
- PWA manifest: `public/manifest.webmanifest` — icon pointing to `commissioned41-brand.jpg?v=5`

## Code Changes Made This Session (June 2026)
1. **`lib/data.ts`** — Added structured trade fields to Deal type:
   - `tradeVin?: string`
   - `tradeYear?: string`
   - `tradeMake?: string`
   - `tradeModel?: string`
   - `tradeAcv?: number`
   - `tradePayoff?: number`

2. **`app/deal-entry/page.tsx`** — Replaced single trade textarea with structured grid UI:
   - Trade VIN (full-width)
   - Year + Make (side by side)
   - Model (full-width)
   - ACV + Payoff (side by side)
   - Trade Notes textarea below

3. **`public/manifest.webmanifest`** — Updated PWA icon to `commissioned41-brand.jpg?v=5`

## Code Changes (June 19, 2026)

### Edit Deal Flow (COMMITTED)
- **`app/deal-entry/page.tsx`** — Full rewrite with edit mode:
  - Wrapped in `<Suspense>` boundary (required by `useSearchParams`)
  - Detects `?id=` query param → loads existing deal into form
  - `editLoaded` ref prevents team-defaults `useEffect` from overriding loaded data
  - Added `salesperson2` field (dropdown, "— Solo Deal —" for no split)
  - Submit calls `updateDeal(editId, preview)` when editing, `addDeal(preview)` when new
  - UI shows "Edit Mode" header, "Update Deal" button, customer name in title when editing
  - Product label fixed inline: `key === "maintenance" ? "MAINT" : key.toUpperCase()`
- **`app/deal-center/page.tsx`** — Added Edit button in Actions column:
  - Gold pencil button linking to `/deal-entry?id=${deal.id}`
  - Delete button preserved below edit button
  - Imported `Link` from next/link, `Pencil` from lucide-react

### Gotham Theme (COMMITTED)
- **`app/globals.css`** — Added `[data-theme="gotham"]`: pitch-black with electric yellow accent (#FFD C00)
- **`components/ThemeProvider.tsx`** — Added `{ key: "gotham", label: "Gotham" }` to themes array
- Theme is selectable in sidebar theme picker

### Navigation Architecture (COMMITTED — June 18-19)
- **`components/AppShell.tsx`** — Left sidebar (244px), always visible on desktop, mobile overlay
  - Background: `#0d0e12/85` (near-black, Apple-style)
  - Active nav: gold background, navy text
  - Bottom: theme picker
  - Mobile: hamburger → full overlay with X close button
  - Hooks all called before early return (card pages skip shell)

### MAINTENANCE Label Fix (COMMITTED)
- **`lib/data.ts`** — `productLabels.maintenance` changed from `"Maintenance"` to `"Maint"` — prevents text overflow in product buttons

## Formula Audit + Consistency Engine (June 19, 2026)

Every screen used to compute salesperson numbers its own way, so the same rep
showed different units/gross on Mission Control vs My Scorecard vs Employee
Profile vs Goals. Centralized into one engine in `lib/data.ts`:

- **Canonical rules (confirmed by Aaron):**
  - A deal counts when **marked sold** (stage Delivered or Funded). F&I
    classification (Classified / Not Classified / DNQ) never removes a unit.
  - Salesperson units = **retail (New+Used) sold**. Wholesale is its own lane.
  - **Split deals = half unit / half gross each** (halves sum to one store unit).
  - PVR goals: **Front $1,200 / Back $1,800** (Store $3,000) — now in
    `goals.frontEnd` / `goals.backEnd`; Mission Control reads them.
- **Single source of truth (lib/data.ts):** `isSold`, `isRetail`,
  `isCountableRetail`, `isCountableFinance`, `salespersonShare`,
  `salespersonStats`, `financeStats`, `salesLeaderboard`, `financeLeaderboard`,
  `unitsLabel`. Every screen calls these — do NOT re-roll per-screen aggregation.
- **Screens updated:** Mission Control, My Scorecard, Employee Profile, Goals,
  Recognition Feed, GM Command, Finance Command, New/Used/Wholesale Command.
- **Bug fixes:** deal IDs now UUID-based via `makeDealId()` (old `deals.length+1`
  collided after deletes → edits/deletes hit wrong deal); wholesale deals save
  `docFee:0` (was phantom $899); Recognition/GM leaders now merge name variants.
- Build verified clean (25 routes); cross-screen math verified by harness.
- **Trade income fixed (real fields):** `Deal` now has `tradeYear/Make/Model/Vin`
  + numeric `tradeAcv` / `tradePayoff`. `tradeEquity(deal) = ACV − payoff`,
  `hasTradeData(deal)`. Deal Entry has a structured trade grid (Year/Make/Model,
  VIN, ACV, Payoff) with a live equity readout; old free-text Trade Info kept as
  notes. GM "Trade Equity Signal" sums `tradeEquity` (falls back to the legacy
  notes regex only for old deals without structured data).

## Official GA Packet Fill (June 20, 2026)

`public/forms/georgia-deal-forms.pdf` is a REAL fillable AcroForm (459 fields,
10 pages: MV-1, title reassignment, MV-7D TAVT, trade, T-8 POA x2, odometer
disclosure x2, agreement to provide insurance, ack). We fill it for real via
`lib/gaPacket.ts` `fillGaPacket(pdfBytes, lead)` using `pdf-lib` (client-side).
Button "Fill Official GA Packet" on CRM Desk → fetches the PDF, fills, opens.

- Helpers: `setText`/`setTextAll` (handles `_page2.._page10` variants),
  `check`, `setBoxes` (one char per box: VIN1..17, Odom1..6, etc.).
- Currently mapped: vehicle (parsed Year/Make/Model), VIN (+char boxes p2/4/5),
  odometer (+boxes), deal dates, New/Used, customer name/address/phone/email,
  dealer name + county (Cobb), TAVT (base/rebate/trade/taxable/rate 7%/amount),
  trade Y/M/M/value/miles.
- Capture fields added to CrmLead + desk form (June 20): vin, driversLicense,
  dlState, dob, county, coBuyer{Name,Dob,Dl,Phone}, vehicle{Color,Body,
  Cylinders,Fuel}, currentTitle, lien{Name,Address}, insurance{Company,Policy,
  AgentName,AgentPhone,AgentAddress,EffectiveFrom,EffectiveTo}, coverage{
  Collision,Comprehensive,FireTheft} + deductibles. All 38 official-form target
  fields verified present on the PDF. UI: Vehicle section (color/body/cyl/fuel/
  title), Contact section (DL/DOB/county), new Co-Buyer + Lienholder&Insurance
  sections. `CoverageToggle` component for coverage+deductible.
- Remaining blanks (by design): notary block (notary completes), GVW, leasing-
  company fields, char-box currency spreads.
- Char-box currency spreads (GASalesTax1..9, Dealer1..12, LienID1..24,
  Title1..20) intentionally NOT auto-filled — per-box digit alignment unverified.
- Dealer/customer forms (Worksheet, We Owe, Privacy, Credit App) are OUR docs
  (no official PDF) — still the HTML builder in `lib/desk.ts`; not yet polished.

## Design Direction (Aaron, ongoing)
Aaron wants Commissioned 41 to feel like a **sleek, Apple-grade dealer operating
system** — not a form dump. Favor: segmented controls / tabs over stacked boxes,
smooth micro-animations (sliding highlights, content cross-fades via the
`.tab-fade` keyframe in globals.css), live previews, and convenience actions
("Same address as Customer 1"). "Animate where you can — make it a live part of
our team." Respect `prefers-reduced-motion`. Apply this lens to every new UI.

First example shipped: CRM Desk **Customers** box — one card with a Customer 1 /
Customer 2 Apple-style segmented control (gold sliding highlight), co-buyer
mirrors the full buyer field set, name previews, and a ✓ on the tab when a
co-buyer is present.

## GitHub Editor Technique
To edit files on GitHub's web editor programmatically (CodeMirror):
```js
const view = document.querySelector('.cm-content').cmTile.view;
view.dispatch({ changes: { from: 0, to: view.state.doc.length, insert: newContent } });
```

## GitHub Editor Technique
To edit files on GitHub's web editor programmatically (CodeMirror):
```js
const view = document.querySelector('.cm-content').cmTile.view;
view.dispatch({ changes: { from: 0, to: view.state.doc.length, insert: newContent } });
```

## Audit Data Snapshot — June 18, 2026
### Store Overview (Mission Control)
- Units: 39 / 130 (30%) | Pace: 63.4/mo | Behind: -36 | Need: 8.27/day
- Front Gross: -$3,606 | Back Gross: $71,113 | Total: $67,507
- Store PVR: $1,731 | Goal PVR: $3,000
- Total w/ Doc: $102,568 (PVR $2,630) | Doc Fees: $35,061

### Salesperson Summary
| Rep | Units | Goal | Pace |
|-----|-------|------|------|
| Shawn Smith | 7 | 18 | 39% |
| Joshua Aarons | 7 | 18 | 39% |
| Noel Bernard | 5 | 16 | 31% |
| Anthony Williams II | 4 | 18 | 22% |
| Daniel Maharaj | 3 | 12 | 25% |
| Rick Brown | 3 | 15 | 20% |
| Joseph Sotis | 3 | 13 | 23% |
| Watson Jones | 2 | 15 | 13% |
| Shaun Houston | 2 | 5 | 40% |
| Gregory Townsend | 2 | 13 | 15% |
| Maged Mroushdi | 1 | 5 | 20% |
| El | 0 | 5 | 0% |

### F&I
- Aaron Price: 19 copies / $45,225 / $2,380 PVR / 36 products / 1.89 PPU
- Bo Tshuma: 16 copies / $23,739 / $1,484 PVR / 0 products / 0.00 PPU ⚠️

### Open Issues
1. Invoice amounts not entered on new deals — 6% holdback = $0
2. Bo Tshuma has ZERO products on 16 copies — needs immediate training
3. Store front gross is negative — 7 of 12 reps upside down
4. Joseph Sotis — negative total gross (-$2,719)
5. Trade income = $0 — numeric values not entered in trade notes
6. El — 0 deliveries with 5-unit goal
7. Watson Jones — 2 of 15 (13% pace)

## Rebrand to "Mission" + Custom Domain + SaaS Pivot (June 20, 2026)

### Brand: now "Mission" with the REAL chrome M
- User-facing name is **Mission** (manifest, page titles, AppShell sidebar). "Commissioned 41 OS" / "C41" retired from the UI; repo slug stays `commissioned41-os`.
- **Real logo wired in** (chrome diamond-cut "M" + "MISSION" wordmark). Aaron supplied the artwork as a phone screenshot; background keyed to transparent with Pillow (no PIL/ImageMagick on machine — `pip install --user Pillow`).
  - `public/brand/mission-mark.png` — M only (transparent)
  - `public/brand/mission-logo.png` — full M + MISSION lockup (transparent)
  - `components/BrandMarks.tsx` — `MissionMark` now renders the real PNG via next/image (replaced a hand-coded SVG *approximation* that looked wrong). Added `MissionLockup`.
- **App / favicon / PWA icons** regenerated from the M on black (Pillow): `public/favicon.ico`, `mission-icon-192.png`, `mission-icon-512.png`, `apple-touch-icon.png`. `app/layout.tsx` `icons` + `public/manifest.webmanifest` point at them; manifest name = "Mission"; service-worker cache bumped to `mission-os-v6` (purges old icons on next load).
- **Social share:** `public/og.png` (1200×630 chrome lockup on black) + Open Graph/Twitter metadata + `metadataBase: https://commissioned41.com` in `app/layout.tsx`.

### Domain: commissioned41.com is the canonical home
- Bought **through Vercel** (Aaron, Pro plan). Live, valid SSL.
- Canonical 308 redirects set via Vercel API (using `VERCEL_TOKEN`): `www.commissioned41.com`, `commissioned41-os.vercel.app`, `mission-dealer.vercel.app` → `https://commissioned41.com`.
- ⚠️ A full-account **`VERCEL_TOKEN` is in `.env.local`** (gitignored, NOT referenced by app code — leftover). Recommend deleting it from there; account-wide tokens shouldn't live in app env. (Paste back temporarily if Vercel API ops are needed.)
- Secrets posture verified clean: only `.env.example` tracked; `.env*.local` gitignored; no secrets in git history; service-role key is server-only.

### DIRECTION DECISION: Mission is going SaaS (multi-dealership)
Aaron confirmed (June 20): **sell Mission to other dealerships**, not just internal. Kennesaw Mazda = first tenant / proof-of-concept. This supersedes the "internal trial only" framing at the top of this file — the trial still matters, but architect for multi-tenant.

### Architecture findings (current = SINGLE-tenant)
- All app data lives in ONE key-value table **`app_store`** (`key text pk, value jsonb`), read/written through a single server choke point **`app/api/store/[key]/route.ts`** using the service-role key (RLS bypassed; role filtering done in app code). `user_profiles` holds users (id, email, role, employee_name).
- "Kennesaw Mazda", the roster, and goals are **hardcoded in `lib/data.ts`** + seeded JSON in `data/`.
- **No organization/tenant concept yet.** Big advantage for retrofitting: one data choke point to make tenant-aware, not scattered queries.

### Multi-tenant build — Phase 1 STARTED on branch `multi-tenant`
- Drafted **`supabase/migrations/0002_multi_tenant.sql`** (committed to branch, **NOT applied to prod**): `organizations` table, `org_id` on `user_profiles` + `app_store`, `current_org_id()` helper (security definer), RLS policies for tenant isolation. Backfill + NOT-NULL/composite-PK enforcement are **commented out** pending staging validation.
- Remaining Phase 1: make `/api/store/[key]` org-aware (resolve caller's org_id, scope every read/write); onboarding (sign-up creates org + first admin, seeds defaults); move roster/store from `lib/data.ts` to per-org data; **isolation test with 2 orgs** (A must not see B); then **backfill Kennesaw Mazda into Org #1 on prod (with backup)**.
- Phases 2–4: onboarding + per-tenant branding (generalize the per-dealer logo concept — see `KennesawMazdaMark`) → Stripe billing + 14-day trial, access gated by subscription → legal (ToS + Privacy, handling employee PII/comp data) + security review → sell.
- **Rule:** tenant isolation must be airtight and proven on a non-prod DB BEFORE prod is migrated. Never improvise schema changes on the live DB (real dealership comp data in active use).

### Environment gaps blocking safe progress
- **Only ONE Supabase project exists** (prod, ref `wwwibdjgabicenvpmivy`). **No Supabase CLI, no Docker** → can't run local Supabase yet. Need a safe non-prod DB before testing isolation. Options: (a) install Docker + Supabase CLI for a local stack; (b) create a free **staging Supabase project** (needs Aaron's account login or a Supabase access token — only the project service key is available, which can't list/create projects); (c) Supabase branching (needs Pro plan + git integration).
- **Claude memory system is broken on this machine:** `/Users/pricefamily/.claude/projects` is owned by **root**, so the agent (user `pricefamily`) can't create its memory dir (`EACCES`). Fix: `sudo chown -R pricefamily:staff /Users/pricefamily/.claude`. Until fixed, project memory lives in THIS file.

### Business setup checklist (LLC filed June 2026)
EIN (IRS, free, ~10 min) → business bank account (separate, preserves LLC shield) → business email on `commissioned41.com` (Google Workspace ~$6/mo, or free Cloudflare Email Routing forwarding to Gmail — DNS setup help available).

## Late June 20 night — naming, domain, login, REAL CRM data

### Naming locked: company = Commissioned 41, product = MissionOS
- The dealership OS product is **MissionOS** (was just "Mission"). Company/LLC/domain stays **Commissioned 41**. Chrome **M** is the mark; "OS" is a light suffix. Renamed across metadata, manifest, sidebar, header, login wordmark — all on `main`, live.
- **App now at its own subdomain: https://missionos.commissioned41.com** (free, SSL, added via Vercel API). `commissioned41.com` still serves the app too for now; eventual plan: root = company marketing, subdomain = the app.

### Login + access
- **Owner login: `aaronprice0918@gmail.com` (Admin/owner), password `Kellcey4ever!`** (Aaron set it). Aaron also has `aprice@kennesawmazda.com` (F&I employee acct).
- No in-app password change yet → reset via Supabase Auth admin API: `PUT {url}/auth/v1/admin/users/{id}` with service-role key, body `{"password":"...","email_confirm":true}`.
- Login redesigned to chrome standard in `components/LoginPanel.tsx`.

### Aaron's CEO business card (live)
- **https://commissioned41.com/ceo.html** (`public/ceo.html`) — black/chrome card: Aaron Price, CEO & President, Commissioned 41; tap-to-call/email, Save-to-Contacts vCard, QR. Email still gmail — swap to `aaron@commissioned41.com` once set up.

### UX standard (Aaron, firm): if it LOOKS interactive, it must BE interactive
- **Going LIVE MONDAY — never a demo, never placeholder data. Real data only.**
- Shared `MetricCard` now takes `onClick` → modal list. **CRM Desk drill-downs shipped:** Active Leads / Showroom / Appointments / At Desk are clickable → list of matching customers; each row has one-tap **Mark Shown** / **At Desk** (Showroom = live "mark-on-arrival": counts status `Shown`/`Desking`). Roll this pattern to every dashboard (Mission Control next).

### REAL CRM data loaded — Saturday 6/20/2026 desk log
- Replaced ALL prior sample leads with the **29 real showroom visits** from the desk log. Live in prod `app_store.crmLeads`. Statuses: 12 Won, 13 Working, 1 Appointment Set, 3 New Lead. Backup of prior data: `/tmp/crmLeads_backup_*.json`.
- **12 units SOLD today (marked Won):** Daniel Smith (Noel Bernard), Mary Hall (Joshua Aarons), Colin Millard (Daniel Maharaj), Stephanie Price (Anthony Williams II), Panfilo Rojo (A. Williams II), James Watson (Rick Brown), Michael Massey Sr (Shawn Smith), Anthony Weaver (Maged Mroushdi), Judith Massey (A. Williams II), Allen Kuykendall (Gregory Townsend), Sarah Sendy (Joseph Sotis), Marcus Coyle (Daniel Maharaj).
- **TODO (Aaron, tomorrow):** gross/financial numbers for these 12 are NOT entered yet — Aaron will provide. They're Won in CRM but NOT in the `deals` dataset, so Mission Control units/gross don't reflect them. When numbers arrive: create matching delivered `deals`. Store that day: 12 sold, 29 showroom, front GP ≈ −$4,056.

### How to write prod data (single-tenant, current)
- `app_store` key-value, PK = `key`. Bulk write via service-role key + PostgREST:
  `POST {url}/rest/v1/app_store?on_conflict=key`, header `Prefer: resolution=merge-duplicates`, body `[{"key":"crmLeads","value":[...],"updated_at":"..."}]`.
- Safe bulk-import: fetch an existing record as a TEMPLATE (exact field shape), deep-copy per row, override known fields, zero financial fields. (One-off `/tmp/import_leads.py` used for this load.)

### OPEN request: get MissionOS into the App Store
- Aaron wants MissionOS in the **App Store**. Currently a PWA (Add to Home Screen). Options to discuss next session: (a) keep PWA + guide install; (b) Google Play via TWA/Bubblewrap (easy); (c) Apple App Store via wrapper (Capacitor/PWABuilder) + Apple Developer acct ($99/yr) + review. Not started.

## MissionOS Assistant — LIVE (June 20 night)
- **The in-app AI assistant is live in production**, powered by **Claude Opus 4.8** (`claude-opus-4-8`). This is the core of the "ultimate BDC manager in your pocket" vision.
- **Where:** `app/api/ai/crm/route.ts` (raw Anthropic HTTP, model const `ASSISTANT_MODEL`) + `components/CrmAiPanel.tsx` (Health / Draft / Chat / Next-Action tabs), rendered on CRM Desk expanded lead cards. Verified end-to-end on prod (real coaching on a real lead).
- **Entitlement (monetization):** assistant is an **owner-only premium add-on**. Server gate `assistantEnabled(req)` in the route returns 403 for non-owners; UI only renders `CrmAiPanel` when `useAuth().isOwner`. Designed to flip to a per-org paid flag (`organizations.ai_assistant_enabled`) when billing lands — Aaron sells the assistant as an upsell that covers each dealer's API cost + margin.
- **API key:** `ANTHROPIC_API_KEY` is set in **Vercel** (encrypted, all targets) — NOT in the repo, NOT in `.env.local`. It's Aaron's Anthropic account key (usage billed to him). Was pasted once in chat; can be rotated at console.anthropic.com if desired.
- **Cost model:** Opus 4.8 ≈ $5/$25 per 1M in/out tokens; each coaching call is cents. The add-on fee is what offsets it per dealer.
- **Next (the vision):** make it PROACTIVE — no-show reschedule nudges, hot-up alerts, daily "next best action" per rep, draft-and-send. Per role (sales/F&I/manager/BDC). Mobile-first.

### Jimmy's coaching memory — LIVE (June 21, commit 7145ca9)
- Jimmy (the GSM assistant) now keeps a **persistent per-rep profile** so he coaches like he knows the team, not like a calculator. Stored in `app_store.repProfiles` (object keyed by rep full name → `{strengths[], weaknesses[], patterns[], notes[], updatedAt}`).
- New **`remember_rep` tool** (`app/api/ai/crm/route.ts`): Jimmy saves observations mid-chat — on his own when he spots something in the data, or when asked. `saveRepObservation()` upserts + dedupes list entries.
- Snapshot (`loadStoreSnapshot`) now loads `repProfiles` and injects a "JIMMY'S COACHING MEMORY" block every turn. Chat path runs through new `callJimmyChat()` (4-iter tool loop) instead of plain `callClaude`.
- Build clean (25 routes), pushed to `main`. Verify on prod: chat Jimmy about a rep, confirm a profile lands in `app_store.repProfiles`.

### Jimmy's sales-trainer playbook — Phase 1 LIVE (June 21, commit 7cb0ac9)
- Aaron's ask: "Find as much material as you can from the best sales trainers/leaders in the world and give it to Jimmy so he can rebuttal it to our salespeople." Jimmy becomes a true sales coach, not just a numbers watchdog.
- **New `lib/salesPlaybook.ts`** exports `SALES_PLAYBOOK` — a distilled reference: trainer philosophies, objection-handling frameworks, named closes (verbatim), price/money word tracks, tonality, Cialdini persuasion levers, negotiation tactics, SPIN/Sandler/Challenger questioning, and a deal-stage cheat sheet. Imported into `app/api/ai/crm/route.ts` and appended to `SYSTEM_PROMPT` with instructions: hand reps the exact rebuttal word track, name the technique/trainer, and tailor to the real customer/vehicle/numbers.
- **Phase 1 (DONE, shipped):** general legends — Ziglar, Tom Hopkins, Brian Tracy, Jordan Belfort (Straight Line/looping/Three Tens), Gitomer, Bettger, Carnegie, Cialdini — PLUS modern negotiation/methodology — Chris Voss (Never Split the Difference), Jeb Blount (Objections/Sales EQ/INKED), Sandler, SPIN, Challenger Sale. Researched via two parallel web-research agents; build clean; pushed.
- **Phase 2 (DONE, shipped June 21, commit d9954ce):** added the automotive-specific clusters to `lib/salesPlaybook.ts`:
  - PART 12 Automotive trainers — Cardone, Andy Elliott, Joe Verde (CRIC/8-steps), Jim Ziegler, Tom Stuker, David Lewis, Mark Tewart, Mosley, Boudreaux: philosophies + verbatim rebuttals (just looking, think about it, talk to spouse, price/payment, trade value, be-back, shop around, sleep on it, check credit), car closes (puppy-dog, takeaway, Ben Franklin, silent, assumptive), full road to the sale.
  - PART 13 F&I — Reahard's welcome/sympathize/reframe/reclose, menu best practice + the F&I interview, VSC/GAP/maintenance/cash-customer/"just the lowest payment" rebuttals.
  - PART 14 BDC/phone/internet + follow-up — appointment-setting, "best price," is-it-available, no-show recovery, confirm cadence, Day 1/2/3/5/8/11/14 unsold cadence, 72-hr sold call, referral/review/equity-upgrade asks.
- Playbook now has 14 parts + cheat sheet. Sent every chat turn — fine for Opus 4.8 (cents/call) but it's now sizable; if cost/latency ever matters, consider gating the playbook to the Chat tab only (not Health/Draft/Next-Action calls).
- **Verify on prod:** ask Jimmy "customer says the payment's too high, what do I say?" — he should hand a specific word track and name the trainer/technique.
- **Possible next ideas (not started):** (a) let Jimmy cite which trainer a rebuttal came from when coaching; (b) per-rep drills — Jimmy assigns a word track to practice based on that rep's weakness in repProfiles; (c) gate playbook to chat-only to trim tokens.

## June 21 — Messaging center + Jimmy redesign & voice

### Jimmy is now a 3D glassmorphism orb (commit 865f346)
- Replaced the wheelchair-character SVG (`JimmyFigure`) with `JimmyOrb` in `components/JimmyAvatar.tsx` — a frosted-glass orb with an evolving neon conic-gradient core (CSS in `ORB_STYLES`). Used for both the floating FAB (lower-right) and the panel header. Core spins faster/brighter while **speaking**, turns red while **listening**; respects `prefers-reduced-motion`.
- Panel gained **context-aware quick-action chips** (`shortcutsFor(pathname)`) that change per screen (Mission Control / CRM Desk / Finance / GM / Deals / Goals / Team), shown before the chat gets going.
- Aaron's bar: must feel Apple-grade. He rejected the prior character look outright.

### Jimmy has a voice (commit a6c9fce)
- New `lib/voice.ts` `useVoice()` hook: speech-to-text in (Web Speech API `webkitSpeechRecognition`) + text-to-speech out (`speechSynthesis`, deeper voice). Degrades gracefully via `inputSupported`/`outputSupported`.
- Wired into BOTH `JimmyAvatar` (floating) and `CrmAiPanel` (lead-card Ask Jimmy tab): mic button (tap to talk, interim fills box, final auto-sends), speaker toggle (Jimmy reads replies aloud), Listening…/Speaking… status. iOS Safari supports both but with quirks; test on Aaron's phone.

### Messaging center → iMessage-style direct + GROUP chat (commit 8ef2881)
- Aaron's ask: messaging must work like iMessage — private AND group, whole team can see group threads.
- **Data model:** new `conversations` store key. `Conversation = {id, type:'direct'|'group', participants[], title?, createdBy, createdAt, updatedAt}`. Direct threads use deterministic id `dm:${[a,b].sort().join('|')}` so both people share one thread; groups `grp:${ts}`. Messages gained `conversationId` + `ts` (sort key); direct messages keep `to` as a server-auth backstop. Person keys are `${role}:${name}` (Admin→Manager), matching server `profileKey`.
- **`components/ChatProvider.tsx`** rewritten: manages conversations + messages, **polls every 5s** for live feel, exposes `ensureDirect`, `createGroup`, `sendToConversation`, `messagesFor`. Legacy 1:1 messages (no conversationId) are surfaced as derived direct threads so nothing is lost. Kept `sendMessage` for back-compat.
- **Server (`app/api/store/[key]/route.ts`)** is now participant-aware (SECURITY-critical): `filterForUser` is async; for `conversations` you only see threads you're in; for `messages` you see legacy to/from-you OR messages in a conversation you participate in (`myConversationIds`). `mergeMessages` is now **author-only** (`from === me`) — can't inject messages as a teammate. New `mergeConversations`: can create a new convo you're in or update one you created, but can't tamper with others' threads.
- **`app/private-chat/page.tsx`** rewritten into two-pane iMessage UI: conversation list (New Message / New Group composers) + bubble thread (sender names shown in groups), mobile back-stack. Nav label "Private Chat" → "Messages" with `MessageSquareText` icon. SectionHeader gained an optional `icon` prop (commit acc2145).

### Both original "things worth flagging" — DONE (commit da0b879)
1. **Playbook gated to chat-only** — moved `SALES_PLAYBOOK` + coaching instructions out of `SYSTEM_PROMPT` into a new `COACHING_PROMPT` that only `callJimmyChat` injects. Health-check / draft / next-action now use the lean base prompt (faster/cheaper).
2. **Per-rep coaching drills** — `remember_rep` tool gained a `drill` field; drills persist per rep in `repProfiles` (`p.drills[]`) and render in the snapshot's coaching memory. Jimmy is instructed (in COACHING_PROMPT) to assign a specific word track tied to a rep's known weakness and follow up on assigned drills.
- **Verify on prod (Aaron's phone):** group chat create + send across two accounts (isolation: rep must NOT see a group they're not in); Jimmy orb look; Jimmy mic + voice on iOS.
- Possible follow-ups: read receipts / unread badges on Messages (skipped in v1 to keep the server auth model clean); typing indicators; push notifications.

### Official Commissioned 41 logo (Aaron shared June 21)
- Brushed-**chrome "C41"** mark on black; the **"1" has a CROSS built into its top** (ties to the Great Commission meaning). Wordmark "COMMISSIONED 41" + tagline **"LIVE DATA. REAL ACCOUNTABILITY."**
- BRANDING PRINCIPLE (Aaron delegated, I set it; commit 1db5744): **the APP stays MissionOS-branded (login/favicon/app-icon/manifest) because it's the product; the COMPANY domain (commissioned41.com) gets the Commissioned 41 brand.** Did NOT change app icons. Added: `public/og-c41.png` (C41 share card) wired as the marketing site's OG/Twitter image; login footer now "A Commissioned 41 product" (replaced the stale "Live data. Real accountability." which is now MissionOS's tagline).
- DONE (commit 9a9ae84): the new chrome logo is now the canonical **`public/brand/commissioned41-logo.jpg`** (1206×1606, regenerated by Aaron in ChatGPT with the corrected tagline "Know Your Mission, Execute With Purpose" — note: comma, not periods; minor). Old gold "yucky yellow" + duplicate/older chrome copies removed. Wired into the **marketing footer** (header stays MissionOS = product; footer = Commissioned 41 = company; footer bg set black so the lockup blends). (Aaron declined my SVG vector logo attempt — keep using his ChatGPT chrome logo.)
- ✅ TAGLINE RESOLVED (Aaron, June 21): **Commissioned 41 tagline = "Know Your Mission. Execute With Purpose."** (he picked it for the company). **"Live Data. Real Accountability." → MissionOS** (the dealership/data product). The current logo art still shows "Live Data. Real Accountability." under COMMISSIONED 41 — update the lockup when convenient (or keep it tagline-free).

### What "Commissioned 41" means (the heart of the brand — Aaron, June 21)
- **Commissioned** = the **Great Commission** — Christ commissioned us to go tell the world the good news of Jesus Christ, our Lord and Savior (Matthew 28).
- **41** = a **new beginning / the chapter after the season of testing.** Biblical 40→41 pattern: flood 40 days & nights → 41st the sun came up; Israel wandered 40 years → 41st reached the Promised Land; Jesus 40 days in the wilderness → 41st He came out. So 41 = breakthrough after the trial — a new chapter (for Aaron personally and those C41 serves).
- It is **faith-rooted and personal** — handle with sincerity, never preachy/gimmicky. Captured in `BRAND.md` → "What the name means."
- (Reaffirmed: **MissionOS** = the operating system Aaron is building **for dealerships** — a tentacle of Commissioned 41.)

### Brand narrative → `BRAND.md` (CORRECTED June 21 — attribution fixed)
- **KEY CORRECTION (Aaron):** the broad life mission belongs to **COMMISSIONED 41 (the parent company)**, NOT MissionOS. Commissioned 41 is the company "with all the tentacles." **MissionOS is just ONE tentacle — the Dealership Operating System** (tied to the dealership). Earlier I'd wrongly pinned the whole life-OS manifesto on "MissionOS." `BRAND.md` was rewritten (commit pending) to attribute the manifesto to Commissioned 41 and frame MissionOS as the first product/tentacle.
- Hierarchy: **Commissioned 41** (company, the mission, many future tentacles) → **MissionOS** (tentacle #1, the dealership OS, live flagship) → inside it, **Jimmy** (in-store coach). **ILA** = company-level AI above the products.
- Tagline (Commissioned 41): **"Know Your Mission. Execute With Purpose."** MissionOS product line: "The Dealership Operating System."
- The live landing already aligns: it pitches MissionOS (dealership) and closes with "At Commissioned 41, we build tools that earn their place on your floor. MissionOS is the first." Good — no site change needed now (still dealership-first to build trust).
- ⚠️ LEFTOVER TO RECONCILE (not done): **"MissionOS Core"** (the owner-only PERSONAL exec OS at `/mission-core`) is mis-named under the corrected model — the personal/life OS is a Commissioned 41 thing, not a MissionOS (dealership) thing. Consider renaming it (e.g., to a Commissioned 41 personal OS) when Aaron's ready. Don't rename without his go-ahead.
- Voice: clear, direct, purposeful, faith-rooted but never preachy. "Commissioned" = every person commissioned for a purpose.
- **Surfaced as a public marketing landing (commit c16507e):** `app/welcome/page.tsx` — premium on-brand page (chrome wordmark, gold-gradient "Know Your Mission. Execute With Purpose." hero, manifesto, the 7 life areas, what-it-does, closing CTA). Public (`isPublicPath` includes `/welcome`) and shell-free (AppShell bypass). CTAs point to the app subdomain.
- **Domain split is now LIVE via `middleware.ts`:** the apex marketing hosts (`commissioned41.com` / `www.commissioned41.com`) rewrite root → `/welcome`. Matcher is `"/"` only, so it can NEVER affect app routes or the `missionos.commissioned41.com` subdomain. ⚠️ AARON: commissioned41.com now shows MARKETING at the root — **use https://missionos.commissioned41.com for the app.** (Other apex paths like commissioned41.com/crm-desk still serve the app for now; only root flipped.)
- DEV NOTE / lesson: do NOT run `npm run build` while the preview dev server is running on the same dir — it clobbers the dev `.next` and every route 500s with "Cannot find module './###.js'". Fix: stop preview, `rm -rf .next`, restart preview. (Hit this verifying /welcome.) For type-checking alongside a live dev server, use `npx tsc --noEmit` (doesn't touch `.next`), and verify rendering via the dev server (curl + preview screenshot) instead of a prod build.

### ⚠️ CRITICAL LESSON (June 21) — the dev server passing ≠ the production build passing
- Production was FROZEN for many commits because `next build` was failing on a TypeScript error in `app/waitlist/page.tsx` (`'stage' is specified more than once` — listing a key as a literal AND via spread of a non-optional-typed object). The DEV server compiled it fine, so I shipped many commits that never deployed (Vercel build failed → prod stuck on the last good build). Symptom: pushed assets 404 in prod (e.g. og-c41.png), pushed fixes not live.
- FIX commit 6bf9b42. RULE GOING FORWARD: **run `npm run build` (production build) before relying on a deploy**, especially after non-trivial TS/JSX changes — dev (`next dev`) is lenient; Vercel runs the strict build + typecheck + ESLint and FAILS the deploy on any error. To build without clobbering the live preview: `preview_stop` → `rm -rf .next` → `npm run build` → fix → `rm -rf .next` → `preview_start`. Or `npx tsc --noEmit` alongside dev (catches type errors, not all build/lint errors). Also: when a pushed change isn't live, FIRST suspect a failed build, not deploy lag.

### HARD RULE (Aaron, June 21) — never intertwine the SaaS business with a dealership's numbers
- **Mission Control (and every dealership-facing surface) is the DEALERSHIP's dashboard — it shows the dealer's store, nothing else.** Do NOT surface Commissioned 41 / MissionOS *business* metrics (waitlist signups, SaaS revenue, # of dealer clients, product KPIs) on Mission Control or any tenant-facing screen. Aaron: "I don't want to intertwine the success of my product into a dealership's numbers."
- The company/owner side (waitlist, product metrics, MissionOS Core) lives ONLY on owner-only surfaces (the "Owner" nav group: `/mission-core`, `/waitlist`), kept fully separate from the dealership app. Long-term this likely becomes a separate company/admin console from the tenant app — keep that separation clean as multi-tenancy lands.
- I proposed a "waitlist count badge on Mission Control" — Aaron rejected it. Don't do that.

### DIRECTION (Aaron, June 21) — use the dealer-OS patterns to run Commissioned 41 itself
- Aaron: *"If anything, I'd need what I built for the car business, for myself for this business."* The CRM/pipeline/leaderboard/accountability patterns from the Dealer OS are exactly what he needs to run **Commissioned 41** (the SaaS company that sells MissionOS to dealerships).
- Build this as an **owner-only company console** (think "Commissioned 41 HQ" / a Mission Control for HIS business), kept fully separate from the tenant dealership app per the HARD RULE above. Likely lives in the "Owner" nav group alongside MissionOS Core + Waitlist.
- Shape: a **dealership-prospect sales pipeline** (stages Lead/Waitlist → Demo → Trial → Won → Lost) FED BY the waitlist signups, with follow-ups/next-actions, plus revenue/MRR and # of stores. Reuse the CRM Desk / deal-flow patterns. The waitlist = top of this funnel.
- STATUS: **first slice BUILT (commit f23afb3).** `/waitlist` is now **Commissioned 41 HQ** — an owner-only sales pipeline: funnel summary (Lead/Demo/Trial/Won/Lost counts + win rate), stage filter, per-prospect stage advance + next-action + notes, copy-emails. Waitlist signups overlay into prospects (default Lead) via owner-only `hqPipeline` store key (pipeline state kept separate from the public `waitlist` capture so signups never get clobbered). Nav: "Commissioned 41 HQ" in the owner group.
- **Company Mission Control BUILT (commit 33569c2):** KPI row at top of HQ derived from the pipeline (no manual numbers): MRR (sum of Won prospect values), Stores Live (# Won), Active Pipeline (Demo+Trial value), Win Rate. Each prospect now has a monthly value ($/mo) field feeding the KPIs.
- **Commissioned 41 advisor BUILT (commit a6dafca):** a THIRD AI surface, owner-only — `app/api/ai/hq/route.ts`. Separate from Jimmy (dealership) and MissionOS Core (personal). Sees the live pipeline (waitlist + hqPipeline → MRR/stores/active/win-rate + every prospect), advises on closing stores & growing MRR, and can ACT via an `update_prospect` tool (advance stage, set next action, set $/mo, add note). chat + briefing actions; replies end with Recommended Action / Why It Matters / Next Step. UI: collapsible Advisor chat (voice mic + speak, quick actions) at the top of the HQ page; auto-refreshes KPIs when the advisor moves a deal.
- **NAMED ILA + HIERARCHY SET (commit c358edf).** The business advisor is **ILA** (spelling: I-L-A; "Iowa" is just voice-to-text). HIERARCHY (Aaron): **ILA lives at the Commissioned 41 (parent/company) level and can control EVERYTHING — the business AND the product.** **Jimmy lives INSIDE MissionOS** (the dealership product) and coaches a single store's floor. ILA sits ABOVE Jimmy. This mirrors the domain structure (commissioned41.com prime → missionos subdomain). Today ILA acts on the sales pipeline (update_prospect); "controls expand over time" — future: give ILA real controls over the platform/MissionOS/Jimmy config.
- THREE AI surfaces, kept distinct (all Opus 4.8 via `ANTHROPIC_API_KEY`): **ILA** (top-level Commissioned 41 company AI, owner-only, `/api/ai/hq`) · **Jimmy** (in-store dealership coach, tenant-facing, `/api/ai/crm`) · **MissionOS Core** (Aaron's personal exec OS, owner-only, `/api/ai/core`).
- NEXT (not built): kanban board view for the pipeline; wire real billing later to replace manual $/mo.

### Business costs / monthly burn (track this; Aaron asked to log expenses)
Recurring costs as of June 21, 2026:
- **Supabase Pro — $25/mo** (paid June 21; gives daily backups + no DB auto-pause — good for prod).
- **Vercel Pro — ~$20/mo** (Aaron is on Pro).
- **Domain commissioned41.com — ~$20/yr (~$1.70/mo)** (bought through Vercel).
- **Anthropic API (Jimmy/ILA/Core) — usage-based, variable** (cents per call; offset by the assistant add-on fee once billing lands).
- **Claude / Claude Code (the AI building this with Aaron) — $100/mo subscription**, plus **~$150 in usage credits consumed to date** (build cost).
- **(future) Stripe** — ~2.9% + 30¢ per charge (no monthly). **(future) business email** — free (Cloudflare routing) or ~$6/mo (Google Workspace).
- **Split the costs (CEO framing):** OPERATING burn to actually run the product for dealers ≈ **~$47/mo** (Supabase $25 + Vercel $20 + domain ~$1.70 + Anthropic API usage). BUILD/tooling investment (temporary, tapers once built) ≈ **$100/mo Claude + $150 credits so far**. Total cash out now ≈ **~$147/mo + the $150 credits**. The $100 Claude is a build accelerator, NOT a per-dealer cost — so unit economics per dealership are strong.
- OFFER MADE: build a live "Costs & Burn" panel in Commissioned 41 HQ so ILA can weigh burn vs MRR. Not built yet.

### PER-ORG STORE SETTINGS (built June 21 — branch `multitenant-rollout`, NOT in prod)
The "constants are Kennesaw/GA-specific" problem is solved. Same formulas, per-org constants, via one org-scoped `storeSettings` app_store key.
- **Model (`lib/data.ts`):** `StoreSettings = { storeName, docFee, holdbackPct (fraction, Mazda new = 0.06), tax: { rate, label, basis: "price_plus_docfee" | "price" }, productWeights: Record<ProductKey, number>, targets: { deliveredUnits, pvrTotal, frontEnd, backEnd, ppuMinimum, ppuElite } }`. `defaultStoreSettings` = Kennesaw/GA (docFee 899, holdback 6%, GA TAVT 7% on price+docfee, UTP=5, targets from the old `goals` const). The legacy `goals`/`productWeights`/`defaultDocFee` consts still exist — they're now just the source of those defaults.
- **How the math reads it:** module-level cache `activeStoreSettings` in data.ts (get/`setActiveStoreSettings`). The pure functions take an optional settings/weights param defaulting to the cache, so EVERY call site stays unchanged: `docFeeIncome`, `manufacturerMoney`, `productUnits`. Because doc fee / holdback / product weights are fully ENCAPSULATED in these functions, every screen got per-org for free. Added `estimatedTax(price, settings)` helper (not wired into deal math yet — no tax computation exists in the deal flow; tax is for the settings UI + future + Jimmy).
- **Provider (`components/StoreSettingsProvider.tsx`):** mirrors TeamProvider/GoalProvider. Loads `storeSettings`, deep-merges over defaults (`mergeSettings` coerces every field, tolerates a `{}` seed or partial/old record → never NaN), pushes into the data.ts cache (synchronously each render + on load), exposes `useStoreSettings()` (`{ settings, updateSettings, resetSettings, loaded }`), auto-saves on change. Mounted in `app/layout.tsx` directly inside `AuthProvider`, above `TeamProvider`.
- **The 5 screens that read TARGET numbers directly** (not via the encapsulated fns) were switched to `useStoreSettings().settings.targets`: Mission Control (`app/page.tsx` — was module consts frontGoal/backGoal/storeGoal), My Scorecard (threaded `targets` into `buildOneOnOne`), Deal Scorecard, Finance Command, GM Command.
- **Store API (`app/api/store/[key]/route.ts`):** `storeSettings` added to `allowedKeys`; `canWrite` = admin-only (like payplans/team). All roles READ it (client math needs it). **Provisioning:** `lib/provision.ts` seeds `storeSettings: {}` for new orgs (client merges in defaults until the admin customizes).
- **UI:** `/store-settings` (admin-only page, nav under Admin group, `Store` icon). Auto-saves. Sections: Store & Deal Economics (name/docFee/holdback%), Tax (label/rate%/applies-to), Product Weights (per product), Targets (units + PVR + PPU). Percentages edited as whole numbers, stored as fractions.
- **Verified:** `npx tsc --noEmit` clean; full `npm run build` clean (28 routes, `/store-settings` 4.58 kB); dev route serves 200, no console errors. NOT interactively tested logged-in (would need prod creds; branch local-dev points at un-migrated prod). Will exercise live after prod cutover.
- **NOT pushed to main** (branch only — goes live only with the multi-tenant prod cutover). **Follow-up:** wire Jimmy's crm prompt fee schedule from `storeSettings` (part of per-tenant Jimmy).

### "Build it as a legit company" roadmap (Aaron, June 21 — "do it right the first time")
Aaron wants C41 built as a real, legit company. Agreed order: Legal → Security/multi-tenancy → Billing → Business basics/ops. Status:
- ✅ **LEGAL DONE (commit 4ff32e1):** `/terms` (Terms of Service) + `/privacy` (Privacy Policy) — real, tailored to a dealership SaaS handling employee comp data + customer PII. Public, shell-free, served on apex, linked in marketing footer. `components/LegalPage.tsx` wrapper. Privacy discloses controller(dealer)/processor(C41) split + AI data sent to Anthropic (not used for training) + subprocessors (Vercel/Supabase/Anthropic). Contact emails in docs: legal@ / privacy@commissioned41.com (need email forwarding set up — see below). ⚠️ These are solid professional drafts — Aaron should have an attorney review before heavy reliance.
- 🟡 **SECURITY & MULTI-TENANCY (in progress):**
  - ✅ **ISOLATION PROVEN on staging (commit c788615).** Created a staging Supabase project (`whboyuuvmqcfytqtvoap`, keys in `.env.staging.local` gitignored). Built org schema + RLS (`supabase/migrations/0003_tenant_isolation_proof.sql`) and an automated test (`scripts/staging-tenant-test.mjs`) — **8/8 checks pass**: Store A can't see/write Store B; anon sees nothing. RLS at the DB enforces it regardless of app code. Run: `node scripts/staging-tenant-test.mjs`.
  - ✅ **APP RETROFIT BUILT + PROVEN (branch `multitenant-rollout`, not merged to main → prod untouched).** Migration `0004_app_multitenant.sql`: organizations, `user_profiles.org_id`, `app_store` gains `org_id` + composite PK `(org_id,key)` with Kennesaw backfill into org `00000000-...-0001`, `current_org_id()`, RLS backstop. Code: `lib/orgs.ts` (DEFAULT_ORG_ID + resolveOrgId); store route resolves caller org from profile and scopes EVERY app_store read/write + merge by org_id (returns 403 if no org); waitlist/core/hq/crm routes scope their app_store access to DEFAULT_ORG. Full prod build passes. **Proven on staging: `scripts/staging-appstore-test.mjs` → 6/6** (Dealer A can't see/write Dealer B; server-scoped reads correct). (Note: `slug` column dropped from the migration — the pre-existing 0003 `organizations` table lacked it and caused errors.)
  - ✅ **SELF-SERVE ONBOARDING BUILT + PROVEN (branch, commit 94e26a4).** Aaron's call: dealers sign up & pay themselves, NO manual provisioning, and the setup must be "idiot-proof" (car salesmen, some HS dropouts). Built: `lib/provision.ts` `provisionOrg()` (creates org + first admin (role Admin, their org) + seeds team/goals); public `/api/signup`; `/signup` = a one-question-at-a-time wizard (welcome → store name → name → email → password → done), plain language, examples, friendly errors, public + shell-free + apex-served. Marketing CTAs (nav/hero/mobile) → `/signup` "Start Free Trial". Proven on staging `scripts/staging-signup-test.mjs` → 4/4 (new dealer signs up, logs in, isolated from Kennesaw).
  - ✅ **PER-ORG STORE SETTINGS BUILT (branch `multitenant-rollout`, this session — see dedicated section below).** The math constants are no longer hardcoded to Kennesaw/GA: doc fee, holdback %, tax rule, product weights, and PVR/unit targets now live in an org-scoped `storeSettings` app_store key, edited at `/store-settings` (admin-only). The pure math functions read the active org's settings, falling back to Kennesaw defaults. (Pay-plan % was already org-scoped via `payplans`.) Remaining gap: Jimmy's crm prompt still hardcodes the GA fee schedule — that's the "per-tenant Jimmy" item, deferred (route is pinned to DEFAULT_ORG anyway, so values match today).
  - ⏭️ **REMAINING:** (1) **Stripe billing / paywall** — signup is a free trial now; gate access by subscription so dealers "just pay" (NEEDS Aaron's Stripe keys). (2) **Per-tenant Jimmy** — crm route pinned to DEFAULT_ORG (owner-only now); switch to caller's org when tenants use Jimmy. (3) **PROD CUTOVER (do together, careful, off-hours):** back up prod → run 0004 on prod (backfills Kennesaw → org #1) → merge `multitenant-rollout` → main (deploys) — order matters (DB before/with code, brief window). Until then the LOCAL dev preview on this branch errors on data screens (points at un-migrated prod) — expected.
  - ✅ Vercel token: removed from `.env.local`; Aaron revoking at vercel.com (UI friction — partial).
- ⏳ **BILLING:** Stripe subscriptions + 14-day trial; gate access by org paid flag; assistant as upsell. NEEDS: Aaron's Stripe account + API keys.
- ⏳ **BUSINESS BASICS (Aaron's to do; I provide steps):** EIN (IRS, free), business bank account, business email `aaron@commissioned41.com` (Google Workspace ~$6/mo OR free Cloudflare Email Routing → forward legal@/privacy@/aaron@ to gmail). Then swap gmail → domain email on CEO card + everywhere.
- ⏳ **OPS DISCIPLINE:** CI build-guard written at `.github/workflows/ci.yml` (runs `npm run build` on push/PR) BUT could not be pushed — the git PAT lacks GitHub `workflow` scope (`remote rejected ... without workflow scope`). File sits uncommitted in the working tree. TO FINISH: Aaron either (a) adds the file via GitHub web UI, or (b) grants the token `workflow` scope so I can push it. Interim guard: I run `npm run build` locally before pushing; Vercel already refuses to deploy a broken build.
- 🔐 **SECRET CLEANUP (partial, June 21):** removed `VERCEL_TOKEN` line from `.env.local` (was a full-account token; not referenced by app code; .env.local is gitignored so never in repo). ⚠️ Removing it from the file does NOT revoke it — Aaron must **rotate/revoke the token at vercel.com → Account → Tokens** since it has been sitting around and was used for API ops.

### Commissioned 41 core portal LIVE — hq.commissioned41.com (commit c1e9e42)
- Aaron asked "how do you log into Commissioned 41 if the only front door is MissionOS marketing?" — valid gap. Built a dedicated owner portal subdomain.
- **`hq.commissioned41.com`** added to the Vercel project via API (VERCEL_TOKEN in .env.local; project prj_jIBTbvOOZypHuQCatSbSBFQznniW, team team_tXukadBSYoTjMq7VRxpNk9UE) — returned `verified: true`, SSL auto-provisioned (apex managed by Vercel). Verified live: hq / → 307 → /waitlist → 200.
- Middleware (`middleware.ts`): `HQ_HOST = "hq.commissioned41.com"`; hq root "/" → 307 redirect to **/waitlist** (Commissioned 41 HQ = the core: pipeline + ILA + KPIs). All other hq routes pass through (same app). Same account/login as the product app (owner: aaronprice0918@gmail.com).
- Three front doors now: **commissioned41.com** = public marketing (→ /welcome) · **missionos.commissioned41.com** = the product app · **hq.commissioned41.com** = the owner/company core (→ Commissioned 41 HQ).
- REFINEMENT (optional, not done): the login on hq.* is still MissionOS-branded ("A Commissioned 41 product"); could host-detect to show a "Commissioned 41" login on hq.*. Also could give hq its own true core home page (currently reuses /waitlist) and host-aware nav. Low priority.

### DOMAIN ARCHITECTURE (confirmed Aaron's plan, June 21) — one prime domain + subdomains
- **commissioned41.com = PRIME domain = the company (marketing front door).** Products/areas branch off as subdomains ("tentacles"). **missionos.commissioned41.com = the MissionOS app.** Aaron's instinct was correct; this is the standard, scalable model. Subdomains are ~free DNS records on Vercel.
- **ENFORCED via `middleware.ts` (commit 33569c2):** apex (`commissioned41.com`/`www`) serves ONLY marketing — the landing, its assets, public APIs (waitlist POST is called from the landing so `/api/*` stays on apex), `/brand/`, `/card/` share links, and static files; **every other (app) route on the apex 307-redirects to `missionos.commissioned41.com`.** The app subdomain + all other hosts (localhost, vercel.app) pass through untouched. Matcher excludes `_next/static|_next/image|favicon`.
- Owner/company console (Commissioned 41 HQ, MissionOS Core) currently lives INSIDE the MissionOS app as owner-only routes — fine for now; could move to its own subdomain (e.g. `hq.commissioned41.com`) later if it grows.
- ⚠️ Effect: hitting an app URL on the bare apex now bounces to the subdomain (one-time re-login since auth is per-origin). Intended.
- 🐞 BUG FIXED (commit 5690efd): apex "/" originally used a middleware **rewrite** to /welcome — but a rewrite keeps the URL "/", so `usePathname()` returned "/", which made **AppShell wrap the marketing page in the dashboard shell** and render the **owner-only Jimmy** on the public landing (and showed login to logged-out visitors). FIX: apex "/" now **307 REDIRECTS** to /welcome (which AppShell + AuthProvider already bypass → truly public, shell-free, no Jimmy). Tradeoff: apex URL shows /welcome. Verified locally via `curl -H "Host: commissioned41.com" localhost:3000/` → 307. LESSON: to render a public/shell-free page at a domain root in this single-app setup, REDIRECT to the public path — never rewrite (client pathname stays at the root and re-triggers shell/auth). Jimmy gating itself was always correct (UI: isOwner; API: 403) — the rewrite just wrongly exposed the shell.

### PRODUCT STRATEGY (Aaron, June 21) — dealership-first, then modular life OS
- **MissionOS = a modular, tiered platform you buy into and upgrade.** The CORE / main component is the **Dealer Operating System** (the live flagship). Future add-on modules, tailored per user: Time Management, Personal Life, Faith & Spiritual, Health & Growth, Leadership. "Buy the core, add what fits your life, upgrade anytime."
- **RIGHT NOW: focus the public product & marketing ONLY on the Dealership OS.** Earn dealers' trust by nailing it flawlessly FIRST; expand into the life/modules later. The modular/life vision is internal context — do NOT market it yet.
- **Founder story (credibility anchor):** built by a finance director with **20 years in the car business** who's lived/breathed the floor — not a software company guessing. Use this in copy.

### Marketing site refocused to dealership-only (commit 9cfaa8b)
- `app/welcome/page.tsx` rewritten as a focused **Dealership OS** single-page site (sticky anchor nav): hero "The Dealership Operating System" + "live and running a real dealership today," Features (CRM Desk, desking, F&I, leaderboards, Jimmy), Why MissionOS (trust/accuracy, real-time, mobile-first, built by an insider), pricing tiers (Single Store / Dealer Group / Enterprise — demo-led, no fake numbers), founder story, demo capture. (Earlier modules/life/7-areas marketing was pulled per the dealership-first call.)
- **Waitlist live:** `app/api/waitlist/route.ts` — public POST (anyone signs up), owner-only GET; stores in `app_store.waitlist`. `components/WaitlistForm.tsx` client form (source tagged "dealership"). Owner can read signups via GET /api/waitlist with auth, or in Supabase.

### Jimmy's look redesigned (commit bac7612) — Aaron rejected the neon orb
- Now a **translucent, see-through light Tiffany-blue glass orb that constantly breathes** (gentle scale + glow pulse) so he feels alive; breathing quickens while speaking/listening; respects `prefers-reduced-motion`. CSS in `ORB_STYLES` (`jimmyBreath`/`jimmyGlow` keyframes) in `components/JimmyAvatar.tsx`. Palette: rgba(129,216,208,*) Tiffany teal. Verified visually by injecting the CSS into the preview (orb only renders for owner inside the authed app).
- PREVIEW QUIRK: the preview viewport was stuck at width=2px → screenshots came out as slivers. Fix: `preview_resize` with explicit width/height (e.g. 1100x760).

### MissionOS Core — owner-only personal executive OS (commit b64f5ef)
- Aaron supplied a "MissionOS Core Intelligence Prompt" (CEO/Chief-of-Staff directives across faith, family, health, finances, business). DECISION (asked & confirmed): build it as a SEPARATE, owner-only brain — NOT merged into Jimmy. Two brains now:
  - **Jimmy** → dealership GSM/sales coach, dealership-scoped, sold to tenants (`app/api/ai/crm/route.ts`).
  - **MissionOS Core** → Aaron's PRIVATE life/executive OS, owner-only (`app/api/ai/core/route.ts`). Never shown to staff/tenants.
- Why separate: merging personal-life directives into Jimmy would surface faith/family coaching to every dealership tenant. Kept isolated on purpose.
- **Route `app/api/ai/core/route.ts`:** owner-gated via `isOwnerEmail`. Core Intelligence system prompt (10 directives + prime objective + ends every reply with Recommended Action / Why It Matters / Next Step). Persistent personal memory in owner-only store key **`missionCore`** (`{northStar, goals[{horizon,area,status,due}], priorities[], notes[]}`) via an `update_mission` tool loop; `chat` + `briefing` actions. Model = claude-opus-4-8.
- **Store route:** `missionCore` added to allowedKeys; `filterForUser` returns null for non-admin; `canWrite` admin-only. Owner's data is private.
- **Page `app/mission-core/page.tsx`:** owner-only (gates on `useAuth().isOwner`). Chat with voice (reuses `lib/voice.ts`) + quick actions ("Give me my briefing", etc.) + a live **Mission Control panel** (north star, active goals grouped by horizon Today→Year, top priorities) that refreshes after each turn as the OS updates its memory.
- **Nav:** owner-only "Owner → MissionOS Core" group in `AppShell.tsx` (hidden unless `isOwner`).
- **Verify on prod (owner login):** open MissionOS Core, run a briefing, set a goal/north star, confirm it persists in `app_store.missionCore` and shows in the panel; confirm a non-owner can't see the nav item or the page.
- Possible next: wire Core to read real dashboard data (deals/goals/calendar) for grounded briefings; scheduled morning briefing (push); decision-intelligence template UI.
