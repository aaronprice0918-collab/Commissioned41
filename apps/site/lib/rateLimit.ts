// Re-exported from the shared package.
// clientIp is the Site-specific import; rateLimit returns {ok, remaining, limit}.
export { rateLimit, clientIp } from "@commissioned41/rate-limit";
export type { RateResult } from "@commissioned41/rate-limit";
